---
phase: 23-signals-risk-pipeline-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/package.json
  - dashboard/src/types/api.ts
  - dashboard/src/hooks/use-signals.ts
  - dashboard/src/hooks/use-risk.ts
  - dashboard/src/hooks/use-pipeline.ts
  - dashboard/src/components/ui/data-table.tsx
  - dashboard/src/components/ui/progress.tsx
  - dashboard/src/components/ui/input.tsx
  - dashboard/src/components/ui/label.tsx
  - dashboard/src/components/ui/checkbox.tsx
  - dashboard/src/components/ui/select.tsx
  - dashboard/src/components/signals/columns.tsx
  - dashboard/src/components/signals/signal-cards.tsx
  - dashboard/src/app/(dashboard)/signals/page.tsx
autonomous: true
requirements: [SGNL-01, SGNL-02, SGNL-03]

must_haves:
  truths:
    - "Scoring table displays symbols with composite score, risk-adjusted score, and signal columns"
    - "Clicking column headers on composite and risk-adjusted toggles sort direction"
    - "Signal recommendation cards show BUY/SELL/HOLD with a numeric strength indicator"
  artifacts:
    - path: "dashboard/src/types/api.ts"
      provides: "ScoreRow, SignalItem, SignalsData, RiskData, PipelineData types"
      contains: "SignalsData"
    - path: "dashboard/src/hooks/use-signals.ts"
      provides: "useSignals TanStack Query hook"
      exports: ["useSignals"]
    - path: "dashboard/src/hooks/use-risk.ts"
      provides: "useRisk TanStack Query hook"
      exports: ["useRisk"]
    - path: "dashboard/src/hooks/use-pipeline.ts"
      provides: "usePipeline query + mutation hooks"
      exports: ["usePipeline"]
    - path: "dashboard/src/components/ui/data-table.tsx"
      provides: "Generic DataTable component wrapping TanStack Table"
      exports: ["DataTable"]
    - path: "dashboard/src/components/signals/columns.tsx"
      provides: "Sortable column definitions for scoring table"
      exports: ["scoringColumns"]
    - path: "dashboard/src/components/signals/signal-cards.tsx"
      provides: "Signal recommendation cards"
      exports: ["SignalCards"]
    - path: "dashboard/src/app/(dashboard)/signals/page.tsx"
      provides: "Signals page replacing stub"
      contains: "useSignals"
  key_links:
    - from: "dashboard/src/app/(dashboard)/signals/page.tsx"
      to: "/api/v1/dashboard/signals"
      via: "useSignals hook"
      pattern: "useSignals"
    - from: "dashboard/src/components/signals/columns.tsx"
      to: "dashboard/src/types/api.ts"
      via: "ColumnDef<ScoreRow>"
      pattern: "ColumnDef<ScoreRow>"
    - from: "dashboard/src/components/ui/data-table.tsx"
      to: "@tanstack/react-table"
      via: "useReactTable + getSortedRowModel"
      pattern: "getSortedRowModel"
---

<objective>
Install shared dependencies (@tanstack/react-table, shadcn form components), create TypeScript types for all 3 pages, create TanStack Query hooks for all 3 pages, build a reusable DataTable component, and build the Signals page with sortable scoring table and signal recommendation cards.

Purpose: Establishes the shared data layer (types + hooks) that Plans 02 and 03 also depend on, and delivers the Signals page as a complete vertical slice.
Output: Working Signals page with sortable scoring table and signal cards, plus shared types/hooks/components for remaining pages.
</objective>

<execution_context>
@/home/mqz/.claude/get-shit-done/workflows/execute-plan.md
@/home/mqz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-signals-risk-pipeline-pages/23-RESEARCH.md
@.planning/phases/22-design-system-overview-page/22-01-SUMMARY.md
@.planning/phases/22-design-system-overview-page/22-02-SUMMARY.md

<interfaces>
<!-- Existing types and patterns the executor needs. -->

From dashboard/src/types/api.ts (extend this file):
```typescript
export interface OverviewData { ... }
export interface Position { ... }
// Add: ScoreRow, SignalItem, SignalsData, RiskData, PipelineData, etc.
```

From dashboard/src/hooks/use-overview.ts (follow this pattern):
```typescript
import { useQuery } from '@tanstack/react-query';
import type { OverviewData } from '@/types/api';

export function useOverview() {
  return useQuery<OverviewData>({
    queryKey: ['overview'],
    queryFn: async () => {
      const res = await fetch('/api/v1/dashboard/overview');
      if (!res.ok) throw new Error(`Backend error: ${res.status}`);
      return res.json();
    },
    staleTime: 30_000,
    retry: 1,
  });
}
```

From dashboard/src/lib/formatters.ts:
```typescript
export function formatCurrency(value: number): string;
export function formatPercent(value: number): string;
export function formatScore(value: number): string;
export function formatDate(dateStr: string): string;
```

From dashboard/src/components/ui/button.tsx:
```typescript
// Available variants: default, outline, secondary, ghost, destructive, link
// Available sizes: default, xs, sm, lg, icon, icon-xs, icon-sm, icon-lg
export { Button, buttonVariants };
```

From dashboard/src/app/(dashboard)/page.tsx (follow page pattern):
```typescript
'use client';
// Import hook, components, Card/CardHeader/CardTitle/CardContent, Skeleton
// Page: loading skeleton -> error state -> data rendering
```

Backend API contract (from src/dashboard/presentation/api_routes.py):
- GET /api/v1/dashboard/signals -> { scores: ScoreRow[], signals: SignalItem[] }
- GET /api/v1/dashboard/risk -> { drawdown_pct, drawdown_level, sector_weights, position_count, max_positions, regime }
- GET /api/v1/dashboard/pipeline -> { pipeline_runs, next_scheduled, approval_status, daily_budget, review_queue }
- POST /api/v1/dashboard/pipeline/run -> { status, symbols, dry_run }
- POST /api/v1/dashboard/approval/create -> { approval_status, daily_budget }
- POST /api/v1/dashboard/approval/suspend -> { approval_status, daily_budget }
- POST /api/v1/dashboard/approval/resume -> { approval_status, daily_budget }
- POST /api/v1/dashboard/review/approve -> { review_queue }
- POST /api/v1/dashboard/review/reject -> { review_queue }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies, add shadcn components, create shared types and hooks</name>
  <files>
    dashboard/package.json
    dashboard/src/types/api.ts
    dashboard/src/hooks/use-signals.ts
    dashboard/src/hooks/use-risk.ts
    dashboard/src/hooks/use-pipeline.ts
    dashboard/src/components/ui/progress.tsx
    dashboard/src/components/ui/input.tsx
    dashboard/src/components/ui/label.tsx
    dashboard/src/components/ui/checkbox.tsx
    dashboard/src/components/ui/select.tsx
  </files>
  <action>
1. Install @tanstack/react-table:
   ```bash
   cd /home/mqz/workspace/trading/dashboard && npm install @tanstack/react-table
   ```

2. Add shadcn form components:
   ```bash
   cd /home/mqz/workspace/trading/dashboard && npx shadcn@latest add progress input label checkbox select --yes
   ```
   Then run `npx biome check --write .` to auto-format to single-quote convention (shadcn generates double quotes).

3. Extend `dashboard/src/types/api.ts` — append these types after existing ones (do NOT modify existing types):
   - `ScoreRow` with fields: symbol (string), composite (number), risk_adjusted (number), strategy (string), signal (string)
   - `SignalItem` with fields: symbol (string), direction (string), strength (number), metadata (Record<string, unknown> | string)
   - `SignalsData` with fields: scores (ScoreRow[]), signals (SignalItem[])
   - `RiskData` with fields: drawdown_pct (number), drawdown_level (string), sector_weights (Record<string, number>), position_count (number), max_positions (number), regime (string)
   - `PipelineStage` with fields: name (string), status (string), symbol_count (number)
   - `PipelineRun` with fields: run_id (string), started_at (string), completed_at (string), status (string), stages (PipelineStage[])
   - `ApprovalStatus` with fields: id (string), score_threshold (number), allowed_regimes (string[]), max_per_trade_pct (number), daily_budget_cap (number), expires_at (string), is_active (boolean), is_suspended (boolean), suspended_reasons (string[]), status (string)
   - `DailyBudget` with fields: spent (number), limit (number), remaining (number)
   - `ReviewItem` with fields: id (number), symbol (string), strategy (string), score (number), reason (string | null), created_at (string)
   - `PipelineData` with fields: pipeline_runs (PipelineRun[]), next_scheduled (string), approval_status (ApprovalStatus | null), daily_budget (DailyBudget), review_queue (ReviewItem[])

4. Create `dashboard/src/hooks/use-signals.ts` following useOverview pattern:
   - useQuery<SignalsData> with queryKey ['signals'], fetch from '/api/v1/dashboard/signals', staleTime 30_000, retry 1

5. Create `dashboard/src/hooks/use-risk.ts` following useOverview pattern:
   - useQuery<RiskData> with queryKey ['risk'], fetch from '/api/v1/dashboard/risk', staleTime 30_000, retry 1

6. Create `dashboard/src/hooks/use-pipeline.ts` with BOTH queries and mutations:
   - `usePipeline()` — useQuery<PipelineData> with queryKey ['pipeline'], fetch from '/api/v1/dashboard/pipeline', staleTime 30_000, retry 1
   - `usePipelineRun()` — useMutation for POST /api/v1/dashboard/pipeline/run with body { symbols: string[], dry_run: boolean }. On success, invalidate queryKey ['pipeline'].
   - `useApprovalCreate()` — useMutation for POST /api/v1/dashboard/approval/create with ApprovalCreateRequest body. On success, invalidate ['pipeline'].
   - `useApprovalSuspend()` — useMutation for POST /api/v1/dashboard/approval/suspend with { approval_id?: string }. On success, invalidate ['pipeline'].
   - `useApprovalResume()` — useMutation for POST /api/v1/dashboard/approval/resume with { approval_id?: string }. On success, invalidate ['pipeline'].
   - `useReviewAction(action: 'approve' | 'reject')` — useMutation for POST /api/v1/dashboard/review/{action} with { review_id: number }. On success, invalidate ['pipeline'].

   All mutations: method 'POST', Content-Type 'application/json', JSON.stringify body, throw on !res.ok.

7. Verify: `cd /home/mqz/workspace/trading/dashboard && npx tsc --noEmit && npx biome check .`
  </action>
  <verify>
    <automated>cd /home/mqz/workspace/trading/dashboard && npx tsc --noEmit && npx biome check .</automated>
  </verify>
  <done>@tanstack/react-table installed, 5 shadcn components added, TypeScript types for all 3 pages defined, TanStack Query hooks for signals/risk/pipeline created with mutations. tsc and biome pass.</done>
</task>

<task type="auto">
  <name>Task 2: Build DataTable component and Signals page with sortable scoring table and signal cards</name>
  <files>
    dashboard/src/components/ui/data-table.tsx
    dashboard/src/components/signals/columns.tsx
    dashboard/src/components/signals/signal-cards.tsx
    dashboard/src/app/(dashboard)/signals/page.tsx
  </files>
  <action>
1. Create `dashboard/src/components/ui/data-table.tsx` — generic reusable DataTable:
   - 'use client' directive
   - Import ColumnDef, SortingState, flexRender, getCoreRowModel, getSortedRowModel, useReactTable from @tanstack/react-table
   - Import Table, TableBody, TableCell, TableHead, TableHeader, TableRow from @/components/ui/table
   - Generic interface DataTableProps<TData, TValue> with columns: ColumnDef<TData, TValue>[] and data: TData[]
   - useState<SortingState>([]) for sorting state
   - useReactTable with data, columns, getCoreRowModel(), onSortingChange, getSortedRowModel(), state: { sorting }
   - Render table headers via flexRender(header.column.columnDef.header, header.getContext())
   - Render rows via flexRender(cell.column.columnDef.cell, cell.getContext())
   - Empty state: "No results." in centered TableCell spanning all columns

2. Create `dashboard/src/components/signals/columns.tsx` — column definitions (OUTSIDE component to avoid recreating on each render):
   - 'use client' directive
   - Import ColumnDef from @tanstack/react-table, ArrowUpDown from lucide-react, Badge/Button from shadcn, formatScore from formatters, ScoreRow from types
   - Define scoringColumns: ColumnDef<ScoreRow>[] with:
     - 'symbol': header "Symbol", cell renders font-medium text
     - 'composite': header is sortable Button (ghost variant, px-0) with ArrowUpDown icon. onClick calls column.toggleSorting(column.getIsSorted() === 'asc'). Cell renders font-mono tabular-nums with formatScore.
     - 'risk_adjusted': same sortable pattern as composite, header "Risk-Adj"
     - 'strategy': header "Strategy", cell renders plain text
     - 'signal': header "Signal", cell renders Badge with variant mapping: BUY -> 'default' (amber), SELL -> 'destructive' (red), HOLD/-- -> 'secondary' (muted)

3. Create `dashboard/src/components/signals/signal-cards.tsx`:
   - 'use client' directive
   - Import Card/CardContent/CardHeader/CardTitle, Badge from shadcn, SignalItem from types
   - SignalCards component receives items: SignalItem[]
   - If no items, show "No active signals" muted text
   - Render responsive grid (grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4)
   - Each card shows: symbol (CardTitle), direction Badge (same variant mapping as scoring table), strength as numeric value (font-mono tabular-nums) paired with a small progress-like bar (div with width proportional to strength 0-1, using semantic color matching direction)
   - Include aria-label on strength indicator for accessibility

4. Replace `dashboard/src/app/(dashboard)/signals/page.tsx` stub entirely:
   - 'use client' directive
   - Import useSignals hook, DataTable, scoringColumns, SignalCards, Card/CardHeader/CardTitle/CardContent, Skeleton
   - Follow OverviewPage pattern: loading skeleton -> error state -> data rendering
   - Loading skeleton: h-8 title placeholder, h-64 table placeholder, h-48 cards placeholder
   - Error: text-destructive with "Backend connection failed: {error.message}"
   - Data layout:
     - h1 "Signals" title
     - Card wrapping DataTable with scoringColumns and data.scores
     - Card wrapping SignalCards with data.signals

5. Verify: `cd /home/mqz/workspace/trading/dashboard && npx tsc --noEmit && npx biome check . && npm run build`
  </action>
  <verify>
    <automated>cd /home/mqz/workspace/trading/dashboard && npx tsc --noEmit && npx biome check . && npm run build</automated>
  </verify>
  <done>DataTable component renders with sortable columns. Signals page shows scoring table with sortable composite/risk-adjusted columns and signal recommendation cards with BUY/SELL/HOLD badges and strength indicators. Full build passes.</done>
</task>

</tasks>

<verification>
cd /home/mqz/workspace/trading/dashboard && npx tsc --noEmit && npx biome check . && npm run build
</verification>

<success_criteria>
- @tanstack/react-table installed and 5 shadcn form components available
- TypeScript types for SignalsData, RiskData, PipelineData match FastAPI response shapes
- useSignals, useRisk, usePipeline hooks created with correct query keys
- usePipelineRun, useApprovalCreate/Suspend/Resume, useReviewAction mutations defined with cache invalidation
- DataTable generic component wraps TanStack Table with sorting support
- Signals page displays scoring table with sortable composite and risk-adjusted columns
- Signal cards display BUY/SELL/HOLD with strength indicator and numeric value
- Full build (next build) passes
</success_criteria>

<output>
After completion, create `.planning/phases/23-signals-risk-pipeline-pages/23-01-SUMMARY.md`
</output>
