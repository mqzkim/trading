---
phase: 23-signals-risk-pipeline-pages
plan: 02
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - dashboard/src/components/risk/drawdown-gauge.tsx
  - dashboard/src/components/risk/sector-donut.tsx
  - dashboard/src/components/risk/position-limits.tsx
  - dashboard/src/components/risk/regime-badge.tsx
  - dashboard/src/app/(dashboard)/risk/page.tsx
autonomous: true
requirements: [RISK-01, RISK-02, RISK-03, RISK-04]

must_haves:
  truths:
    - "Drawdown gauge shows current drawdown percentage with green/yellow/red coloring based on tier"
    - "Sector donut chart shows proportional sector allocation with color legend"
    - "Position limit bars show current count vs maximum (N/20)"
    - "Regime badge displays current market regime with semantic color"
  artifacts:
    - path: "dashboard/src/components/risk/drawdown-gauge.tsx"
      provides: "CSS arc gauge with 3-tier coloring"
      exports: ["DrawdownGauge"]
    - path: "dashboard/src/components/risk/sector-donut.tsx"
      provides: "CSS conic-gradient donut chart"
      exports: ["SectorDonut"]
    - path: "dashboard/src/components/risk/position-limits.tsx"
      provides: "Progress bar for position count"
      exports: ["PositionLimits"]
    - path: "dashboard/src/components/risk/regime-badge.tsx"
      provides: "Colored regime badge"
      exports: ["RegimeBadge"]
    - path: "dashboard/src/app/(dashboard)/risk/page.tsx"
      provides: "Risk page replacing stub"
      contains: "useRisk"
  key_links:
    - from: "dashboard/src/app/(dashboard)/risk/page.tsx"
      to: "/api/v1/dashboard/risk"
      via: "useRisk hook"
      pattern: "useRisk"
    - from: "dashboard/src/components/risk/drawdown-gauge.tsx"
      to: "dashboard/src/types/api.ts"
      via: "RiskData.drawdown_pct and drawdown_level"
      pattern: "drawdown_pct|drawdown_level"
    - from: "dashboard/src/components/risk/sector-donut.tsx"
      to: "dashboard/src/types/api.ts"
      via: "RiskData.sector_weights"
      pattern: "sector_weights"
---

<objective>
Build the Risk page with CSS-based drawdown gauge, sector exposure donut chart, position limit progress bars, and market regime badge.

Purpose: Provides the trader with a visual risk dashboard showing drawdown health, sector concentration, position capacity, and current market regime -- all without adding chart libraries (pure CSS).
Output: Fully functional Risk page with 4 visualization components.
</objective>

<execution_context>
@/home/mqz/.claude/get-shit-done/workflows/execute-plan.md
@/home/mqz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-signals-risk-pipeline-pages/23-RESEARCH.md
@.planning/phases/23-signals-risk-pipeline-pages/23-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (must be completed first) -->

From dashboard/src/types/api.ts:
```typescript
export interface RiskData {
  drawdown_pct: number;
  drawdown_level: string;  // "normal" | "warning" | "critical"
  sector_weights: Record<string, number>;
  position_count: number;
  max_positions: number;
  regime: string;
}
```

From dashboard/src/hooks/use-risk.ts:
```typescript
export function useRisk(): UseQueryResult<RiskData>;
```

From dashboard/src/lib/formatters.ts:
```typescript
export function formatPercent(value: number): string;
```

Available shadcn components: Card, CardContent, CardHeader, CardTitle, Badge, Skeleton, Button, Progress, Separator
Available theme tokens: text-profit (cyan), text-loss (red), text-interactive (amber), text-muted-foreground, bg-card
Available font classes: font-mono tabular-nums
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build drawdown gauge, sector donut, position limits, and regime badge components</name>
  <files>
    dashboard/src/components/risk/drawdown-gauge.tsx
    dashboard/src/components/risk/sector-donut.tsx
    dashboard/src/components/risk/position-limits.tsx
    dashboard/src/components/risk/regime-badge.tsx
  </files>
  <action>
1. Create `dashboard/src/components/risk/drawdown-gauge.tsx`:
   - 'use client' directive
   - Props: { pct: number; level: string } (pct is 0-100, level is "normal"|"warning"|"critical")
   - CSS semicircle arc gauge using conic-gradient:
     - Map 0-30% drawdown to 0-180 degrees (clamp at 30%)
     - Fill color based on level: normal -> oklch(0.75 0.18 180) (profit/green-cyan), warning -> oklch(0.78 0.15 75) (amber), critical -> oklch(0.65 0.22 25) (red)
     - Track color: oklch(0.2 0.005 250) (dark muted background)
     - Gradient: conic-gradient(from 180deg, fillColor 0deg Xdeg, trackColor Xdeg 180deg, transparent 180deg)
   - Layout: relative container h-20 w-40 overflow-hidden, inner div h-40 w-40 rounded-full with gradient, inner cutout div for donut effect, center label showing pct with semantic color class (text-profit/text-interactive/text-loss), "Drawdown" label below
   - Use key={pct} on the gradient div to force re-render on data changes

2. Create `dashboard/src/components/risk/sector-donut.tsx`:
   - 'use client' directive
   - Props: { sectors: Record<string, number> }
   - Define SECTOR_COLORS array with 6 distinguishable oklch colors
   - Build conic-gradient string from sector entries, accumulating percentages
   - Layout: flex row with donut (h-32 w-32, rounded-full, inner cutout via absolute inset-4 rounded-full bg-card) and legend (sector name with color swatch + percentage in font-mono tabular-nums)
   - Empty state: "No positions" text-muted-foreground
   - Use key={JSON.stringify(sectors)} to force re-render on data changes

3. Create `dashboard/src/components/risk/position-limits.tsx`:
   - 'use client' directive
   - Props: { count: number; max: number }
   - Import Progress from @/components/ui/progress
   - Show label "Position Limits" with "{count} / {max}" in font-mono tabular-nums
   - Progress component with value={(count / max) * 100}
   - Color hint: if count/max > 0.8, add text-interactive warning color to the count number

4. Create `dashboard/src/components/risk/regime-badge.tsx`:
   - 'use client' directive
   - Props: { regime: string }
   - Import Badge from shadcn
   - Color mapping using className:
     - Bull: bg-profit/20 text-profit border-profit/30
     - Bear: bg-loss/20 text-loss border-loss/30
     - Accumulation: bg-chart-1/20 text-chart-1 border-chart-1/30 (or use a blue-ish oklch)
     - Distribution: bg-interactive/20 text-interactive border-interactive/30
     - Unknown/default: bg-muted text-muted-foreground
   - Render Badge variant="outline" with mapped className

5. Verify: `cd /home/mqz/workspace/trading/dashboard && npx tsc --noEmit && npx biome check .`
  </action>
  <verify>
    <automated>cd /home/mqz/workspace/trading/dashboard && npx tsc --noEmit && npx biome check .</automated>
  </verify>
  <done>All 4 risk visualization components type-check and lint cleanly. DrawdownGauge uses CSS conic-gradient with 3-tier coloring. SectorDonut builds conic-gradient from sector weights. PositionLimits uses shadcn Progress. RegimeBadge maps regime to semantic colors.</done>
</task>

<task type="auto">
  <name>Task 2: Assemble Risk page replacing stub</name>
  <files>
    dashboard/src/app/(dashboard)/risk/page.tsx
  </files>
  <action>
1. Replace `dashboard/src/app/(dashboard)/risk/page.tsx` stub entirely:
   - 'use client' directive
   - Import useRisk hook, DrawdownGauge, SectorDonut, PositionLimits, RegimeBadge, Card/CardHeader/CardTitle/CardContent, Skeleton, Separator
   - Follow OverviewPage pattern: loading skeleton -> error state -> data rendering
   - Loading skeleton: h-8 title placeholder, grid with h-48 skeleton (gauge area), h-48 skeleton (donut area), h-24 skeleton (position limits), h-8 skeleton (regime badge)
   - Error: text-destructive with "Backend connection failed: {error.message}"
   - Data layout:
     - h1 "Risk" title
     - Top row (grid grid-cols-1 lg:grid-cols-2 gap-6):
       - Card "Drawdown" containing DrawdownGauge with pct={data.drawdown_pct} level={data.drawdown_level}
       - Card "Sector Exposure" containing SectorDonut with sectors={data.sector_weights}
     - Bottom row (grid grid-cols-1 lg:grid-cols-2 gap-6):
       - Card "Position Limits" containing PositionLimits with count={data.position_count} max={data.max_positions}
       - Card "Market Regime" containing RegimeBadge with regime={data.regime}, plus a descriptive label (e.g., "Current market regime detected by HMM model")

2. Verify: `cd /home/mqz/workspace/trading/dashboard && npx tsc --noEmit && npx biome check . && npm run build`
  </action>
  <verify>
    <automated>cd /home/mqz/workspace/trading/dashboard && npx tsc --noEmit && npx biome check . && npm run build</automated>
  </verify>
  <done>Risk page renders drawdown gauge with 3-tier coloring, sector donut chart, position limit progress bar, and regime badge. Full build passes.</done>
</task>

</tasks>

<verification>
cd /home/mqz/workspace/trading/dashboard && npx tsc --noEmit && npx biome check . && npm run build
</verification>

<success_criteria>
- Drawdown gauge renders semicircle arc with percentage and 3-tier color (green <=10%, amber <=15%, red >15%)
- Sector donut chart renders proportional segments with color legend
- Position limits show progress bar with count/max text
- Regime badge shows regime name with semantic color
- Risk page assembles all 4 components in Card layout with loading/error states
- Full build passes
</success_criteria>

<output>
After completion, create `.planning/phases/23-signals-risk-pipeline-pages/23-02-SUMMARY.md`
</output>
