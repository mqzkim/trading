---
phase: 22-design-system-overview-page
plan: 02
type: execute
wave: 2
depends_on: [22-01]
files_modified:
  - dashboard/src/components/overview/kpi-cards.tsx
  - dashboard/src/components/overview/holdings-table.tsx
  - dashboard/src/components/overview/equity-curve.tsx
  - dashboard/src/components/overview/trade-history.tsx
  - dashboard/src/app/(dashboard)/page.tsx
autonomous: false
requirements: [OVER-01, OVER-02, OVER-03, OVER-04]

must_haves:
  truths:
    - "KPI cards display total portfolio value, daily P&L (color-coded), current drawdown (3-tier coloring), and pipeline status"
    - "Holdings table shows each position with ticker, qty, price, P&L (color-coded cyan/red), stop, target, and composite score in monospace"
    - "TradingView equity curve chart renders with regime background coloring (Bull=green, Bear=red, Accumulation=blue, Distribution=amber)"
    - "Trade history table displays past executed trades with symbol, direction, prices, qty, value, score, status, date"
    - "Overview page assembles all 4 sections with loading skeleton and error state"
  artifacts:
    - path: "dashboard/src/components/overview/kpi-cards.tsx"
      provides: "4 KPI metric cards using shadcn Card"
      contains: "formatCurrency"
    - path: "dashboard/src/components/overview/holdings-table.tsx"
      provides: "Holdings table using shadcn Table with semantic P&L coloring"
      contains: "text-profit"
    - path: "dashboard/src/components/overview/equity-curve.tsx"
      provides: "TradingView Lightweight Charts equity curve with regime histogram overlay"
      contains: "createChart"
    - path: "dashboard/src/components/overview/trade-history.tsx"
      provides: "Trade history table using shadcn Table"
      contains: "TradeHistoryItem"
    - path: "dashboard/src/app/(dashboard)/page.tsx"
      provides: "Overview page assembling all 4 sections with useOverview hook"
      contains: "useOverview"
  key_links:
    - from: "dashboard/src/app/(dashboard)/page.tsx"
      to: "dashboard/src/hooks/use-overview.ts"
      via: "useOverview() hook call"
      pattern: "useOverview"
    - from: "dashboard/src/components/overview/kpi-cards.tsx"
      to: "dashboard/src/lib/formatters.ts"
      via: "formatCurrency, formatPercent imports"
      pattern: "formatCurrency|formatPercent"
    - from: "dashboard/src/components/overview/equity-curve.tsx"
      to: "lightweight-charts"
      via: "createChart + AreaSeries + HistogramSeries"
      pattern: "createChart"
    - from: "dashboard/src/components/overview/holdings-table.tsx"
      to: "dashboard/src/components/ui/table.tsx"
      via: "shadcn Table component imports"
      pattern: "Table.*TableHeader.*TableBody"
---

<objective>
Build the 4 Overview page sections (KPI cards, holdings table, equity curve chart, trade history table) and assemble them into the Overview page using the design system from Plan 01.

Purpose: This is the user-facing result of Phase 22 -- the Bloomberg-style Overview page where the user sees their portfolio at a glance.
Output: Working Overview page with real data from FastAPI, Bloomberg dark theme, monospace numbers, TradingView chart.
</objective>

<execution_context>
@/home/mqz/.claude/get-shit-done/workflows/execute-plan.md
@/home/mqz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-design-system-overview-page/22-RESEARCH.md
@.planning/phases/22-design-system-overview-page/22-01-SUMMARY.md

<interfaces>
<!-- Types and utilities created by Plan 01 that this plan consumes directly. -->

From dashboard/src/types/api.ts (created by Plan 01):
```typescript
export interface Position {
  symbol: string;
  qty: number;
  current_price: number;
  pnl_pct: number;
  pnl_dollar: number;
  stop_price: number;
  target_price: number;
  composite_score: number;
  market_value: number;
}

export interface TradeHistoryItem {
  symbol: string;
  direction: string;
  entry_price: number;
  stop_loss_price: number;
  take_profit_price: number;
  quantity: number;
  position_value: number;
  composite_score: number;
  signal_direction: string;
  status: string;
  created_at: string;
}

export interface EquityCurve {
  dates: string[];
  values: number[];
}

export interface RegimePeriod {
  start: string;
  end: string;
  regime: string;
}

export interface LastPipeline {
  run_id: string;
  status: string;
  started_at: string | null;
  finished_at: string | null;
}

export interface OverviewData {
  total_value: number;
  today_pnl: number;
  drawdown_pct: number;
  last_pipeline: LastPipeline | null;
  positions: Position[];
  trade_history: TradeHistoryItem[];
  equity_curve: EquityCurve;
  regime_periods: RegimePeriod[];
}
```

From dashboard/src/lib/formatters.ts (created by Plan 01):
```typescript
export function formatCurrency(value: number): string;
export function formatPercent(value: number): string;
export function formatScore(value: number): string;
export function formatDate(dateStr: string): string;
```

From dashboard/src/hooks/use-overview.ts (created by Plan 01):
```typescript
export function useOverview(): UseQueryResult<OverviewData>;
```

From dashboard/src/components/ui/ (vendored by Plan 01):
- card.tsx: Card, CardContent, CardHeader, CardTitle
- table.tsx: Table, TableBody, TableCell, TableHead, TableHeader, TableRow
- badge.tsx: Badge
- skeleton.tsx: Skeleton

Theme tokens available as Tailwind classes (from Plan 01 globals.css):
- bg-background, bg-card, bg-muted, bg-secondary
- text-foreground, text-muted-foreground, text-card-foreground
- text-profit (cyan), text-loss (red), text-neutral (white), text-interactive (amber)
- border-border
- font-mono (JetBrains Mono)
- tabular-nums (CSS class for aligned numbers)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Overview page components and assemble the page</name>
  <files>
    dashboard/src/components/overview/kpi-cards.tsx
    dashboard/src/components/overview/holdings-table.tsx
    dashboard/src/components/overview/equity-curve.tsx
    dashboard/src/components/overview/trade-history.tsx
    dashboard/src/app/(dashboard)/page.tsx
  </files>
  <action>
    Create 4 component files in dashboard/src/components/overview/ and replace the Overview page stub.

    1. **kpi-cards.tsx** ('use client'):
       - Import Card, CardContent, CardHeader, CardTitle from @/components/ui/card
       - Import formatCurrency, formatPercent from @/lib/formatters
       - Import OverviewData type from @/types/api
       - Props: { data: OverviewData }
       - Render 4 cards in a `grid grid-cols-4 gap-4` layout:
         - Card 1 "Total Value": data.total_value formatted as currency, text-2xl font-mono tabular-nums
         - Card 2 "Daily P&L": data.today_pnl formatted as currency with sign prefix (+/-), colored text-profit if >= 0 else text-loss
         - Card 3 "Drawdown": data.drawdown_pct formatted as percent, colored text-profit if <= 10, text-interactive if <= 15, text-loss if > 15
         - Card 4 "Pipeline": data.last_pipeline?.status ?? 'No runs', text-lg font-mono
       - Each CardHeader has pb-2, CardTitle has text-sm text-muted-foreground

    2. **holdings-table.tsx** ('use client'):
       - Import Table, TableBody, TableCell, TableHead, TableHeader, TableRow from @/components/ui/table
       - Import formatCurrency, formatScore from @/lib/formatters
       - Import Position type from @/types/api
       - Props: { positions: Position[] }
       - If positions empty, show "No open positions" in text-muted-foreground
       - Table columns: Ticker, Qty, Price, P&L, Stop, Target, Score
       - All numeric cells: text-right font-mono tabular-nums
       - P&L cell: text-profit if pnl_dollar >= 0 else text-loss, show formatCurrency(pos.pnl_dollar)
       - Ticker cell: font-medium
       - Score cell: formatScore(pos.composite_score)

    3. **equity-curve.tsx** ('use client'):
       - Import useRef, useLayoutEffect from react
       - Import createChart, ColorType, AreaSeries, HistogramSeries from lightweight-charts
       - Props: { data: { time: string; value: number }[], regimePeriods: { start: string; end: string; regime: string }[] }
       - REGIME_COLORS record: Bull='rgba(0,200,83,0.08)', Bear='rgba(255,82,82,0.08)', Accumulation='rgba(79,195,247,0.08)', Distribution='rgba(255,183,77,0.08)', Unknown='rgba(128,128,128,0.05)'
       - useLayoutEffect (NOT useEffect -- synchronous cleanup prevents memory leaks):
         - Guard: return if containerRef.current is null or data.length === 0
         - createChart with transparent background, textColor '#9ca3af', fontFamily 'JetBrains Mono, monospace', grid lines rgba(255,255,255,0.04), height 300
         - Set chartRef.current = { isRemoved: false } for cleanup guard
         - Add AreaSeries: lineColor '#4fc3f7', topColor 'rgba(79,195,247,0.25)', bottomColor 'rgba(79,195,247,0.0)', lineWidth 2
         - If regimePeriods.length > 0: add HistogramSeries on separate priceScaleId='', map each data point's time to a regime color, value=1. Set scaleMargins top:0 bottom:0.
         - chart.timeScale().fitContent()
         - ResizeObserver on containerRef.current to call chart.applyOptions({ width })
         - Cleanup: set isRemoved=true, disconnect ResizeObserver, call chart.remove()
       - Render: div with ref={containerRef} className="w-full"
       - If data is empty, show "No equity data" placeholder text

    4. **trade-history.tsx** ('use client'):
       - Import Table components from @/components/ui/table
       - Import formatCurrency, formatScore, formatDate from @/lib/formatters
       - Import TradeHistoryItem type from @/types/api
       - Props: { trades: TradeHistoryItem[] }
       - If trades empty, show "No executed trades" in text-muted-foreground
       - Table columns: Symbol, Direction, Entry, Stop, Target, Qty, Value, Score, Status, Date
       - All price/value cells: text-right font-mono tabular-nums
       - Direction cell: text-profit for BUY/LONG, text-loss for SELL/SHORT
       - Date cell: formatDate(trade.created_at)

    5. **Replace dashboard/src/app/(dashboard)/page.tsx**:
       - 'use client' directive
       - Import useOverview from @/hooks/use-overview
       - Import KpiCards, HoldingsTable, EquityCurve, TradeHistory from @/components/overview/
       - Import Skeleton from @/components/ui/skeleton
       - Call useOverview() to get { data, isLoading, error }
       - Loading state: render Skeleton placeholders (4 skeleton cards + skeleton rectangle for table area)
       - Error state: render error message in text-destructive with "Backend connection failed" text
       - Data state: render sections in order with gap-6:
         a. KpiCards with data
         b. Two-column grid (lg:grid-cols-3): left 2 cols = holdings table in a Card wrapper titled "Holdings", right 1 col = trade history in a Card wrapper titled "Recent Trades"
         c. Equity curve in a Card wrapper titled "Equity Curve" -- transform data.equity_curve into {time, value}[] format for the chart component (zip dates and values arrays)
       - Page title "Overview" as h1 with text-xl font-bold mb-4
  </action>
  <verify>
    <automated>cd /home/mqz/workspace/trading/dashboard && npx tsc --noEmit && npx biome check . && npm run build</automated>
  </verify>
  <done>
    - KPI cards render total value, daily P&L (color-coded), drawdown (3-tier color), pipeline status
    - Holdings table shows positions with monospace numbers and P&L semantic coloring
    - Equity curve chart renders with regime background coloring
    - Trade history table displays executed trades
    - Overview page assembles all sections with loading skeleton and error state
    - Full build passes (tsc + biome + next build)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of Bloomberg-style Overview page</name>
  <files>dashboard/src/app/(dashboard)/page.tsx</files>
  <action>
    User verifies the Overview page renders correctly with Bloomberg dark theme, all 4 sections visible, semantic colors applied, and monospace numbers aligned.
  </action>
  <verify>
    <automated>cd /home/mqz/workspace/trading/dashboard && npm run build</automated>
  </verify>
  <done>User confirms the Overview page looks correct with Bloomberg dark theme and all sections rendering</done>
  <what-built>Bloomberg-style Overview page with KPI cards, holdings table, TradingView equity curve chart with regime coloring, and trade history table</what-built>
  <how-to-verify>
    1. Start FastAPI backend: `cd /home/mqz/workspace/trading && python -m src.dashboard.presentation.app` (or however it starts)
    2. Start Next.js dev server: `cd /home/mqz/workspace/trading/dashboard && npm run dev`
    3. Open http://localhost:3000 in browser
    4. Verify:
       a. Page has deep black Bloomberg-style background (not gray, not white)
       b. Navigation bar shows Overview/Signals/Risk/Pipeline links with amber hover color
       c. 4 KPI cards display at top (Total Value, Daily P&L, Drawdown, Pipeline)
       d. Numbers use monospace font (all digits same width, columns align)
       e. P&L numbers show cyan for profit, red for loss
       f. Drawdown shows green/amber/red based on tier (0-10%/10-15%/15%+)
       g. Holdings table shows position data in aligned columns
       h. Equity curve chart renders (if equity data exists) with colored regime backgrounds
       i. Trade history table shows executed trades
       j. If FastAPI is not running, error message appears instead of crash
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `cd /home/mqz/workspace/trading/dashboard && npx tsc --noEmit` -- zero type errors
2. `cd /home/mqz/workspace/trading/dashboard && npx biome check .` -- zero lint/format errors
3. `cd /home/mqz/workspace/trading/dashboard && npm run build` -- production build succeeds
4. Manual: Overview page renders all 4 sections with Bloomberg dark theme
5. Manual: Numbers in monospace, P&L color-coded, drawdown tiered
6. Manual: Chart renders without console errors, no memory leak warnings
</verification>

<success_criteria>
- KPI cards show total value, daily P&L (cyan/red), drawdown (3-tier), pipeline status
- Holdings table shows positions with aligned monospace numbers and semantic P&L colors
- TradingView equity curve renders with regime background coloring (if data exists)
- Trade history table shows executed trades
- All data flows from FastAPI via the BFF proxy through useOverview hook
- Loading skeletons appear while data is fetching
- Error state displays when backend is unreachable
- Bloomberg dark theme applied consistently (black bg, amber accents, monospace numbers)
- Full build passes
</success_criteria>

<output>
After completion, create `.planning/phases/22-design-system-overview-page/22-02-SUMMARY.md`
</output>
