---
phase: 01-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data_ingest/__init__.py
  - src/data_ingest/domain/__init__.py
  - src/data_ingest/domain/value_objects.py
  - src/data_ingest/domain/events.py
  - src/data_ingest/infrastructure/__init__.py
  - src/data_ingest/infrastructure/duckdb_store.py
  - src/data_ingest/infrastructure/sqlite_store.py
  - src/data_ingest/DOMAIN.md
  - src/shared/infrastructure/__init__.py
  - src/shared/infrastructure/event_bus.py
  - tests/unit/test_data_ingest_vos.py
  - tests/unit/test_event_bus.py
  - tests/unit/test_duckdb_store.py
  - pyproject.toml
  - requirements.txt
autonomous: true
requirements:
  - DATA-03

must_haves:
  truths:
    - "Data ingest domain VOs (Ticker, OHLCV, FinancialStatement, FilingDate, DataQualityReport) are immutable and validate invariants"
    - "DuckDB store creates persistent analytical database with ohlcv and financials tables"
    - "SQLite store wraps existing core/data/cache.py for operational state"
    - "Async event bus publishes and subscribes DomainEvents correctly"
    - "New dependencies (duckdb, edgartools, aiohttp) are installed and importable"
  artifacts:
    - path: "src/data_ingest/domain/value_objects.py"
      provides: "Ticker, OHLCV, FinancialStatement, FilingDate, DataQualityReport VOs"
      min_lines: 80
    - path: "src/data_ingest/infrastructure/duckdb_store.py"
      provides: "DuckDB analytical storage with point-in-time schema"
      exports: ["DuckDBStore"]
    - path: "src/shared/infrastructure/event_bus.py"
      provides: "Async in-process event bus"
      exports: ["AsyncEventBus"]
  key_links:
    - from: "src/data_ingest/domain/value_objects.py"
      to: "src/shared/domain/base_value_object.py"
      via: "inheritance from ValueObject"
      pattern: "class.*ValueObject"
    - from: "src/data_ingest/domain/events.py"
      to: "src/shared/domain/domain_event.py"
      via: "inheritance from DomainEvent"
      pattern: "class.*DomainEvent"
    - from: "src/data_ingest/infrastructure/duckdb_store.py"
      to: "duckdb"
      via: "duckdb.connect persistent file"
      pattern: "duckdb\\.connect"
---

<objective>
Create the data_ingest bounded context foundation: domain value objects, domain events, DuckDB analytical store, SQLite operational store wrapper, and async event bus. Install new dependencies.

Purpose: Establish the type contracts and storage infrastructure that all subsequent data ingestion and scoring plans depend on. Without these VOs and stores, no data can be ingested or queried.

Output: Importable domain VOs, working DuckDB store with point-in-time schema, SQLite wrapper, async event bus, all with passing tests.
</objective>

<execution_context>
@C:/Users/USER/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/USER/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01/CONTEXT.md
@.planning/phases/01/01-RESEARCH.md

<interfaces>
<!-- Existing shared domain classes the executor MUST use -->

From src/shared/domain/__init__.py:
```python
from .base_entity import Entity
from .base_value_object import ValueObject
from .domain_event import DomainEvent
from .result import Ok, Err, Result

__all__ = ["Entity", "ValueObject", "DomainEvent", "Ok", "Err", "Result"]
```

From src/shared/domain/base_value_object.py:
```python
@dataclass(frozen=True)
class ValueObject(ABC):
    def __post_init__(self) -> None:
        self._validate()

    @abstractmethod
    def _validate(self) -> None:
        """Invariant validation. Raise ValueError on violation."""
        ...
```

From src/shared/domain/domain_event.py:
```python
@dataclass(frozen=True)
class DomainEvent(ABC):
    occurred_on: datetime = field(
        default_factory=lambda: datetime.now(timezone.utc),
        compare=False,
    )

    @property
    def event_type(self) -> str:
        return f"{self.__class__.__module__}.{self.__class__.__name__}"
```

From core/data/cache.py:
```python
def get(key: str) -> Any | None: ...
def set(key: str, value: Any, ttl: int) -> None: ...
def delete(key: str) -> None: ...
def purge_expired() -> int: ...
PRICE_TTL = 86_400       # 24 hours
FUNDAMENTALS_TTL = 604_800  # 7 days
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install dependencies + create domain VOs and events</name>
  <files>
    pyproject.toml,
    src/data_ingest/__init__.py,
    src/data_ingest/domain/__init__.py,
    src/data_ingest/domain/value_objects.py,
    src/data_ingest/domain/events.py,
    src/data_ingest/infrastructure/__init__.py,
    src/data_ingest/DOMAIN.md,
    tests/unit/test_data_ingest_vos.py
  </files>
  <behavior>
    - Ticker("AAPL") creates valid, Ticker("") raises ValueError, Ticker("aapl") raises ValueError
    - OHLCV with valid date/open/high/low/close/volume creates successfully
    - OHLCV with high < low raises ValueError
    - FinancialStatement with filing_date < period_end raises ValueError
    - FinancialStatement stores both period_end and filing_date as date objects
    - FilingDate wraps a date and validates not-future
    - DataQualityReport with passed=True has empty failures list
    - DataIngestedEvent and QualityCheckFailedEvent inherit DomainEvent
  </behavior>
  <action>
    1. Add new dependencies to pyproject.toml [project.dependencies]:
       - duckdb>=1.5.0
       - edgartools>=5.23.0
       - aiohttp>=3.9
       Also add pytest-asyncio>=0.21 to [project.optional-dependencies].dev if not present (it is present already, verify).
       Run `pip install -e ".[dev]"` to install.

    2. Create src/data_ingest/ package structure:
       - __init__.py (empty)
       - domain/__init__.py (public API exports)
       - domain/value_objects.py with these frozen dataclass VOs extending ValueObject:
         - Ticker(ticker: str) -- uppercase, 1-10 chars, no special chars
         - OHLCV(ticker: str, date: date, open: float, high: float, low: float, close: float, volume: int) -- validate high >= low, close > 0, volume >= 0
         - FinancialStatement(ticker: str, period_end: date, filing_date: date, form_type: str, revenue: float, net_income: float, total_assets: float, total_liabilities: float, working_capital: float, retained_earnings: float, ebit: float, operating_cashflow: float, free_cashflow: float, current_ratio: float, debt_to_equity: float, roa: float, roe: float) -- validate filing_date >= period_end, form_type in ("10-Q", "10-K")
         - FilingDate(value: date) -- validate not in future (allow 1 day tolerance for timezone)
         - DataQualityReport(ticker: str, passed: bool, missing_pct: float, stale_days: int, outlier_count: int, failures: tuple[str, ...]) -- use tuple not list (frozen dataclass), validate missing_pct 0-100

       - domain/events.py with DomainEvent subclasses:
         - DataIngestedEvent(ticker: str, ohlcv_rows: int, financial_quarters: int)
         - QualityCheckFailedEvent(ticker: str, failures: tuple[str, ...])

    3. Create src/data_ingest/DOMAIN.md following DDD rules with responsibilities, entities, use cases, invariants.

    4. Write tests FIRST in tests/unit/test_data_ingest_vos.py covering all behaviors listed above, then implement VOs to pass.
  </action>
  <verify>
    <automated>cd C:/workspace/trading && python -m pytest tests/unit/test_data_ingest_vos.py -x -v</automated>
  </verify>
  <done>All domain VOs create successfully with valid data, raise ValueError with invalid data, DataIngestedEvent and QualityCheckFailedEvent instantiate correctly. pip install of new deps succeeds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DuckDB store + SQLite wrapper + async event bus</name>
  <files>
    src/data_ingest/infrastructure/duckdb_store.py,
    src/data_ingest/infrastructure/sqlite_store.py,
    src/shared/infrastructure/__init__.py,
    src/shared/infrastructure/event_bus.py,
    tests/unit/test_duckdb_store.py,
    tests/unit/test_event_bus.py
  </files>
  <behavior>
    - DuckDBStore.connect(":memory:") creates ohlcv + financials tables
    - DuckDBStore.store_ohlcv(ticker, df) inserts rows, DuckDBStore.get_ohlcv(ticker) returns them
    - DuckDBStore.store_financials(ticker, records) inserts with filing_date, get_latest_financials(ticker, as_of_date) returns only records with filing_date <= as_of_date (point-in-time correctness)
    - DuckDBStore uses batch insert (single transaction) -- no row-by-row
    - SQLiteStore wraps core/data/cache.py get/set/delete with domain-typed interface
    - AsyncEventBus.subscribe(EventType, handler) registers handler
    - AsyncEventBus.publish(event) calls all registered handlers for that event type
    - AsyncEventBus handles both sync and async handlers
    - Publishing event with no subscribers does not raise
  </behavior>
  <action>
    1. Create src/data_ingest/infrastructure/duckdb_store.py:
       - class DuckDBStore with connect(db_path), close(), store_ohlcv(ticker, df), get_ohlcv(ticker, start_date=None, end_date=None), store_financials(ticker, records: list[dict]), get_latest_financials(ticker, as_of_date) methods
       - Schema: ohlcv table (ticker VARCHAR, date DATE, open DOUBLE, high DOUBLE, low DOUBLE, close DOUBLE, volume BIGINT, PRIMARY KEY(ticker, date))
       - Schema: financials table (ticker VARCHAR, period_end DATE, filing_date DATE, form_type VARCHAR, revenue DOUBLE, net_income DOUBLE, total_assets DOUBLE, total_liabilities DOUBLE, working_capital DOUBLE, retained_earnings DOUBLE, ebit DOUBLE, operating_cashflow DOUBLE, free_cashflow DOUBLE, current_ratio DOUBLE, debt_to_equity DOUBLE, roa DOUBLE, roe DOUBLE, PRIMARY KEY(ticker, period_end, form_type))
       - Use INSERT OR REPLACE for upsert
       - get_latest_financials MUST filter by filing_date <= as_of_date (point-in-time correctness per user decision)
       - Default db_path: "data/analytics.duckdb"
       - Use ":memory:" in tests

    2. Create src/data_ingest/infrastructure/sqlite_store.py:
       - class SQLiteStore that wraps core/data/cache.py
       - Methods: get_cached(key), set_cached(key, value, ttl), delete_cached(key), log_ingestion(ticker, status, details)
       - Add ingestion_log table (id INTEGER PRIMARY KEY AUTOINCREMENT, ticker TEXT, status TEXT, details TEXT, created_at REAL)
       - This class is a thin adapter -- delegates to core.data.cache for cache ops

    3. Create src/shared/infrastructure/event_bus.py:
       - class AsyncEventBus following the pattern from RESEARCH.md
       - subscribe(event_type: type[DomainEvent], handler: Callable)
       - publish(event: DomainEvent) -- handles both sync and async handlers
       - Use collections.defaultdict for handler registry
       - Create src/shared/infrastructure/__init__.py exporting AsyncEventBus

    4. Write tests FIRST, then implement:
       - tests/unit/test_duckdb_store.py: use ":memory:" DuckDB, test CRUD + point-in-time query
       - tests/unit/test_event_bus.py: test subscribe, publish, sync/async handlers, no-subscriber case
  </action>
  <verify>
    <automated>cd C:/workspace/trading && python -m pytest tests/unit/test_duckdb_store.py tests/unit/test_event_bus.py -x -v</automated>
  </verify>
  <done>DuckDB creates tables, stores/retrieves OHLCV and financials with point-in-time filtering. SQLite wrapper delegates to core cache. AsyncEventBus publishes events to registered handlers. All tests pass.</done>
</task>

</tasks>

<verification>
cd C:/workspace/trading && python -m pytest tests/unit/test_data_ingest_vos.py tests/unit/test_duckdb_store.py tests/unit/test_event_bus.py -v
cd C:/workspace/trading && python -c "import duckdb; import edgar; import aiohttp; print('Dependencies OK')"
cd C:/workspace/trading && ruff check src/data_ingest/ src/shared/infrastructure/
cd C:/workspace/trading && mypy src/data_ingest/ src/shared/infrastructure/ --ignore-missing-imports
</verification>

<success_criteria>
- All 3 test files pass (VOs, DuckDB store, event bus)
- New dependencies importable (duckdb, edgar, aiohttp)
- DuckDB point-in-time query returns only records with filing_date <= as_of_date
- Domain VOs enforce invariants (frozen, validated)
- Event bus handles async publish/subscribe
- ruff + mypy pass with 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/01/01-01-SUMMARY.md`
</output>
