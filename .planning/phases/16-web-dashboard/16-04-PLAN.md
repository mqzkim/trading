---
phase: 16-web-dashboard
plan: 04
type: execute
wave: 3
depends_on: ["16-01", "16-02", "16-03"]
files_modified:
  - src/dashboard/application/queries.py
  - src/dashboard/presentation/routes.py
  - src/dashboard/presentation/templates/pipeline.html
  - src/dashboard/presentation/templates/partials/pipeline_status.html
  - src/dashboard/infrastructure/sse_bridge.py
  - tests/unit/test_dashboard_web.py
  - tests/unit/test_dashboard_sse.py
autonomous: true
requirements:
  - DASH-05
  - DASH-06
  - DASH-07

must_haves:
  truths:
    - "Pipeline page shows run history with stage results and next scheduled time"
    - "Pipeline page shows current strategy approval status with create form and suspend/resume buttons"
    - "Pipeline page shows daily budget progress bar with spent/limit amounts"
    - "Pipeline page shows manual review queue with approve/reject buttons"
    - "SSE endpoint streams named events (OrderFilled, PipelineStatusChanged, DrawdownTierChanged, RegimeChanged)"
    - "HTMX sse-swap updates page sections when events arrive without page refresh"
  artifacts:
    - path: "src/dashboard/application/queries.py"
      provides: "PipelineQueryHandler aggregating pipeline, approval, budget, review data"
    - path: "src/dashboard/presentation/templates/pipeline.html"
      provides: "Full pipeline page with runs, approval, budget, review queue"
    - path: "tests/unit/test_dashboard_sse.py"
      provides: "SSE bridge and endpoint tests"
  key_links:
    - from: "src/dashboard/presentation/routes.py"
      to: "src/dashboard/application/queries.py"
      via: "pipeline route calls PipelineQueryHandler"
      pattern: "pipeline_handler"
    - from: "routes.py POST endpoints"
      to: "src/approval/application/handlers.py"
      via: "approval create/suspend/resume actions delegate to ApprovalHandler"
      pattern: "approval_handler"
    - from: "sse_bridge.py"
      to: "routes.py SSE endpoint"
      via: "bridge.stream() yields events that SSE endpoint sends as ServerSentEvent"
      pattern: "sse_bridge\\.stream"
---

<objective>
Implement the Pipeline & Approval page (pipeline runs, strategy approval management, daily budget, manual review queue) and wire the SSE real-time update system end-to-end.

Purpose: DASH-05 (pipeline status), DASH-06 (approval management), DASH-07 (real-time SSE) complete the operational visibility loop. This is the final plan -- after it, the dashboard is fully functional.
Output: Pipeline page with all management capabilities + live SSE updates across all pages.
</objective>

<execution_context>
@/home/mqz/.claude/get-shit-done/workflows/execute-plan.md
@/home/mqz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-web-dashboard/16-CONTEXT.md
@.planning/phases/16-web-dashboard/16-RESEARCH.md
@.planning/phases/16-web-dashboard/16-01-SUMMARY.md
@.planning/phases/16-web-dashboard/16-02-SUMMARY.md
@.planning/phases/16-web-dashboard/16-03-SUMMARY.md
@src/bootstrap.py
@src/approval/application/handlers.py
@src/approval/infrastructure/sqlite_approval_repo.py
@src/approval/infrastructure/sqlite_budget_repo.py
@src/approval/infrastructure/sqlite_review_queue_repo.py
@src/pipeline/infrastructure/sqlite_run_repo.py
@src/pipeline/infrastructure/scheduler.py

<interfaces>
From src/approval/application/handlers.py:
```python
class ApprovalHandler:
    def create_approval(self, command) -> dict: ...
    def get_status(self) -> dict: ...
    def revoke(self, approval_id: str) -> None: ...
    def resume(self, approval_id: str) -> None: ...
    def suspend_if_regime_invalid(self, regime: str) -> None: ...
```

From src/approval/infrastructure/sqlite_review_queue_repo.py:
```python
class SqliteReviewQueueRepository:
    def find_pending(self) -> list[dict]: ...
    def approve(self, review_id: str) -> None: ...
    def reject(self, review_id: str) -> None: ...
```

From src/approval/infrastructure/sqlite_budget_repo.py:
```python
class SqliteBudgetRepository:
    def get_today(self) -> Optional[dict]: ...  # Check actual interface
```

From src/pipeline/infrastructure/sqlite_run_repo.py:
```python
class SqlitePipelineRunRepository:
    def find_latest(self) -> Optional[PipelineRun]: ...
    def find_recent(self, limit: int) -> list[PipelineRun]: ...
```

From src/pipeline/infrastructure/scheduler.py:
```python
class SchedulerService:
    def next_run_time(self) -> Optional[datetime]: ...
```

From src/dashboard/infrastructure/sse_bridge.py (created in 16-01):
```python
class SSEBridge:
    def subscribe_events(self, *event_types) -> None: ...
    async def stream(self) -> AsyncIterator[dict]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pipeline query handler + approval action routes + SSE partial rendering</name>
  <files>
    src/dashboard/application/queries.py
    src/dashboard/presentation/routes.py
    src/dashboard/infrastructure/sse_bridge.py
    tests/unit/test_dashboard_sse.py
  </files>
  <action>
1. Add `PipelineQueryHandler` to queries.py:
   - `__init__(self, ctx: dict)`
   - `handle(self) -> dict` returning:
     - `pipeline_runs`: list of recent pipeline runs from pipeline_run_repo.find_recent(10). Each with run_id, started_at, completed_at, status, stage_results (list of stage name + status + symbol_count)
     - `next_scheduled`: from scheduler_service.next_run_time() formatted as string, or "Not scheduled"
     - `approval_status`: from approval_handler.get_status() (current active approval or None)
     - `daily_budget`: from budget_repo.get_today() -- spent, limit, remaining. If None, return default zeroes.
     - `review_queue`: from review_queue_repo.find_pending() -- list of pending trade reviews

2. Update routes.py -- add HTMX action endpoints (POST routes for approval management):
   - POST `/approval/create`: read form data (score_threshold, allowed_regimes, max_per_trade_pct, daily_budget_cap, expires_in_days), call approval_handler.create_approval(), return rendered approval-status partial HTML
   - POST `/approval/suspend`: call approval_handler.revoke(approval_id from form), return updated approval-status partial
   - POST `/approval/resume`: call approval_handler.resume(approval_id from form), return updated approval-status partial
   - POST `/review/approve`: call review_queue_repo.approve(review_id), return updated review-queue partial
   - POST `/review/reject`: call review_queue_repo.reject(review_id), return updated review-queue partial
   - All POST routes return HTML fragments (partials) for HTMX hx-swap, not full pages

3. Update SSE endpoint in routes.py to render HTML partials per event type:
   - Create a helper `_render_partial(event_type: str, payload: dict, request: Request) -> str` that:
     - For "OrderFilled": re-render holdings_table.html partial with fresh data from OverviewQueryHandler
     - For "PipelineCompletedEvent"/"PipelineHaltedEvent": re-render pipeline_status.html partial
     - For "DrawdownAlertEvent": re-render drawdown_gauge.html partial with new gauge JSON
     - For "RegimeChangedEvent": re-render regime_badge.html partial with new regime
   - The SSE endpoint yields ServerSentEvent with event=event_type_name and data=rendered HTML

4. Create `tests/unit/test_dashboard_sse.py`:
   - `test_sse_bridge_subscribe`: create SSEBridge with SyncEventBus, subscribe to an event type, publish event, verify queue receives it
   - `test_sse_bridge_multiple_consumers`: create 2 stream iterators, publish event, both receive it
   - `test_sse_bridge_queue_full_drops`: fill queue to maxsize, publish event, no exception raised
   - `test_sse_endpoint_content_type`: GET /dashboard/events returns content-type text/event-stream
   - Use asyncio.run or pytest-asyncio for async bridge tests
  </action>
  <verify>
    <automated>cd /home/mqz/workspace/trading && python -m pytest tests/unit/test_dashboard_sse.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <done>PipelineQueryHandler aggregates pipeline/approval/budget/review data. Approval HTMX action routes return HTML partials. SSE endpoint renders event-specific partials. SSE tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Pipeline & Approval page template + full integration test</name>
  <files>
    src/dashboard/presentation/templates/pipeline.html
    src/dashboard/presentation/templates/partials/pipeline_status.html
    tests/unit/test_dashboard_web.py
  </files>
  <action>
1. Update `partials/pipeline_status.html`:
   - Shows "Last run: {time} - {status}" and "Next: {next_scheduled}"
   - Status colored: green for SUCCESS/COMPLETED, red for FAILED/HALTED, yellow for RUNNING
   - `sse-swap="PipelineStatusChanged"` attribute on wrapper

2. Create full `pipeline.html`:
   - Extends base.html, sets active_page = "pipeline"
   - Section 1: "Pipeline Status" with pipeline_status partial included
   - Section 2: "Run History" table with columns: Run ID (truncated), Started, Completed, Status, Stages
     - Stages column: compact view of stage name + icon (check/x/spinner) per stage result
     - For loop over pipeline_runs
     - Empty state: "No pipeline runs yet"

   - Section 3: "Strategy Approval" (id="approval-status")
     - If approval_status exists and is active:
       - Show approval details: score threshold, allowed regimes, max per-trade %, expiration date
       - If status == "active": show "Suspend" button with `hx-post="/dashboard/approval/suspend"` `hx-target="#approval-status"` `hx-swap="innerHTML"`
       - If status == "suspended": show "Resume" button similarly + suspended_reasons text
     - If no active approval: show "Create Approval" form (per CONTEXT.md):
       - Inputs: score_threshold (number step=0.1), allowed_regimes (text, comma-separated), max_per_trade_pct (number step=0.1), daily_budget_cap (number), expires_in_days (number default=30)
       - Submit button with `hx-post="/dashboard/approval/create"` `hx-target="#approval-status"` `hx-swap="innerHTML"`

   - Section 4: "Daily Budget" (id="budget-section")
     - Progress bar: Tailwind h-4 bg-gray-200 rounded with inner div bg-blue-600 width=(spent/limit*100)%
     - Text: "$X,XXX / $Y,YYY spent" with remaining amount
     - If no budget: "No budget set"

   - Section 5: "Manual Review Queue" (id="review-queue")
     - Table: Symbol, Strategy, Score, Reason (why queued), Actions
     - Each row has Approve and Reject buttons:
       - Approve: `hx-post="/dashboard/review/approve"` with hidden input review_id, `hx-target="#review-queue"` `hx-swap="innerHTML"`
       - Reject: similarly for reject
     - Empty state: "No trades pending review"

3. Update `tests/unit/test_dashboard_web.py`:
   - Add mock data for pipeline runs, approval status, budget, review queue to ctx
   - `test_pipeline_page_renders_runs`: GET /dashboard/pipeline contains "Run History"
   - `test_pipeline_shows_approval_form`: with no active approval, contains "Create Approval" form
   - `test_pipeline_shows_active_approval`: with active approval mock, contains "Suspend" button
   - `test_pipeline_shows_budget_bar`: contains budget progress section
   - `test_pipeline_shows_review_queue`: with pending reviews, contains "Approve" button
   - `test_approval_create_post`: POST /dashboard/approval/create with form data returns 200 with HTML fragment
   - `test_review_approve_post`: POST /dashboard/review/approve returns 200

4. Run full test suite to verify nothing is broken:
   - `pytest tests/unit/test_dashboard_web.py tests/unit/test_dashboard_sse.py tests/unit/test_dashboard_charts.py -x`
  </action>
  <verify>
    <automated>cd /home/mqz/workspace/trading && python -m pytest tests/unit/test_dashboard_web.py tests/unit/test_dashboard_sse.py tests/unit/test_dashboard_charts.py -x -v 2>&1 | tail -40</automated>
  </verify>
  <done>Pipeline page shows run history, approval management (create/suspend/resume), daily budget bar, and manual review queue with approve/reject buttons. All HTMX actions return HTML partials. Full dashboard test suite passes.</done>
</task>

</tasks>

<verification>
- `pytest tests/unit/test_dashboard_web.py tests/unit/test_dashboard_sse.py tests/unit/test_dashboard_charts.py -x` all pass
- Pipeline page shows run history table
- Approval create form submits via HTMX and updates approval section
- Budget progress bar renders correctly
- Review queue approve/reject buttons work via HTMX POST
- SSE bridge publishes events to connected consumers
- Full dashboard is operational with all 4 pages
</verification>

<success_criteria>
- DASH-05: Pipeline status with last run, next scheduled, stage results visible
- DASH-06: Strategy approval and daily budget manageable from dashboard (create, suspend, resume, view)
- DASH-07: SSE endpoint streams 4 event types, HTMX sse-swap updates page sections in real-time
- All 9 DASH requirements fulfilled across all 4 plans
</success_criteria>

<output>
After completion, create `.planning/phases/16-web-dashboard/16-04-SUMMARY.md`
</output>
