---
phase: 16-web-dashboard
plan: 03
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - src/dashboard/application/queries.py
  - src/dashboard/presentation/routes.py
  - src/dashboard/presentation/templates/signals.html
  - src/dashboard/presentation/templates/risk.html
  - src/dashboard/presentation/templates/partials/drawdown_gauge.html
  - src/dashboard/presentation/templates/partials/regime_badge.html
  - tests/unit/test_dashboard_web.py
autonomous: true
requirements:
  - DASH-02
  - DASH-04

must_haves:
  truths:
    - "Signals page shows sortable scoring table with composite, F/Z/M/G, technical, signal columns"
    - "Signals page uses heatmap background color intensity based on composite score"
    - "Risk page shows semi-circular drawdown gauge with 10/15/20% markers"
    - "Risk page shows sector exposure donut chart with 25% limit line"
    - "Risk page shows regime status badge"
  artifacts:
    - path: "src/dashboard/application/queries.py"
      provides: "SignalsQueryHandler and RiskQueryHandler"
    - path: "src/dashboard/presentation/templates/signals.html"
      provides: "Scoring table with heatmap styling + signal recommendations"
    - path: "src/dashboard/presentation/templates/risk.html"
      provides: "Drawdown gauge + sector donut + position limits + regime badge"
  key_links:
    - from: "src/dashboard/presentation/routes.py"
      to: "src/dashboard/application/queries.py"
      via: "signals/risk routes call respective query handlers"
      pattern: "signals_handler|risk_handler"
    - from: "risk.html"
      to: "charts.py"
      via: "gauge and donut chart JSON passed to template"
      pattern: "Plotly\\.newPlot"
---

<objective>
Implement the Signals page (scoring table + signal recommendations) and Risk page (drawdown gauge, sector exposure, position limits, regime badge).

Purpose: DASH-02 (scoring/signals) and DASH-04 (risk metrics) give the operator the analytical view -- what the system recommends and what the current risk posture looks like.
Output: Two fully populated pages reading from scoring, signal, portfolio, and regime repositories.
</objective>

<execution_context>
@/home/mqz/.claude/get-shit-done/workflows/execute-plan.md
@/home/mqz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-web-dashboard/16-CONTEXT.md
@.planning/phases/16-web-dashboard/16-RESEARCH.md
@.planning/phases/16-web-dashboard/16-01-SUMMARY.md
@src/bootstrap.py
@src/scoring/infrastructure/sqlite_score_repo.py
@src/signals/infrastructure/sqlite_signal_repo.py
@src/regime/infrastructure/sqlite_regime_repo.py
@src/portfolio/domain/aggregates.py

<interfaces>
From src/scoring/infrastructure/sqlite_score_repo.py:
```python
class SqliteScoreRepository:
    def find_latest(self, symbol: str) -> Optional[dict]: ...
    def find_all_latest(self) -> list[dict]: ...
```

From src/signals/infrastructure/sqlite_signal_repo.py:
```python
class SqliteSignalRepository:
    def find_latest(self, symbol: str) -> Optional[dict]: ...
    def find_all_latest(self) -> list[dict]: ...
```

From src/regime/infrastructure/sqlite_regime_repo.py:
```python
class SqliteRegimeRepository:
    def find_latest(self) -> Optional[dict]: ...
```

From src/portfolio/domain/aggregates.py:
```python
class Portfolio:
    @property
    def drawdown(self) -> float: ...
    @property
    def drawdown_level(self) -> DrawdownLevel: ...
    def sector_weights(self) -> dict[str, float]: ...  # Check actual method name
```

From src/dashboard/presentation/charts.py (created in 16-01):
```python
def build_drawdown_gauge(current_drawdown_pct: float) -> dict: ...
def build_sector_donut(sectors: dict[str, float]) -> dict: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Signals + Risk query handlers and route wiring</name>
  <files>
    src/dashboard/application/queries.py
    src/dashboard/presentation/routes.py
  </files>
  <action>
1. Add `SignalsQueryHandler` to queries.py:
   - `__init__(self, ctx: dict)`
   - `handle(self) -> dict` returning:
     - `scores`: list from ctx score_repo (via score_handler or direct repo access from bootstrap ctx). Each entry has symbol, composite_score, fundamental_score (F), valuation_score (Z), momentum_score (M), growth_score (G), technical_score, and any other available sub-scores. Check actual score dict keys in the sqlite repo.
     - `signals`: list from ctx signal_repo. Each entry has symbol, signal_type, confidence, strategy, recommendation.
     - Sort scores by composite_score descending.

2. Add `RiskQueryHandler` to queries.py:
   - `__init__(self, ctx: dict)`
   - `handle(self) -> dict` returning:
     - `drawdown_pct`: portfolio drawdown as float (0.0 to 1.0)
     - `drawdown_level`: DrawdownLevel enum value string
     - `sector_weights`: dict of sector->weight from positions. Calculate from open positions grouped by sector. If positions don't have sector info, derive from a simple sector mapping or return empty dict.
     - `position_count`: number of open positions
     - `max_positions`: configurable limit (e.g., 20 or from settings)
     - `regime`: latest regime from regime_repo (e.g., "Bull", "Bear", "Sideways", "Crisis") or "Unknown" if no data
     - `gauge_json`: pre-built Plotly gauge JSON from build_drawdown_gauge()
     - `donut_json`: pre-built Plotly donut JSON from build_sector_donut()

3. Update routes.py:
   - Import SignalsQueryHandler, RiskQueryHandler
   - GET `/signals`: create handler, call handle(), pass scores and signals to signals.html template
   - GET `/risk`: create handler, call handle(), pass all risk data including gauge_json and donut_json to risk.html template
  </action>
  <verify>
    <automated>cd /home/mqz/workspace/trading && python -c "
from unittest.mock import MagicMock
from src.dashboard.application.queries import SignalsQueryHandler, RiskQueryHandler

# Verify SignalsQueryHandler returns expected shape
mock_ctx = {
    'score_repo': MagicMock(find_all_latest=MagicMock(return_value=[])),
    'signal_repo': MagicMock(find_all_latest=MagicMock(return_value=[])),
    'position_repo': MagicMock(find_all_open=MagicMock(return_value=[])),
    'regime_repo': MagicMock(find_latest=MagicMock(return_value=None)),
}
sig_result = SignalsQueryHandler(mock_ctx).handle()
assert 'scores' in sig_result and 'signals' in sig_result, f'SignalsQueryHandler missing keys: {sig_result.keys()}'
risk_result = RiskQueryHandler(mock_ctx).handle()
assert 'drawdown_pct' in risk_result and 'regime' in risk_result and 'gauge_json' in risk_result, f'RiskQueryHandler missing keys: {risk_result.keys()}'
print('handlers OK -- return shapes verified')
"</automated>
  </verify>
  <done>SignalsQueryHandler returns {scores, signals} and RiskQueryHandler returns {drawdown_pct, drawdown_level, sector_weights, position_count, max_positions, regime, gauge_json, donut_json}. Routes wired to pass data to templates.</done>
</task>

<task type="auto">
  <name>Task 2: Signals + Risk page templates and tests</name>
  <files>
    src/dashboard/presentation/templates/signals.html
    src/dashboard/presentation/templates/risk.html
    src/dashboard/presentation/templates/partials/drawdown_gauge.html
    src/dashboard/presentation/templates/partials/regime_badge.html
    tests/unit/test_dashboard_web.py
  </files>
  <action>
1. Update `signals.html`:
   - Extends base.html, sets active_page = "signals"
   - Section: "Scoring Results" heading
   - Table with headers: Symbol, Composite, F, Z, M, G, Technical, Signal
   - For loop over `scores`: render each row
   - Composite score cell background: use inline Tailwind bg-opacity based on score value. E.g., scores > 0.8 get bg-green-500/30, 0.6-0.8 bg-green-500/20, 0.4-0.6 bg-yellow-500/20, < 0.4 bg-red-500/20. Implement via Jinja2 conditionals.
   - Signal column: show recommendation from matching signal entry if exists
   - Section: "Signal Recommendations" heading
   - Cards or list of signal entries: symbol, strategy consensus, confidence level
   - Empty states: "No scoring data" / "No signals generated"
   - Table is sortable via simple HTMX: `hx-get="/dashboard/signals?sort=composite"` on column headers (sort param handled in route, default composite desc). Use hx-target on the table body, hx-swap="innerHTML". Simple server-side sort.

2. Update `partials/drawdown_gauge.html`:
   - `<div id="drawdown-gauge"></div>` container
   - Plotly.newPlot script using gauge_json
   - Add `sse-swap="DrawdownTierChanged"` on a wrapper div

3. Update `partials/regime_badge.html`:
   - Badge showing regime name with color: Bull=green, Bear=red, Sideways=yellow, Crisis=gray
   - Tailwind badge classes (rounded-full, px-3, py-1, text-sm, font-semibold)
   - `sse-swap="RegimeChanged"` on wrapper div

4. Update `risk.html`:
   - Extends base.html, sets active_page = "risk"
   - Two-column grid layout (lg:grid-cols-2)
   - Left column: Drawdown Gauge (include partial) + Position Limits utilization bar
   - Right column: Sector Exposure donut chart (Plotly.newPlot with donut_json) + Regime badge (include partial)
   - Position limits: progress bar showing `{{ position_count }} / {{ max_positions }}` with Tailwind bg-blue-600 width percentage
   - Drawdown level text indicator below gauge
   - Empty state handling for all sections

5. Update `tests/unit/test_dashboard_web.py`:
   - Add mock data for scores and signals to ctx fixture
   - `test_signals_page_renders_table`: GET /dashboard/signals contains "Scoring Results" and table headers
   - `test_signals_empty_state`: with empty scores, contains "No scoring data"
   - `test_risk_page_renders_gauge`: GET /dashboard/risk contains "drawdown-gauge" div
   - `test_risk_page_shows_regime`: GET /dashboard/risk contains regime badge text
   - `test_risk_page_sector_chart`: GET /dashboard/risk contains "sector-donut" div
  </action>
  <verify>
    <automated>cd /home/mqz/workspace/trading && python -m pytest tests/unit/test_dashboard_web.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <done>Signals page shows heatmap-styled scoring table with sortable columns and signal recommendations. Risk page shows drawdown gauge, sector donut, position limits, and regime badge. All tests pass.</done>
</task>

</tasks>

<verification>
- `pytest tests/unit/test_dashboard_web.py -x` passes all tests including new signals/risk tests
- Signals page renders scoring table with heatmap background colors
- Risk page renders drawdown gauge with proper threshold markers
- Regime badge displays current regime with correct color
</verification>

<success_criteria>
- DASH-02: Scoring and signal results displayed with heatmap styling
- DASH-04: Risk metrics including drawdown gauge (0-20% with markers), sector donut, position limits, regime badge all visible
- SSE swap attributes on gauge and regime badge for live updates
</success_criteria>

<output>
After completion, create `.planning/phases/16-web-dashboard/16-03-SUMMARY.md`
</output>
