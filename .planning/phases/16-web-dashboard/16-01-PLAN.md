---
phase: 16-web-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/settings.py
  - src/dashboard/__init__.py
  - src/dashboard/DOMAIN.md
  - src/dashboard/application/__init__.py
  - src/dashboard/application/queries.py
  - src/dashboard/infrastructure/__init__.py
  - src/dashboard/infrastructure/sse_bridge.py
  - src/dashboard/presentation/__init__.py
  - src/dashboard/presentation/routes.py
  - src/dashboard/presentation/app.py
  - src/dashboard/presentation/charts.py
  - src/dashboard/presentation/templates/base.html
  - src/dashboard/presentation/templates/overview.html
  - src/dashboard/presentation/templates/signals.html
  - src/dashboard/presentation/templates/risk.html
  - src/dashboard/presentation/templates/pipeline.html
  - src/dashboard/presentation/templates/partials/holdings_table.html
  - src/dashboard/presentation/templates/partials/kpi_cards.html
  - src/dashboard/presentation/templates/partials/pipeline_status.html
  - src/dashboard/presentation/templates/partials/drawdown_gauge.html
  - src/dashboard/presentation/templates/partials/regime_badge.html
  - tests/unit/test_dashboard_web.py
autonomous: true
requirements:
  - DASH-09

must_haves:
  truths:
    - "FastAPI dashboard app starts and serves /dashboard/ with base layout"
    - "Paper/live mode banner renders correctly based on EXECUTION_MODE setting"
    - "Sidebar navigation has 4 pages: Overview, Signals, Risk, Pipeline"
    - "SSE bridge infrastructure subscribes to SyncEventBus and streams to async consumers"
  artifacts:
    - path: "src/dashboard/presentation/app.py"
      provides: "FastAPI app factory with bootstrap ctx on app.state"
      exports: ["create_dashboard_app"]
    - path: "src/dashboard/presentation/routes.py"
      provides: "Dashboard router with page routes and SSE endpoint"
      exports: ["router"]
    - path: "src/dashboard/infrastructure/sse_bridge.py"
      provides: "SSEBridge class bridging SyncEventBus to async SSE"
      exports: ["SSEBridge"]
    - path: "src/dashboard/presentation/charts.py"
      provides: "Plotly chart builders for equity curve, gauge, donut"
      exports: ["build_equity_curve", "build_drawdown_gauge", "build_sector_donut"]
    - path: "src/dashboard/presentation/templates/base.html"
      provides: "Base template with CDN scripts, sidebar, mode banner, SSE connect"
    - path: "tests/unit/test_dashboard_web.py"
      provides: "Test scaffold for dashboard routes"
  key_links:
    - from: "src/dashboard/presentation/app.py"
      to: "src/bootstrap.py"
      via: "bootstrap() ctx stored on app.state"
      pattern: "app\\.state\\.ctx"
    - from: "src/dashboard/infrastructure/sse_bridge.py"
      to: "src/shared/infrastructure/sync_event_bus.py"
      via: "subscribe to domain events, push to asyncio.Queue"
      pattern: "bus\\.subscribe"
---

<objective>
Create the dashboard bounded context skeleton: FastAPI app factory, base template with sidebar + mode banner, SSE bridge infrastructure, Plotly chart utility module, and all empty page templates. This is the foundation all other plans build upon.

Purpose: Establish the entire dashboard structure so subsequent plans (02-04) only add page content, not infrastructure.
Output: Working dashboard at /dashboard/ with sidebar navigation, mode banner, and empty page shells.
</objective>

<execution_context>
@/home/mqz/.claude/get-shit-done/workflows/execute-plan.md
@/home/mqz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-web-dashboard/16-CONTEXT.md
@.planning/phases/16-web-dashboard/16-RESEARCH.md
@src/bootstrap.py
@src/settings.py
@src/shared/infrastructure/sync_event_bus.py

<interfaces>
<!-- Key types and contracts from existing codebase -->

From src/settings.py:
```python
class Settings(BaseSettings):
    EXECUTION_MODE: Literal["paper", "live"] = "paper"
    PIPELINE_SCHEDULE_HOUR: int = 16
    PIPELINE_SCHEDULE_MINUTE: int = 30
settings = Settings()
```

From src/shared/infrastructure/sync_event_bus.py:
```python
class SyncEventBus:
    def subscribe(self, event_type: type, handler: Callable) -> None: ...
    def publish(self, event: Any) -> None: ...
```

From src/execution/domain/value_objects.py:
```python
class ExecutionMode(str, Enum):
    PAPER = "paper"
    LIVE = "live"
```

From src/execution/domain/events.py:
```python
class OrderFilledEvent(DomainEvent): ...
```

From src/regime/domain/events.py:
```python
class RegimeChangedEvent(DomainEvent): ...
```

From src/pipeline/domain/events.py:
```python
class PipelineCompletedEvent(DomainEvent): ...
class PipelineHaltedEvent(DomainEvent): ...
```

From src/portfolio/domain/events.py:
```python
class DrawdownAlertEvent(DomainEvent): ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard bounded context skeleton + SSE bridge + chart utilities</name>
  <files>
    pyproject.toml
    src/settings.py
    src/dashboard/__init__.py
    src/dashboard/DOMAIN.md
    src/dashboard/application/__init__.py
    src/dashboard/application/queries.py
    src/dashboard/infrastructure/__init__.py
    src/dashboard/infrastructure/sse_bridge.py
    src/dashboard/presentation/__init__.py
    src/dashboard/presentation/charts.py
  </files>
  <action>
1. Add `"plotly>=6.0"` to pyproject.toml dependencies list. Add `"jinja2>=3.1"` if not already present (it should be a FastAPI transitive dep but make explicit).

2. Add `DASHBOARD_HOST: str = "0.0.0.0"` and `DASHBOARD_PORT: int = 8000` to Settings in src/settings.py.

3. Create src/dashboard/ bounded context with standard DDD structure:
   - `__init__.py`: empty
   - `DOMAIN.md`: Dashboard context description (read-only view context, no domain layer)
   - `application/__init__.py`: empty
   - `application/queries.py`: Define query dataclasses for each page:
     - `OverviewQuery` (no params)
     - `SignalsQuery` (no params)
     - `RiskQuery` (no params)
     - `PipelineQuery` (no params)
     These are placeholders for Plan 02-04 to implement handlers.

4. Create `src/dashboard/infrastructure/sse_bridge.py`:
   - `SSEBridge` class with `__init__(self, bus: SyncEventBus)`
   - `_queues: list[asyncio.Queue]` for fan-out to multiple SSE consumers
   - `_on_event(self, event)` sync handler: serialize event to dict with "type" (class name) and "payload" (dict of attrs as strings), call `q.put_nowait(data)` for each queue (catch QueueFull, drop)
   - `subscribe_events(self, *event_types)`: call `self._bus.subscribe(et, self._on_event)` for each
   - `async def stream(self) -> AsyncIterator[dict]`: create Queue(maxsize=100), append to _queues, yield from queue in try/finally that removes queue

5. Create `src/dashboard/presentation/charts.py`:
   - `build_equity_curve(values: list[float], dates: list[str], regime_periods: list[dict]) -> dict`: Plotly line chart with regime background vrects (Bull=green, Bear=red, Sideways=yellow, Crisis=gray), dark template, returns JSON-serializable dict via `json.loads(fig.to_json())`
   - `build_drawdown_gauge(current_drawdown_pct: float) -> dict`: Plotly go.Indicator gauge with axis range [0, 20] (per user decision: 0-20% with 10/15/20% markers matching the 3-tier drawdown defense). Three color steps: green 0-10%, yellow 10-15%, red 15-20%. Do NOT extend beyond 20% -- the system triggers full liquidation at 20%.
   - `build_sector_donut(sectors: dict[str, float]) -> dict`: Plotly go.Pie with hole=0.5, returns JSON dict
   - Use `import plotly.graph_objects as go` and `import json`
  </action>
  <verify>
    <automated>cd /home/mqz/workspace/trading && python -c "from src.dashboard.infrastructure.sse_bridge import SSEBridge; from src.dashboard.presentation.charts import build_drawdown_gauge; print('imports OK')"</automated>
  </verify>
  <done>Dashboard bounded context created with SSEBridge, chart utilities, and query placeholders. plotly added to dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: FastAPI app, base template, page shells, routes, and test scaffold</name>
  <files>
    src/dashboard/presentation/app.py
    src/dashboard/presentation/routes.py
    src/dashboard/presentation/templates/base.html
    src/dashboard/presentation/templates/overview.html
    src/dashboard/presentation/templates/signals.html
    src/dashboard/presentation/templates/risk.html
    src/dashboard/presentation/templates/pipeline.html
    src/dashboard/presentation/templates/partials/holdings_table.html
    src/dashboard/presentation/templates/partials/kpi_cards.html
    src/dashboard/presentation/templates/partials/pipeline_status.html
    src/dashboard/presentation/templates/partials/drawdown_gauge.html
    src/dashboard/presentation/templates/partials/regime_badge.html
    tests/unit/test_dashboard_web.py
  </files>
  <action>
1. Create `src/dashboard/presentation/app.py`:
   - `create_dashboard_app(ctx: dict | None = None) -> FastAPI`: factory function
   - Creates FastAPI(title="Trading Dashboard")
   - If ctx is None, calls `bootstrap()` from src.bootstrap
   - Stores ctx on `app.state.ctx`
   - Creates SSEBridge(ctx["bus"]), subscribes to OrderFilledEvent, PipelineCompletedEvent, PipelineHaltedEvent, DrawdownAlertEvent, RegimeChangedEvent
   - Stores bridge on `app.state.sse_bridge`
   - Includes dashboard router from routes.py
   - Returns app

2. Create `src/dashboard/presentation/routes.py`:
   - `router = APIRouter(prefix="/dashboard")`
   - `TEMPLATE_DIR = Path(__file__).parent / "templates"`
   - `templates = Jinja2Templates(directory=str(TEMPLATE_DIR))`
   - GET `/` -> overview page (for now: renders overview.html with execution_mode from ctx)
   - GET `/signals` -> signals page (shell)
   - GET `/risk` -> risk page (shell)
   - GET `/pipeline` -> pipeline page (shell)
   - GET `/events` -> SSE endpoint: `async def sse_events(request: Request)` returning EventSourceResponse that yields ServerSentEvent from sse_bridge.stream()
   - All page routes use `def` (sync, not async) since they read from sync repos
   - Pass `execution_mode` to every template for the mode banner

3. Create `base.html` template:
   - DOCTYPE html, lang="en"
   - Head: meta charset, viewport, title "Trading Dashboard"
   - CDN scripts in order: Tailwind CSS, HTMX 2.0.8, htmx-ext-sse 2.2.4, plotly-3.4.0
   - Body with `hx-ext="sse"` and `sse-connect="/dashboard/events"`
   - Mode banner: if execution_mode == "live" -> red bg "LIVE TRADING", else green bg "PAPER TRADING" (DASH-09)
   - Flex layout: sidebar (w-64, bg-gray-900, min-h-screen) with 4 nav links (Overview /, Signals /signals, Risk /risk, Pipeline /pipeline) with current page highlighting via `{% block active_page %}` pattern
   - Main content area (flex-1, p-6, bg-gray-100) with `{% block content %}{% endblock %}`
   - Block `{% block extra_scripts %}{% endblock %}` before closing body for page-specific JS

4. Create page shell templates (overview.html, signals.html, risk.html, pipeline.html):
   - Each extends base.html
   - Each has `{% block content %}` with placeholder heading "Overview / Signals / Risk / Pipeline & Approval"
   - overview.html: empty divs with ids for kpi-cards, holdings-table, equity-chart (with sse-swap attributes)
   - signals.html: empty div for scoring-table
   - risk.html: empty divs for drawdown-gauge, sector-chart, position-limits
   - pipeline.html: empty divs for pipeline-runs, approval-status, budget-bar, review-queue

5. Create partial templates (all in partials/):
   - `holdings_table.html`: empty table shell with headers (Ticker, Qty, Price, P&L %, P&L $, Stop, Target, Score)
   - `kpi_cards.html`: 4 card placeholders
   - `pipeline_status.html`: status text placeholder
   - `drawdown_gauge.html`: chart container div
   - `regime_badge.html`: badge span placeholder

6. Create `tests/unit/test_dashboard_web.py`:
   - Import FastAPI TestClient, create_dashboard_app
   - Fixture: create app with mock ctx (minimal dict with execution_mode=ExecutionMode.PAPER, bus=SyncEventBus(), and stub repos)
   - `test_overview_page`: GET /dashboard/ returns 200, response contains "PAPER TRADING" and "Overview"
   - `test_signals_page`: GET /dashboard/signals returns 200
   - `test_risk_page`: GET /dashboard/risk returns 200
   - `test_pipeline_page`: GET /dashboard/pipeline returns 200
   - `test_mode_banner_live`: create app with execution_mode=LIVE, verify "LIVE TRADING" in response
   - `test_mode_banner_paper`: verify "PAPER TRADING" in response
   - `test_sse_endpoint_connects`: GET /dashboard/events returns 200 with content-type text/event-stream
  </action>
  <verify>
    <automated>cd /home/mqz/workspace/trading && python -m pytest tests/unit/test_dashboard_web.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <done>Dashboard serves 4 pages with sidebar navigation and paper/live mode banner. SSE endpoint connects. All route tests pass.</done>
</task>

</tasks>

<verification>
- `python -c "from src.dashboard.presentation.app import create_dashboard_app; print('app factory OK')"` succeeds
- `pytest tests/unit/test_dashboard_web.py -x` passes all tests
- Mode banner shows PAPER (green) or LIVE (red) based on execution_mode (DASH-09)
- All 4 pages render via sidebar navigation
</verification>

<success_criteria>
- Dashboard bounded context exists at src/dashboard/ with DDD structure
- FastAPI app factory creates working app with bootstrap ctx
- Base template renders sidebar + mode banner + CDN scripts
- SSE bridge subscribes to domain events and streams to consumers
- Chart utility functions generate valid Plotly JSON
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/16-web-dashboard/16-01-SUMMARY.md`
</output>
