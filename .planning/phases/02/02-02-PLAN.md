---
phase: 02-analysis-core
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - core/valuation/__init__.py
  - core/valuation/dcf.py
  - core/valuation/epv.py
  - core/valuation/relative.py
  - src/valuation/__init__.py
  - src/valuation/domain/__init__.py
  - src/valuation/domain/value_objects.py
  - src/valuation/domain/events.py
  - src/valuation/domain/repositories.py
  - src/valuation/domain/services.py
  - src/valuation/DOMAIN.md
  - tests/unit/test_dcf_model.py
  - tests/unit/test_epv_model.py
  - tests/unit/test_relative_multiples.py
  - tests/unit/test_valuation_vos.py
autonomous: true
requirements: [VALU-01, VALU-02, VALU-03]

must_haves:
  truths:
    - "DCF model produces intrinsic value with terminal value capped at 40% and WACC clipped to 6-14%"
    - "EPV model produces normalized earnings-based valuation using 5-year averaged operating margin"
    - "Relative multiples rank stocks by PER/PBR/EV-EBITDA percentile within GICS sector"
    - "All valuation results are immutable frozen dataclass VOs with validation"
    - "ValuationCompletedEvent carries all model outputs for downstream consumption"
  artifacts:
    - path: "core/valuation/dcf.py"
      provides: "compute_wacc() and compute_dcf() pure math"
      exports: ["compute_wacc", "compute_dcf"]
    - path: "core/valuation/epv.py"
      provides: "compute_epv() pure math"
      exports: ["compute_epv"]
    - path: "core/valuation/relative.py"
      provides: "compute_relative() pure math"
      exports: ["compute_relative"]
    - path: "src/valuation/domain/value_objects.py"
      provides: "DCFResult, EPVResult, RelativeMultiplesResult, WACC, IntrinsicValue, MarginOfSafety VOs"
      min_lines: 80
    - path: "src/valuation/domain/events.py"
      provides: "ValuationCompletedEvent"
      contains: "ValuationCompletedEvent"
    - path: "src/valuation/domain/repositories.py"
      provides: "IValuationRepository ABC"
      contains: "class IValuationRepository"
    - path: "src/valuation/DOMAIN.md"
      provides: "Bounded context documentation"
      min_lines: 20
    - path: "tests/unit/test_dcf_model.py"
      provides: "DCF unit tests with WACC clipping and TV cap"
      min_lines: 50
    - path: "tests/unit/test_epv_model.py"
      provides: "EPV unit tests"
      min_lines: 30
    - path: "tests/unit/test_relative_multiples.py"
      provides: "Relative multiples unit tests"
      min_lines: 30
    - path: "tests/unit/test_valuation_vos.py"
      provides: "VO validation tests"
      min_lines: 40
  key_links:
    - from: "core/valuation/dcf.py"
      to: "nothing external"
      via: "pure math, no imports beyond stdlib"
      pattern: "def compute_dcf"
    - from: "src/valuation/domain/events.py"
      to: "src/shared/domain"
      via: "DomainEvent base class"
      pattern: "from src\\.shared\\.domain import DomainEvent"
    - from: "src/valuation/domain/value_objects.py"
      to: "src/shared/domain"
      via: "ValueObject base class"
      pattern: "from src\\.shared\\.domain import ValueObject"
---

<objective>
Create the valuation bounded context domain layer (VOs, events, repository interface) and implement the three core valuation models as pure math functions (DCF, EPV, Relative Multiples).

Purpose: Establishes the valuation domain foundation and the mathematical models that Plan 03 will wire into the ensemble. Keeping pure math in core/valuation/ follows the CoreScoringAdapter pattern from Phase 1.
Output: New src/valuation/ bounded context with domain layer, core/valuation/ with 3 model implementations, comprehensive TDD tests.
</objective>

<execution_context>
@/home/mqz/.claude/get-shit-done/workflows/execute-plan.md
@/home/mqz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02/02-CONTEXT.md
@.planning/phases/02/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/shared/domain/__init__.py:
```python
from .base_value_object import ValueObject
from .domain_event import DomainEvent
```

From src/data_ingest/domain/events.py (event pattern to follow):
```python
@dataclass(frozen=True, kw_only=True)
class DataIngestedEvent(DomainEvent):
    ticker: str
    ohlcv_rows: int
    financial_quarters: int
```

From src/data_ingest/infrastructure/duckdb_store.py (store pattern):
```python
class DuckDBStore:
    def connect(self, db_path: str = "data/analytics.duckdb") -> None
    def store_financials(self, ticker: str, records: list[dict]) -> None
    def get_latest_financials(self, ticker: str, as_of_date: date) -> pd.DataFrame
```

Existing DuckDB financials schema columns:
```
ticker, period_end, filing_date, form_type, revenue, net_income, total_assets,
total_liabilities, working_capital, retained_earnings, ebit, operating_cashflow,
free_cashflow, current_ratio, debt_to_equity, roa, roe
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create valuation bounded context skeleton + domain VOs + events</name>
  <files>src/valuation/__init__.py, src/valuation/domain/__init__.py, src/valuation/domain/value_objects.py, src/valuation/domain/events.py, src/valuation/domain/repositories.py, src/valuation/domain/services.py, src/valuation/application/__init__.py, src/valuation/infrastructure/__init__.py, src/valuation/DOMAIN.md, tests/unit/test_valuation_vos.py</files>
  <action>
    Create the valuation bounded context directory structure following the exact DDD pattern from src/data_ingest/ and src/scoring/:

    1. Create directory structure:
       - src/valuation/
       - src/valuation/domain/
       - src/valuation/application/
       - src/valuation/infrastructure/

    2. Create src/valuation/domain/value_objects.py with frozen dataclass VOs:
       - WACC(value: float, beta: float, risk_free_rate: float, equity_risk_premium: float, cost_of_equity: float, cost_of_debt: float, weight_equity: float, weight_debt: float, clipped: bool = False). Validate value in [0.01, 0.30] range.
       - DCFResult(dcf_value: float, per_share: float, tv_pct: float, tv_capped: bool, wacc: WACC, projected_fcfs: tuple[float, ...], confidence_penalty: float = 0.0). Validate dcf_value >= 0.
       - EPVResult(epv_total: float, per_share: float, normalized_margin: float, adjusted_earnings: float, earnings_cv: float | None = None). Validate epv_total >= 0.
       - RelativeMultiplesResult(per_percentile: float | None, pbr_percentile: float | None, ev_ebitda_percentile: float | None, composite_percentile: float, sector: str, peer_count: int). Validate percentiles 0-100 when not None.
       - IntrinsicValue(mid: float, dcf_component: float, epv_component: float, relative_component: float, confidence: float, effective_weights: dict[str, float]). Validate mid >= 0, confidence in [0, 1].
       - MarginOfSafety(value: float, intrinsic_mid: float, market_price: float, sector_threshold: float, has_margin: bool). Validate market_price > 0.
       All VOs inherit from ValueObject and use _validate().

    3. Create src/valuation/domain/events.py:
       - ValuationCompletedEvent(DomainEvent, kw_only=True): ticker, intrinsic_value, margin_of_safety, confidence, dcf_value, epv_value, relative_value, sector

    4. Create src/valuation/domain/repositories.py:
       - IValuationRepository(ABC): abstract methods save_valuation(ticker, result_dict) and get_valuation(ticker, as_of_date)

    5. Create src/valuation/domain/services.py:
       - Empty file with docstring placeholder for EnsembleValuationService (implemented in Plan 03)

    6. Create all __init__.py files with appropriate public API exports

    7. Create src/valuation/DOMAIN.md per DDD rules:
       - Responsibility: Intrinsic value estimation through ensemble valuation models
       - Core entities: DCFResult, EPVResult, RelativeMultiplesResult, IntrinsicValue, MarginOfSafety
       - External dependencies: data_ingest (via events for financial data), scoring (via events for G-Score)
       - Use cases: Single-stock valuation, batch universe valuation, margin of safety screening
       - Invariant rules: TV cap 40%, WACC clip 6-14%, ensemble weights DCF 40% + EPV 35% + Relative 25%

    8. Create tests/unit/test_valuation_vos.py:
       - Test all VO construction and validation: valid values pass, invalid values raise ValueError
       - Test WACC clipping flag, DCFResult tv_capped flag, MarginOfSafety has_margin flag
       - Test ValuationCompletedEvent construction with kw_only
  </action>
  <verify>
    <automated>cd /home/mqz/workspace/trading && .venv/bin/pytest tests/unit/test_valuation_vos.py -x -v</automated>
  </verify>
  <done>Valuation bounded context skeleton created with all DDD layers, domain VOs validate correctly, event follows kw_only pattern, DOMAIN.md documents the context</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: DCF + EPV + Relative Multiples pure math in core/valuation/</name>
  <files>core/valuation/__init__.py, core/valuation/dcf.py, core/valuation/epv.py, core/valuation/relative.py, tests/unit/test_dcf_model.py, tests/unit/test_epv_model.py, tests/unit/test_relative_multiples.py</files>
  <behavior>
    DCF tests (test_dcf_model.py):
    - Test 1: compute_wacc() clips result to 6% floor (CAPM gives 4% -> returns 0.06)
    - Test 2: compute_wacc() clips result to 14% ceiling (CAPM gives 18% -> returns 0.14)
    - Test 3: compute_wacc() returns CAPM result when within 6-14% range
    - Test 4: compute_dcf() with base_fcf=1B, growth=10%, wacc=10% produces reasonable DCF (>0)
    - Test 5: compute_dcf() caps terminal value at 40% of total when TV exceeds 40%
    - Test 6: compute_dcf() tv_capped=False when TV is naturally under 40%
    - Test 7: compute_dcf() returns confidence_penalty=0.2 when TV cap triggers
    - Test 8: compute_dcf() with negative base_fcf returns dcf_value=0 (no negative DCF)
    - Test 9: compute_dcf() Terminal Value averages Gordon Growth and Exit Multiple

    EPV tests (test_epv_model.py):
    - Test 1: compute_epv() with stable margins produces positive EPV
    - Test 2: compute_epv() uses 5-year average operating margin (not single year)
    - Test 3: compute_epv() maintenance_capex = depreciation * 1.1
    - Test 4: compute_epv() with zero wacc returns zero EPV with confidence=0
    - Test 5: compute_epv() flags earnings_cv > 0.5 as cyclical warning
    - Test 6: compute_epv() with zero shares_outstanding returns per_share=0

    Relative tests (test_relative_multiples.py):
    - Test 1: compute_relative() returns percentile ranking within provided sector data
    - Test 2: compute_relative() excludes negative-PER stocks from PER ranking (returns None)
    - Test 3: compute_relative() excludes negative-EBITDA stocks from EV/EBITDA ranking (returns None)
    - Test 4: compute_relative() composite percentile averages available multiples only
    - Test 5: compute_relative() stock at sector median gets ~50th percentile
    - Test 6: compute_relative() with all multiples unavailable returns composite_percentile=None
  </behavior>
  <action>
    RED: Create tests/unit/test_dcf_model.py, tests/unit/test_epv_model.py, tests/unit/test_relative_multiples.py with all test cases above. Tests import from core.valuation.dcf, core.valuation.epv, core.valuation.relative. Run -- all must fail.

    GREEN: Create core/valuation/ directory with __init__.py.

    1. Implement core/valuation/dcf.py:
       - compute_wacc(beta, risk_free_rate, equity_risk_premium, debt_to_equity, cost_of_debt, tax_rate) -> dict with keys: wacc, cost_of_equity, weight_equity, weight_debt, clipped. WACC = We*Ke + Wd*Kd*(1-T). Clip to [0.06, 0.14] per locked decision. Set clipped=True if bounds hit.
       - compute_dcf(base_fcf, stage1_growth, stage2_growth, wacc, exit_multiple, projection_years=5, terminal_value_cap=0.40) -> dict with keys: dcf_value, projected_fcfs, tv_gordon, tv_exit, tv_average, tv_pct, tv_capped, confidence_penalty. If base_fcf <= 0, return dcf_value=0. Terminal Value = average of Gordon Growth Model and Exit Multiple method. Cap TV at 40% of total. If cap triggers, confidence_penalty=0.2.

    2. Implement core/valuation/epv.py:
       - compute_epv(revenues: list[float], operating_margins: list[float], tax_rate, depreciation, wacc, shares_outstanding) -> dict with keys: epv_total, epv_per_share, normalized_margin, adjusted_earnings, earnings_cv. Use 5-year average operating margin applied to most recent revenue. Maintenance CapEx = depreciation * 1.1. Adjusted earnings = NOPAT + depreciation - maintenance_capex. EPV = adjusted_earnings / wacc. If wacc <= 0, return zeros with confidence=0. Calculate coefficient of variation of annual earnings for cyclical detection (flag if CV > 0.5).

    3. Implement core/valuation/relative.py:
       - compute_relative(per, pbr, ev_ebitda, sector_pers: list[float], sector_pbrs: list[float], sector_ev_ebitdas: list[float]) -> dict with keys: per_percentile, pbr_percentile, ev_ebitda_percentile, composite_percentile, peer_count. Exclude negative PER from ranking (return None). Exclude negative EV/EBITDA from ranking (return None). PBR always ranked (can be negative for distressed). Composite = average of available percentiles. If no multiples available, composite_percentile=None. Percentile = (count of peers below value / total peers) * 100.

    All functions are pure math -- no imports beyond stdlib (math, statistics).

    Run tests -- all must pass.
  </action>
  <verify>
    <automated>cd /home/mqz/workspace/trading && .venv/bin/pytest tests/unit/test_dcf_model.py tests/unit/test_epv_model.py tests/unit/test_relative_multiples.py -x -v</automated>
  </verify>
  <done>All three valuation models produce correct results for known inputs, WACC clips to 6-14%, TV caps at 40%, EPV uses 5-year margin average, Relative excludes negative multiples, all tests pass</done>
</task>

</tasks>

<verification>
```bash
cd /home/mqz/workspace/trading
.venv/bin/pytest tests/unit/test_valuation_vos.py tests/unit/test_dcf_model.py tests/unit/test_epv_model.py tests/unit/test_relative_multiples.py -v
.venv/bin/mypy src/valuation/ --ignore-missing-imports
.venv/bin/ruff check src/valuation/ core/valuation/
```
</verification>

<success_criteria>
- Valuation bounded context exists at src/valuation/ with proper DDD structure
- All 6 domain VOs validate correctly (WACC, DCFResult, EPVResult, RelativeMultiplesResult, IntrinsicValue, MarginOfSafety)
- ValuationCompletedEvent follows kw_only pattern
- DCF: WACC clips 6-14%, TV caps at 40%, negative FCF returns 0
- EPV: 5-year margin average, maintenance capex proxy, CV cyclical flag
- Relative: excludes negative PER/EBITDA, percentile ranking, composite average
- DOMAIN.md documents bounded context per DDD rules
- All tests pass, mypy clean, ruff clean
</success_criteria>

<output>
After completion, create `.planning/phases/02/02-02-SUMMARY.md`
</output>
