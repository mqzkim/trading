---
phase: 06-live-data-pipeline-korean-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data_ingest/infrastructure/edgartools_client.py
  - src/data_ingest/domain/value_objects.py
  - src/data_ingest/domain/__init__.py
  - src/data_ingest/infrastructure/quality_checker.py
  - src/scoring/domain/value_objects.py
  - tests/unit/test_edgartools_client.py
  - tests/unit/test_data_ingest_vos.py
  - tests/unit/test_quality_checker.py
  - tests/unit/test_yfinance_adapter.py
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03]

must_haves:
  truths:
    - "edgartools_client stores the actual ticker symbol (not filing_date) in financial records"
    - "Ticker VO accepts Korean 6-digit numeric tickers (e.g., '005930') in addition to US uppercase tickers"
    - "scoring Symbol VO accepts Korean numeric tickers without raising ValueError"
    - "QualityChecker staleness uses business-day-aware logic (no false positives on weekends/holidays)"
    - "QualityChecker accepts a configurable max_stale_days parameter for market-specific thresholds"
    - "yfinance pipeline with auto_adjust=True returns adjusted OHLCV columns and no separate Adj Close column"
    - "edgartools handles small-cap tickers with missing XBRL fields gracefully (returns partial data, not exception)"
  artifacts:
    - path: "src/data_ingest/infrastructure/edgartools_client.py"
      provides: "Fixed ticker field on line 154"
      contains: "\"ticker\": ticker"
    - path: "src/data_ingest/domain/value_objects.py"
      provides: "Ticker VO with extended regex for Korean tickers, MarketType enum"
      contains: "MarketType"
    - path: "src/data_ingest/infrastructure/quality_checker.py"
      provides: "Business-day-aware staleness check"
      contains: "max_stale_days"
    - path: "src/scoring/domain/value_objects.py"
      provides: "Symbol VO accepting numeric Korean tickers"
      contains: "isupper() or self.ticker.isdigit()"
    - path: "tests/unit/test_yfinance_adapter.py"
      provides: "Validation that auto_adjust=True returns correct adjusted OHLCV columns"
      contains: "auto_adjust"
  key_links:
    - from: "src/data_ingest/infrastructure/edgartools_client.py"
      to: "DuckDB financials table"
      via: "ticker field in returned dict"
      pattern: "\"ticker\": ticker"
    - from: "src/data_ingest/domain/value_objects.py"
      to: "src/data_ingest/infrastructure/pipeline.py"
      via: "Ticker VO construction"
      pattern: "Ticker\\("
    - from: "src/data_ingest/infrastructure/yfinance_client.py"
      to: "core/data/client.py"
      via: "CoreDataClient wrapping yfinance with auto_adjust=True"
      pattern: "auto_adjust"
---

<objective>
Fix US data pipeline bugs, validate yfinance adjusted close, validate edgartools small-cap coverage, and harden quality validation for multi-market support

Purpose: The edgartools ticker bug (line 154) silently corrupts stored financials by putting filing_date in the ticker field. The Ticker VO regex rejects Korean numeric tickers. The staleness checker produces false positives on weekends. The yfinance pipeline needs empirical validation that auto_adjust=True returns correct adjusted OHLCV. The edgartools pipeline needs empirical validation against small-cap tickers with potentially sparse XBRL. All must be fixed/validated before Korean data or regime data can be added.

Output: Corrected edgartools_client.py, extended Ticker/Symbol VOs, business-day-aware QualityChecker, validated yfinance adjusted close pipeline, validated edgartools small-cap coverage
</objective>

<execution_context>
@/home/mqz/.claude/get-shit-done/workflows/execute-plan.md
@/home/mqz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-live-data-pipeline-korean-data/06-RESEARCH.md

<interfaces>
<!-- Existing types the executor needs -->

From src/data_ingest/domain/value_objects.py:
```python
_TICKER_RE = re.compile(r"^[A-Z]{1,10}$")

@dataclass(frozen=True)
class Ticker(ValueObject):
    ticker: str
    def _validate(self) -> None:
        if not _TICKER_RE.match(self.ticker):
            raise ValueError(...)
```

From src/scoring/domain/value_objects.py:
```python
@dataclass(frozen=True)
class Symbol(ValueObject):
    ticker: str
    def _validate(self) -> None:
        if not self.ticker or not self.ticker.isupper():
            raise ValueError(...)
        if len(self.ticker) > 10:
            raise ValueError(...)
```

From src/data_ingest/infrastructure/edgartools_client.py (line 153-154, the bug):
```python
return {
    "ticker": str(filing_date),  # BUG: should be ticker parameter
    ...
}
```

From src/data_ingest/infrastructure/yfinance_client.py:
```python
class YFinanceClient:
    def __init__(self, semaphore=None):
        self._client = CoreDataClient()  # uses auto_adjust=True internally
    async def fetch_ohlcv(self, ticker, days=756) -> pd.DataFrame:
        # Returns DataFrame with columns: open, high, low, close, volume
```

From core/data/client.py (line 82, auto_adjust behavior):
```python
# auto_adjust=True means ALL OHLCV columns are already adjusted for splits/dividends
# No separate "Adj Close" column is returned
```

From src/data_ingest/infrastructure/quality_checker.py:
```python
class QualityChecker:
    def validate_ohlcv(self, ticker, df, max_stale_days=3) -> DataQualityReport:
        # Uses calendar days: stale_days = (now - last_date).days
```

From src/shared/domain/__init__.py:
```python
class ValueObject:
    def __post_init__(self): self._validate()
    def _validate(self) -> None: ...
class DomainEvent: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix edgartools ticker bug, extend Ticker/Symbol VOs, and validate small-cap XBRL coverage</name>
  <files>
    src/data_ingest/infrastructure/edgartools_client.py,
    src/data_ingest/domain/value_objects.py,
    src/data_ingest/domain/__init__.py,
    src/scoring/domain/value_objects.py,
    tests/unit/test_edgartools_client.py,
    tests/unit/test_data_ingest_vos.py
  </files>
  <behavior>
    - Test: edgartools _extract_filing returns dict with "ticker" field equal to the ticker parameter, not filing_date
    - Test: Ticker("AAPL") succeeds (US format unchanged)
    - Test: Ticker("005930") succeeds (Korean 6-digit numeric)
    - Test: Ticker("") raises ValueError
    - Test: Ticker("abc") raises ValueError (lowercase)
    - Test: MarketType.US.value == "us" and MarketType.KR.value == "kr"
    - Test: scoring Symbol("005930") succeeds (Korean numeric)
    - Test: scoring Symbol("AAPL") succeeds (US unchanged)
    - Test: scoring Symbol("abc") raises ValueError (lowercase, non-numeric)
    - Test: edgartools handles small-cap ticker with missing XBRL fields gracefully (returns partial data with 0.0 defaults, not exception)
    - Test: edgartools handles small-cap ticker where some financial statements are None (e.g., no cash flow statement) -- returns record with operating_cashflow=0.0
    - Test: edgartools returns empty list (not exception) when Company() raises for unknown small-cap ticker
  </behavior>
  <action>
    1. Fix edgartools_client.py line 154: change `"ticker": str(filing_date)` to `"ticker": ticker`. This is the actual ticker parameter passed to `_extract_filing`. Add `ticker` parameter to the method signature (currently it only receives `filing` and `form_type`). Update `_fetch_sync` to pass ticker to `_extract_filing(filing, form_type="10-Q", ticker=ticker)`.

    2. In value_objects.py: Add `MarketType` enum (US="us", KR="kr") with `from enum import Enum`. Change `_TICKER_RE` from `^[A-Z]{1,10}$` to `^[A-Z0-9]{1,10}$` so both "AAPL" and "005930" are accepted. Keep the existing validation error message but update it to say "uppercase letters or digits".

    3. Export `MarketType` from data_ingest/domain/__init__.py.

    4. In scoring/domain/value_objects.py: Change Symbol._validate from `self.ticker.isupper()` to `self.ticker.isupper() or self.ticker.isdigit()` so Korean numeric tickers pass validation. Keep the length check (max 10).

    5. Add tests to test_edgartools_client.py verifying the ticker field fix. Add tests to test_data_ingest_vos.py for Korean ticker format and MarketType enum.

    6. DATA-02 validation: Add small-cap XBRL coverage tests to test_edgartools_client.py. Create mock filings that simulate small-cap scenarios:
       - Filing with income_statement but no cash_flow_statement (statement is None) -- verify record returned with operating_cashflow=0.0
       - Filing with all financial statements but get_value returns default 0.0 for most XBRL fields (sparse XBRL) -- verify record returned with zero values rather than raising
       - Company not found for obscure small-cap ticker -- verify empty list returned
       These tests validate that the edgartools pipeline handles the sparse/missing XBRL data typical of small-cap filings (per RESEARCH.md open question 3).
  </action>
  <verify>
    <automated>cd /home/mqz/workspace/trading && python -m pytest tests/unit/test_edgartools_client.py tests/unit/test_data_ingest_vos.py -x -v</automated>
  </verify>
  <done>edgartools returns correct ticker in financial records; Ticker VO accepts both "AAPL" and "005930"; Symbol VO accepts Korean numeric tickers; MarketType enum exists; edgartools tested against representative small-cap scenarios (missing XBRL fields return None/0.0 rather than raising); DATA-02 validated</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make QualityChecker staleness business-day-aware</name>
  <files>
    src/data_ingest/infrastructure/quality_checker.py,
    tests/unit/test_quality_checker.py
  </files>
  <behavior>
    - Test: Data from Friday checked on Monday (3 calendar days, 1 business day) passes with default max_stale_days=3
    - Test: Data from 5+ business days ago fails with default max_stale_days=3
    - Test: max_stale_days=5 allows data from 5 business days ago (covers Korean holidays)
    - Test: Empty DataFrame still returns stale_days=0 (no regression)
    - Test: Weekend-only gap (Friday to Monday) does not trigger stale warning
  </behavior>
  <action>
    1. In quality_checker.py, change the staleness calculation from calendar days to business days. Use `numpy.busday_count(last_date.date(), now.date())` or `pd.bdate_range(last_date, now).size - 1` to count business days between last data point and now. Import numpy (already a project dependency).

    2. The `max_stale_days` parameter semantics change from "calendar days" to "business days". Default remains 3 (which now covers weekends automatically -- Friday data on Monday = 1 business day, well under 3).

    3. Update the failure message to say "business days" instead of "days": `f"Stale data: {stale_bdays} business days since last update (threshold: {max_stale_days})"`.

    4. Add/update tests in test_quality_checker.py covering: weekend gap (Friday->Monday = 1 bday, passes), long gap (>3 bdays, fails), custom threshold (max_stale_days=5), empty DataFrame edge case.

    Research pitfall 7 note: Korean market has more holidays (Chuseok, Lunar New Year). When Plan 02 builds the pykrx adapter, the pipeline should pass max_stale_days=5 for Korean data. This task just makes the parameter work correctly.
  </action>
  <verify>
    <automated>cd /home/mqz/workspace/trading && python -m pytest tests/unit/test_quality_checker.py -x -v</automated>
  </verify>
  <done>QualityChecker counts business days for staleness; Friday-to-Monday gap does not trigger stale warning; configurable threshold works for multi-market use</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Validate yfinance auto_adjust=True returns adjusted OHLCV (DATA-01)</name>
  <files>
    tests/unit/test_yfinance_adapter.py
  </files>
  <behavior>
    - Test: YFinanceClient.fetch_ohlcv returns DataFrame with exactly columns {open, high, low, close, volume} -- no "Adj Close" or "adj_close" column present
    - Test: YFinanceClient.fetch_ohlcv returns adjusted values (Close column IS the adjusted close, not raw close) by verifying CoreDataClient is called with auto_adjust=True semantics
    - Test: OHLCV DataFrame returned by fetch_ohlcv has no NaN values in close column (auto_adjust does not introduce NaNs)
    - Test: Column names are lowercase (open, high, low, close, volume) matching system convention
  </behavior>
  <action>
    1. Add new test class `TestYFinanceAdjustedCloseValidation` to the existing tests/unit/test_yfinance_adapter.py file.

    2. Add test `test_ohlcv_has_no_adj_close_column`: Mock CoreDataClient to return a DataFrame with columns {open, high, low, close, volume}. Call fetch_ohlcv. Assert "Adj Close" not in result.columns and "adj_close" not in result.columns. This validates that auto_adjust=True means the Close column IS the adjusted close -- there is no separate adjusted column (per RESEARCH.md pitfall 6 and yfinance 0.2.28+ behavior).

    3. Add test `test_ohlcv_columns_are_adjusted_lowercase`: Assert result.columns exactly equals {"open", "high", "low", "close", "volume"} -- confirming the system's convention of lowercase adjusted OHLCV.

    4. Add test `test_ohlcv_close_has_no_nans`: Mock with a DataFrame where all close values are non-NaN. Verify result["close"].isna().sum() == 0. This ensures auto_adjust does not introduce NaN artifacts.

    5. Add test `test_core_client_receives_ticker_and_days`: Verify CoreDataClient.get_price_history is called with the exact ticker and days parameters, confirming the adapter passes through correctly to the client that uses auto_adjust=True.

    NOTE: These tests validate DATA-01 by confirming the yfinance pipeline returns adjusted OHLCV in the expected format. The underlying auto_adjust=True is set in core/data/client.py (already correct per RESEARCH.md pitfall 6). These unit tests validate the contract -- live API validation is a manual-only verification (see VALIDATION.md).
  </action>
  <verify>
    <automated>cd /home/mqz/workspace/trading && python -m pytest tests/unit/test_yfinance_adapter.py -x -v</automated>
  </verify>
  <done>yfinance pipeline confirmed to return adjusted close via auto_adjust=True; no separate Adj Close column; columns are lowercase {open, high, low, close, volume}; DATA-01 validated at unit level</done>
</task>

</tasks>

<verification>
cd /home/mqz/workspace/trading && python -m pytest tests/unit/test_edgartools_client.py tests/unit/test_data_ingest_vos.py tests/unit/test_quality_checker.py tests/unit/test_yfinance_adapter.py -x -v
cd /home/mqz/workspace/trading && python -m pytest tests/ -x --timeout=60
</verification>

<success_criteria>
- edgartools_client.py line 154 stores actual ticker symbol, not filing_date
- Ticker("005930") succeeds; Ticker("AAPL") still succeeds
- Symbol("005930") succeeds; Symbol("AAPL") still succeeds
- MarketType enum with US and KR values exists in data_ingest domain
- QualityChecker staleness uses business days -- Friday data on Monday passes
- yfinance fetch_ohlcv returns {open, high, low, close, volume} with no Adj Close column (DATA-01)
- edgartools handles small-cap filings with sparse/missing XBRL gracefully (DATA-02)
- All existing tests still pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/06-live-data-pipeline-korean-data/06-01-SUMMARY.md`
</output>
