---
phase: 05-tech-debt-infrastructure-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/infrastructure/sync_event_bus.py
  - src/shared/infrastructure/db_factory.py
  - src/shared/infrastructure/__init__.py
  - src/bootstrap.py
  - tests/unit/test_sync_event_bus.py
  - tests/unit/test_db_factory.py
  - tests/unit/test_bootstrap.py
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03

must_haves:
  truths:
    - "SyncEventBus routes published events to subscribed handlers synchronously"
    - "DBFactory returns consistent DuckDB connections and SQLite paths from a single data directory"
    - "bootstrap() returns a context dict with pre-wired handlers, event bus, and db_factory"
    - "bootstrap() accepts an optional db_factory parameter for test injection"
  artifacts:
    - path: "src/shared/infrastructure/sync_event_bus.py"
      provides: "Synchronous event bus for CLI context"
      exports: ["SyncEventBus"]
      min_lines: 20
    - path: "src/shared/infrastructure/db_factory.py"
      provides: "Centralized database path and connection management"
      exports: ["DBFactory"]
      min_lines: 25
    - path: "src/bootstrap.py"
      provides: "Composition Root wiring all bounded contexts"
      exports: ["bootstrap"]
      min_lines: 40
  key_links:
    - from: "src/bootstrap.py"
      to: "src/shared/infrastructure/sync_event_bus.py"
      via: "import and instantiation"
      pattern: "SyncEventBus\\(\\)"
    - from: "src/bootstrap.py"
      to: "src/shared/infrastructure/db_factory.py"
      via: "import and instantiation"
      pattern: "DBFactory\\(\\)"
    - from: "src/bootstrap.py"
      to: "src/scoring/application/handlers.py"
      via: "handler creation with injected dependencies"
      pattern: "ScoreSymbolHandler\\("
---

<objective>
Build the three infrastructure primitives that the rest of Phase 5 depends on: SyncEventBus, DBFactory, and the Composition Root (bootstrap).

Purpose: These three pieces replace the God Orchestrator pattern and centralize all dependency wiring. Every subsequent plan (CLI commands, event publishing, screener fix) will consume these primitives.

Output: `src/shared/infrastructure/sync_event_bus.py`, `src/shared/infrastructure/db_factory.py`, `src/bootstrap.py` with passing unit tests.
</objective>

<execution_context>
@/home/mqz/.claude/get-shit-done/workflows/execute-plan.md
@/home/mqz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tech-debt-infrastructure-foundation/05-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/shared/domain/__init__.py:
```python
class Entity: ...
class ValueObject: ...
class DomainEvent: ...
class Ok: ...
class Err: ...
class Result: ...
```

From src/shared/infrastructure/event_bus.py (AsyncEventBus -- reference for SyncEventBus API):
```python
class AsyncEventBus:
    def __init__(self) -> None:
        self._handlers: dict[str, list[Callable]] = defaultdict(list)

    def subscribe(self, event_type: type[DomainEvent], handler: Callable) -> None: ...
    async def publish(self, event: DomainEvent) -> None: ...
```

From src/scoring/application/handlers.py:
```python
class ScoreSymbolHandler:
    def __init__(self, score_repo: IScoreRepository, fundamental_client=None, technical_client=None, sentiment_client=None): ...
    def handle(self, cmd: ScoreSymbolCommand) -> Result: ...
```

From src/scoring/domain/__init__.py:
```python
# Exports: Symbol, FundamentalScore, TechnicalScore, SentimentScore, SafetyGate,
#          CompositeScore, STRATEGY_WEIGHTS, DEFAULT_STRATEGY, ScoreUpdatedEvent,
#          CompositeScoringService, SafetyFilterService, IScoreRepository
```

Existing handlers to wire in bootstrap:
- src/scoring/application/handlers.py: ScoreSymbolHandler
- src/signals/application/handlers.py: GenerateSignalHandler
- src/regime/application/handlers.py: DetectRegimeHandler
- src/portfolio/application/handlers.py: PortfolioManagerHandler
- src/backtest/application/handlers.py: BacktestHandler
- src/execution/application/handlers.py: TradePlanHandler
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SyncEventBus and DBFactory with tests</name>
  <files>
    src/shared/infrastructure/sync_event_bus.py,
    src/shared/infrastructure/db_factory.py,
    src/shared/infrastructure/__init__.py,
    tests/unit/test_sync_event_bus.py,
    tests/unit/test_db_factory.py
  </files>
  <behavior>
    - SyncEventBus: subscribe registers handler for event type, publish calls all handlers for that event type
    - SyncEventBus: publish with no subscribers does not raise
    - SyncEventBus: multiple handlers for same event type all get called
    - SyncEventBus: different event types route to different handlers
    - DBFactory: duckdb_conn() returns a DuckDBPyConnection from the configured data_dir
    - DBFactory: duckdb_conn() returns the same connection on repeated calls (singleton per factory)
    - DBFactory: sqlite_path("scoring") returns "{data_dir}/scoring.db"
    - DBFactory: close() closes the DuckDB connection and allows re-creation on next call
    - DBFactory: data directory is created if it does not exist
  </behavior>
  <action>
    1. Create `src/shared/infrastructure/sync_event_bus.py`:
       - Class `SyncEventBus` with same API as `AsyncEventBus` but synchronous.
       - `subscribe(event_type: type[DomainEvent], handler: Callable) -> None` -- registers handler keyed by event class name.
       - `publish(event: DomainEvent) -> None` -- calls all registered handlers for that event type. No async, no await.
       - Use `defaultdict(list)` for handler storage, same as AsyncEventBus.
       - Import `DomainEvent` from `src.shared.domain`.

    2. Create `src/shared/infrastructure/db_factory.py`:
       - Class `DBFactory` with `__init__(self, data_dir: str = "data")`.
       - `duckdb_conn()` returns a lazily-created `duckdb.DuckDBPyConnection` from `{data_dir}/analytics.duckdb`.
       - `sqlite_path(name: str)` returns `{data_dir}/{name}.db` as a string.
       - `close()` closes the DuckDB connection if open.
       - `os.makedirs(data_dir, exist_ok=True)` in `__init__`.

    3. Update `src/shared/infrastructure/__init__.py` to export `SyncEventBus`, `DBFactory`, and existing `AsyncEventBus`.

    4. Write tests FIRST (RED), then implement (GREEN):
       - `tests/unit/test_sync_event_bus.py`: Mirror the 6 test patterns from `tests/unit/test_event_bus.py` but synchronous. Test subscribe/publish routing, no-subscriber publish, multi-handler, multi-event-type.
       - `tests/unit/test_db_factory.py`: Test duckdb_conn returns connection, sqlite_path returns correct path, close lifecycle, data_dir creation. Use `tmp_path` fixture for isolation.
  </action>
  <verify>
    <automated>cd /home/mqz/workspace/trading && python -m pytest tests/unit/test_sync_event_bus.py tests/unit/test_db_factory.py -x -v</automated>
  </verify>
  <done>SyncEventBus passes all 4+ tests (subscribe, publish, no-subscriber, multi-handler). DBFactory passes all 4+ tests (connection, path, close, mkdir). Both classes exported from shared/infrastructure/__init__.py.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Composition Root (bootstrap) with tests</name>
  <files>
    src/bootstrap.py,
    tests/unit/test_bootstrap.py
  </files>
  <behavior>
    - bootstrap() returns a dict containing at minimum: "bus" (SyncEventBus), "db_factory" (DBFactory), "score_handler" (ScoreSymbolHandler)
    - bootstrap(db_factory=custom_factory) uses the provided factory instead of creating a new one
    - bootstrap() wires ScoreSymbolHandler with a SqliteScoreRepository using db_factory.sqlite_path("scoring")
    - bootstrap() creates event subscriptions (initially commented out / minimal -- just structure, no side effects per RESEARCH pitfall 3)
    - Calling bootstrap() twice returns independent contexts (no module-level singletons)
  </behavior>
  <action>
    1. Create `src/bootstrap.py` with a `bootstrap()` function:
       - Accepts optional `db_factory: DBFactory | None` parameter (None = create default).
       - Creates `SyncEventBus` instance.
       - Creates repository instances using `db_factory.sqlite_path(...)` for paths.
       - Creates handler instances with injected repos.
       - Per RESEARCH pitfall 3: Define event subscriptions in comments showing WHERE they will be wired (e.g., `# bus.subscribe(ScoreUpdatedEvent, signal_handler.on_score_updated)`) but do NOT activate them yet. Phase 6+ will enable cross-context event flows.
       - Returns dict: `{"bus": bus, "db_factory": db_factory, "score_handler": score_handler, ...}`.
       - This is the ONLY file that imports from all bounded contexts. All other files import from their own context only.

    2. Write tests FIRST:
       - `tests/unit/test_bootstrap.py`:
         - Test that `bootstrap()` returns dict with required keys ("bus", "db_factory", "score_handler").
         - Test that `bootstrap(db_factory=custom)` uses the custom factory.
         - Test that `bootstrap()` creates a SyncEventBus (not AsyncEventBus).
         - Test that score_handler is a ScoreSymbolHandler instance.
         - Use `tmp_path` to create a test DBFactory pointing at temp directory.

    3. Per RESEARCH open question 3 (lazy init): The bootstrap function should create handlers eagerly for now (the CLI has 11 commands but startup cost is negligible for a CLI tool). Lazy initialization can be added in Phase 6+ if API server startup becomes a concern.
  </action>
  <verify>
    <automated>cd /home/mqz/workspace/trading && python -m pytest tests/unit/test_bootstrap.py -x -v</automated>
  </verify>
  <done>bootstrap() returns a properly wired context dict. Custom db_factory injection works. All handlers are real instances with injected dependencies. Tests pass.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `python -m pytest tests/unit/test_sync_event_bus.py tests/unit/test_db_factory.py tests/unit/test_bootstrap.py -x -v` -- all pass
2. `python -m pytest tests/unit/ -x -q` -- existing tests unbroken
3. `ruff check src/shared/infrastructure/sync_event_bus.py src/shared/infrastructure/db_factory.py src/bootstrap.py` -- no lint errors
</verification>

<success_criteria>
- SyncEventBus is a working synchronous event bus with subscribe/publish
- DBFactory centralizes all database paths and connections
- bootstrap() returns a fully wired context dict with handlers and event bus
- All new code has passing tests
- Existing 352+ tests remain green
</success_criteria>

<output>
After completion, create `.planning/phases/05-tech-debt-infrastructure-foundation/05-01-SUMMARY.md`
</output>
