---
phase: 24-real-time-and-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/src/hooks/use-sse.ts
  - dashboard/src/app/providers.tsx
autonomous: true
requirements: [RT-01]

must_haves:
  truths:
    - "SSE hook maps 5 backend event types to the correct TanStack Query key invalidations"
    - "A single EventSource connection persists across page navigation (provider-level)"
    - "EventSource is cleaned up on unmount (no leaked connections)"
  artifacts:
    - path: "dashboard/src/hooks/use-sse.ts"
      provides: "useSSE hook with EventSource connection and query invalidation mapping"
      exports: ["useSSE"]
    - path: "dashboard/src/app/providers.tsx"
      provides: "SSEListener component mounted inside QueryClientProvider"
      contains: "SSEListener"
  key_links:
    - from: "dashboard/src/hooks/use-sse.ts"
      to: "/api/v1/dashboard/events"
      via: "new EventSource('/api/v1/dashboard/events')"
      pattern: "new EventSource"
    - from: "dashboard/src/hooks/use-sse.ts"
      to: "@tanstack/react-query"
      via: "queryClient.invalidateQueries"
      pattern: "invalidateQueries"
    - from: "dashboard/src/app/providers.tsx"
      to: "dashboard/src/hooks/use-sse.ts"
      via: "SSEListener component calling useSSE()"
      pattern: "useSSE"
---

<objective>
Wire SSE events from the FastAPI backend to React dashboard so domain events (OrderFilled, DrawdownAlert, RegimeChanged, PipelineCompleted, PipelineHalted) trigger automatic TanStack Query cache invalidation and UI updates without page refresh.

Purpose: Complete the real-time feedback loop -- when the trading system executes orders, detects regime changes, or completes pipeline runs, the dashboard reflects changes immediately.
Output: `use-sse.ts` hook + `SSEListener` in Providers
</objective>

<execution_context>
@/home/mqz/.claude/get-shit-done/workflows/execute-plan.md
@/home/mqz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-real-time-and-integration/24-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly -- no codebase exploration needed. -->

From dashboard/src/app/providers.tsx:
```typescript
'use client';
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
import { ThemeProvider } from 'next-themes';
import { useState } from 'react';

export function Providers({ children }: { children: React.ReactNode }) {
  const [queryClient] = useState(
    () => new QueryClient({
      defaultOptions: {
        queries: { staleTime: 30_000, refetchOnWindowFocus: true },
      },
    }),
  );

  return (
    <QueryClientProvider client={queryClient}>
      <ThemeProvider attribute="class" defaultTheme="dark" enableSystem={false}>
        {children}
      </ThemeProvider>
    </QueryClientProvider>
  );
}
```

Existing query keys used by hooks:
- `['overview']` -- dashboard/src/hooks/use-overview.ts
- `['signals']` -- dashboard/src/hooks/use-signals.ts
- `['risk']` -- dashboard/src/hooks/use-risk.ts
- `['pipeline']` -- dashboard/src/hooks/use-pipeline.ts

Backend SSE event types (from api_routes.py via SSEBridge):
- `OrderFilledEvent` -- order execution completed
- `DrawdownAlertEvent` -- drawdown tier changed
- `RegimeChangedEvent` -- market regime transition
- `PipelineCompletedEvent` -- pipeline run finished
- `PipelineHaltedEvent` -- pipeline run halted/failed

Event-to-query mapping (from 24-RESEARCH.md):
| SSE Event Type | Query Keys to Invalidate |
|----------------|--------------------------|
| OrderFilledEvent | ['overview'] |
| DrawdownAlertEvent | ['risk'], ['overview'] |
| RegimeChangedEvent | ['risk'], ['overview'], ['signals'] |
| PipelineCompletedEvent | ['pipeline'], ['overview'] |
| PipelineHaltedEvent | ['pipeline'], ['overview'] |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useSSE hook with EventSource connection and event-to-query mapping</name>
  <files>dashboard/src/hooks/use-sse.ts</files>
  <action>
Create `dashboard/src/hooks/use-sse.ts` with a `useSSE` custom hook that:

1. Defines an `EVENT_QUERY_MAP` constant mapping SSE event type strings to arrays of query key arrays:
   - `OrderFilledEvent` -> `[['overview']]`
   - `DrawdownAlertEvent` -> `[['risk'], ['overview']]`
   - `RegimeChangedEvent` -> `[['risk'], ['overview'], ['signals']]`
   - `PipelineCompletedEvent` -> `[['pipeline'], ['overview']]`
   - `PipelineHaltedEvent` -> `[['pipeline'], ['overview']]`

2. Uses `useQueryClient()` from TanStack Query to get the shared query client.

3. In a `useEffect`:
   - Creates `new EventSource('/api/v1/dashboard/events')`
   - Iterates over `EVENT_QUERY_MAP` entries and calls `es.addEventListener(eventType, handler)` for each event type
   - Each handler calls `queryClient.invalidateQueries({ queryKey: key })` for each query key in the mapping
   - Returns cleanup function that calls `es.close()`
   - Dependency array: `[queryClient]`

Do NOT:
- Add manual reconnection logic (EventSource auto-reconnects per SSE spec)
- Use `es.onmessage` (backend sends named events, not generic messages)
- Parse event data payloads (invalidation only needs the event type, not the data)
- Add any npm dependencies (use browser-native EventSource API)
  </action>
  <verify>
    <automated>cd /home/mqz/workspace/trading/dashboard && npx tsc --noEmit && npx biome check .</automated>
  </verify>
  <done>use-sse.ts exports useSSE hook. TypeScript compiles with zero errors. Biome lint passes.</done>
</task>

<task type="auto">
  <name>Task 2: Mount SSEListener in Providers for app-wide SSE connection</name>
  <files>dashboard/src/app/providers.tsx</files>
  <action>
Modify `dashboard/src/app/providers.tsx` to mount the SSE hook at the provider level:

1. Add import: `import { useSSE } from '@/hooks/use-sse';`

2. Create a small `SSEListener` component inside the file (not exported, internal only):
   ```
   function SSEListener() {
     useSSE();
     return null;
   }
   ```
   This is needed because `useSSE` calls `useQueryClient()` which requires `QueryClientProvider` as an ancestor. The hook cannot be called directly in `Providers` because `QueryClientProvider` wraps the children.

3. Place `<SSEListener />` as a child of `QueryClientProvider`, before `ThemeProvider`:
   ```
   <QueryClientProvider client={queryClient}>
     <SSEListener />
     <ThemeProvider ...>
       {children}
     </ThemeProvider>
   </QueryClientProvider>
   ```

Do NOT:
- Create a separate file for SSEListener (it is a trivial 4-line component, keep it in providers.tsx)
- Place SSEListener outside QueryClientProvider (it needs useQueryClient access)
- Add SSEListener per-page (one connection at provider level covers all pages)
  </action>
  <verify>
    <automated>cd /home/mqz/workspace/trading/dashboard && npx tsc --noEmit && npx biome check .</automated>
  </verify>
  <done>Providers.tsx renders SSEListener inside QueryClientProvider. TypeScript compiles. Biome passes. A single EventSource connection will be established when the app loads and persist across page navigation.</done>
</task>

</tasks>

<verification>
1. TypeScript: `cd dashboard && npx tsc --noEmit` -- zero errors
2. Lint: `cd dashboard && npx biome check .` -- zero errors
3. Manual: Start FastAPI (`python -m src.dashboard.presentation.app`) + Next.js (`cd dashboard && npm run dev`), open browser DevTools Network tab, confirm EventSource connection to `/api/v1/dashboard/events` is established
</verification>

<success_criteria>
- use-sse.ts exists with exported useSSE hook mapping 5 event types to query invalidations
- providers.tsx contains SSEListener component calling useSSE() inside QueryClientProvider
- TypeScript compiles with zero errors
- Biome lint passes with zero errors
- Single EventSource connection at provider level covers all dashboard pages
</success_criteria>

<output>
After completion, create `.planning/phases/24-real-time-and-integration/24-01-SUMMARY.md`
</output>
