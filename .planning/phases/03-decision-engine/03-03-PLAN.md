---
phase: 03-decision-engine
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - src/backtest/__init__.py
  - src/backtest/domain/__init__.py
  - src/backtest/domain/value_objects.py
  - src/backtest/domain/services.py
  - src/backtest/domain/events.py
  - src/backtest/domain/repositories.py
  - src/backtest/application/__init__.py
  - src/backtest/application/commands.py
  - src/backtest/application/handlers.py
  - src/backtest/infrastructure/__init__.py
  - src/backtest/infrastructure/core_backtest_adapter.py
  - src/backtest/infrastructure/duckdb_backtest_store.py
  - src/backtest/DOMAIN.md
  - tests/unit/test_backtest_validation.py
autonomous: true
requirements: [BACK-01, BACK-02]

must_haves:
  truths:
    - "Walk-forward validation produces IS and OOS metrics across N folds with overfitting score"
    - "Performance report includes Sharpe ratio, max drawdown, win rate, and profit factor"
    - "Backtest results are persisted to DuckDB for historical comparison"
    - "Profit factor is computed as gross_profit / abs(gross_loss) without modifying core/backtest/metrics"
  artifacts:
    - path: "src/backtest/domain/value_objects.py"
      provides: "BacktestConfig, PerformanceReport (with profit_factor), WalkForwardConfig VOs"
      contains: "class PerformanceReport"
    - path: "src/backtest/domain/services.py"
      provides: "BacktestValidationService with profit_factor computation"
      exports: ["BacktestValidationService"]
    - path: "src/backtest/infrastructure/core_backtest_adapter.py"
      provides: "Adapter wrapping core/backtest/ engine, walk_forward, and metrics"
      exports: ["CoreBacktestAdapter"]
    - path: "src/backtest/infrastructure/duckdb_backtest_store.py"
      provides: "DuckDB persistence for backtest results"
      exports: ["DuckDBBacktestStore"]
    - path: "tests/unit/test_backtest_validation.py"
      provides: "Tests for walk-forward validation and performance report (BACK-01, BACK-02)"
  key_links:
    - from: "src/backtest/infrastructure/core_backtest_adapter.py"
      to: "core/backtest/engine.py"
      via: "Direct call to run_backtest()"
      pattern: "from core\\.backtest\\.engine import run_backtest"
    - from: "src/backtest/infrastructure/core_backtest_adapter.py"
      to: "core/backtest/walk_forward.py"
      via: "Direct call to run_walk_forward()"
      pattern: "from core\\.backtest\\.walk_forward import run_walk_forward"
    - from: "src/backtest/application/handlers.py"
      to: "src/backtest/infrastructure/core_backtest_adapter.py"
      via: "DI injection of CoreBacktestAdapter"
      pattern: "CoreBacktestAdapter"
    - from: "src/backtest/domain/services.py"
      to: "PerformanceReport VO"
      via: "compute_profit_factor() enriches metrics from core"
      pattern: "profit_factor"
---

<objective>
Create the backtest bounded context (new DDD context) with walk-forward validation, performance reporting with profit factor, and DuckDB persistence.

Purpose: Enable strategy validation through walk-forward backtesting that produces IS/OOS metrics (Sharpe, max drawdown, win rate, profit factor) and an overfitting score. All math delegates to core/backtest/ through an adapter; only profit factor is new domain logic.

Output: Complete src/backtest/ bounded context (domain + application + infrastructure), CoreBacktestAdapter wrapping core/backtest/, DuckDBBacktestStore for result persistence, DOMAIN.md, and full test coverage for BACK-01 and BACK-02.
</objective>

<execution_context>
@/home/mqz/.claude/get-shit-done/workflows/execute-plan.md
@/home/mqz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-decision-engine/03-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From core/backtest/engine.py:
```python
@dataclass
class BacktestResult:
    symbol: str
    start_date: str
    end_date: str
    initial_capital: float
    final_capital: float
    metrics: PerformanceMetrics
    equity_curve: list[float]
    trade_log: list[dict]
    signals: list[str]

def run_backtest(symbol, ohlcv_df, signals_series, initial_capital=100_000.0) -> BacktestResult: ...
```

From core/backtest/metrics.py:
```python
@dataclass
class PerformanceMetrics:
    cagr: float
    sharpe_ratio: float
    max_drawdown: float  # negative, e.g., -0.25
    win_rate: float      # 0-1
    total_return: float
    num_trades: int
    avg_return_per_trade: float

def compute_metrics(equity_curve: pd.Series, trade_returns: list[float]) -> PerformanceMetrics: ...
```

From core/backtest/walk_forward.py:
```python
@dataclass
class WalkForwardResult:
    n_splits: int
    splits: list[dict]       # Each has is_metrics, oos_metrics
    oos_metrics: PerformanceMetrics  # Averaged OOS
    is_metrics: PerformanceMetrics   # Averaged IS
    overfitting_score: float  # IS Sharpe - OOS Sharpe

def run_walk_forward(ohlcv_df, signals_series, n_splits=5, train_ratio=0.7,
                     initial_capital=100_000.0, symbol="WF") -> WalkForwardResult: ...
```

From src/shared/domain/__init__.py:
```python
class Entity, ValueObject, DomainEvent, Ok, Err, Result
```

Established DDD patterns from Phase 1-2:
- Frozen dataclass VOs with _validate()
- DomainEvent with @dataclass(frozen=True, kw_only=True)
- Repository ABC with abstractmethod
- DuckDB store with DI connection, _ensure_table(), INSERT OR REPLACE
- Adapter: thin wrapper, no math rewriting
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backtest domain layer + CoreBacktestAdapter + tests</name>
  <files>
    src/backtest/__init__.py,
    src/backtest/domain/__init__.py,
    src/backtest/domain/value_objects.py,
    src/backtest/domain/services.py,
    src/backtest/domain/events.py,
    src/backtest/domain/repositories.py,
    src/backtest/infrastructure/__init__.py,
    src/backtest/infrastructure/core_backtest_adapter.py,
    src/backtest/DOMAIN.md,
    tests/unit/test_backtest_validation.py
  </files>
  <behavior>
    - Test: BacktestConfig VO validates n_splits >= 2, 0 < train_ratio < 1, initial_capital > 0
    - Test: PerformanceReport VO includes all 8 fields: cagr, sharpe_ratio, max_drawdown, win_rate, total_return, num_trades, avg_return_per_trade, profit_factor
    - Test: BacktestValidationService.compute_profit_factor([0.10, -0.05, 0.08, -0.03]) returns sum(positives)/abs(sum(negatives)) = 0.18/0.08 = 2.25
    - Test: compute_profit_factor with no losses returns inf (if any profit) or 0.0 (if no profit)
    - Test: compute_profit_factor with no profits returns 0.0
    - Test: BacktestValidationService.enrich_metrics() adds profit_factor to core PerformanceMetrics output
    - Test: CoreBacktestAdapter.run_single() delegates to core/backtest/engine.run_backtest() and returns result
    - Test: CoreBacktestAdapter.run_walk_forward() delegates to core/backtest/walk_forward.run_walk_forward() and returns result
    - Test: WalkForwardConfig VO validates same as BacktestConfig plus valid symbol
  </behavior>
  <action>
    1. Create `src/backtest/` directory structure:
       - `src/backtest/__init__.py` (empty)
       - `src/backtest/domain/__init__.py` (public API exports)
       - `src/backtest/application/__init__.py` (empty for now, Task 2 fills)
       - `src/backtest/infrastructure/__init__.py` (exports)

    2. Create `src/backtest/domain/value_objects.py`:
       - `BacktestConfig` (frozen dataclass VO): `symbol: str`, `initial_capital: float = 100_000.0`. Validates symbol non-empty, initial_capital > 0.
       - `WalkForwardConfig` (frozen dataclass VO): `symbol: str`, `n_splits: int = 5`, `train_ratio: float = 0.7`, `initial_capital: float = 100_000.0`. Validates n_splits >= 2, 0 < train_ratio < 1, initial_capital > 0, symbol non-empty.
       - `PerformanceReport` (frozen dataclass VO): `cagr: float`, `sharpe_ratio: float`, `max_drawdown: float`, `win_rate: float`, `total_return: float`, `num_trades: int`, `avg_return_per_trade: float`, `profit_factor: float`. No validation needed (computed values).

    3. Create `src/backtest/domain/events.py`:
       - `BacktestCompletedEvent(DomainEvent)`: `symbol: str`, `sharpe_ratio: float`, `profit_factor: float`, `max_drawdown: float`. Uses `@dataclass(frozen=True, kw_only=True)` pattern.

    4. Create `src/backtest/domain/repositories.py`:
       - `IBacktestResultRepository(ABC)`: `save(symbol, config, report) -> None`, `find_latest(symbol) -> dict | None`, `find_all() -> list[dict]`.

    5. Create `src/backtest/domain/services.py`:
       - `BacktestValidationService` with:
         - `compute_profit_factor(trade_returns: list[float]) -> float`: gross_profit = sum(r for r > 0), gross_loss = sum(r for r < 0), return gross_profit / abs(gross_loss). Handle edge cases: no losses -> inf if any profit else 0.0, no profits -> 0.0.
         - `enrich_metrics(core_metrics: dict, trade_returns: list[float]) -> PerformanceReport`: Takes the 7-field dict from core PerformanceMetrics, computes profit_factor, returns PerformanceReport VO with all 8 fields.

    6. Create `src/backtest/infrastructure/core_backtest_adapter.py`:
       - `CoreBacktestAdapter`:
         - `run_single(symbol, ohlcv_df, signals_series, initial_capital) -> dict`: Calls `core.backtest.engine.run_backtest()`, returns BacktestResult as dict (converting dataclass fields).
         - `run_walk_forward(ohlcv_df, signals_series, n_splits, train_ratio, initial_capital, symbol) -> dict`: Calls `core.backtest.walk_forward.run_walk_forward()`, returns WalkForwardResult as dict.
         - `extract_trade_returns(backtest_result_dict) -> list[float]`: Extracts trade returns from trade_log entries.

    7. Create `src/backtest/DOMAIN.md` per DDD rules:
       - Responsibility: Walk-forward backtesting and strategy validation
       - Core entities: BacktestConfig, WalkForwardConfig, PerformanceReport
       - External dependencies: signals context (via events), scoring context (via events)
       - Key use cases: Run backtest, run walk-forward validation, generate performance report
       - Invariant rules: Profit factor computed in adapter (never modify core/backtest/metrics), walk-forward minimum 2 splits

    8. Update `src/backtest/domain/__init__.py` to export all VOs, services, events, repos.
    9. Update `src/backtest/infrastructure/__init__.py` to export `CoreBacktestAdapter`.

    10. Write tests in `tests/unit/test_backtest_validation.py`:
        - Test VO validations (BacktestConfig, WalkForwardConfig).
        - Test compute_profit_factor with various trade return lists.
        - Test enrich_metrics produces PerformanceReport with correct profit_factor.
        - Test CoreBacktestAdapter.run_single() with synthetic OHLCV DataFrame and signals Series (10 rows of price data + BUY/HOLD/SELL signals). Verify result has expected keys (symbol, metrics, equity_curve, trade_log).
        - Test CoreBacktestAdapter.run_walk_forward() with synthetic data, verify result has n_splits, oos_metrics, is_metrics, overfitting_score.
  </action>
  <verify>
    <automated>cd /home/mqz/workspace/trading && python -m pytest tests/unit/test_backtest_validation.py -x -v</automated>
  </verify>
  <done>Backtest domain layer with VOs, services, events, repos created. CoreBacktestAdapter wraps core/backtest/. Profit factor computation works. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Backtest application handlers + DuckDB store</name>
  <files>
    src/backtest/application/__init__.py,
    src/backtest/application/commands.py,
    src/backtest/application/handlers.py,
    src/backtest/infrastructure/duckdb_backtest_store.py,
    src/backtest/infrastructure/__init__.py,
    src/backtest/domain/__init__.py,
    tests/unit/test_backtest_validation.py
  </files>
  <behavior>
    - Test: RunBacktestCommand validates symbol non-empty
    - Test: RunWalkForwardCommand validates n_splits >= 2
    - Test: BacktestHandler.run_backtest() returns Ok with PerformanceReport including profit_factor
    - Test: BacktestHandler.run_walk_forward() returns Ok with IS/OOS PerformanceReports and overfitting_score
    - Test: BacktestHandler emits BacktestCompletedEvent after successful run
    - Test: DuckDBBacktestStore.save() persists backtest result with symbol and config
    - Test: DuckDBBacktestStore.find_latest() returns most recent result for a symbol
    - Test: DuckDBBacktestStore.find_all() returns all stored results
  </behavior>
  <action>
    1. Create `src/backtest/application/commands.py`:
       - `RunBacktestCommand` (frozen dataclass): `symbol: str`, `initial_capital: float = 100_000.0`.
       - `RunWalkForwardCommand` (frozen dataclass): `symbol: str`, `n_splits: int = 5`, `train_ratio: float = 0.7`, `initial_capital: float = 100_000.0`.
       - Both commands also accept `ohlcv_df` and `signals_series` as fields (passed from orchestration layer). Use `field(repr=False)` for DataFrames to avoid verbose repr.

    2. Create `src/backtest/application/handlers.py`:
       - `BacktestHandler`:
         - Constructor accepts `CoreBacktestAdapter`, `IBacktestResultRepository`, `BacktestValidationService`.
         - `run_backtest(cmd: RunBacktestCommand) -> Result`:
           - Calls `adapter.run_single(cmd.symbol, cmd.ohlcv_df, cmd.signals_series, cmd.initial_capital)`.
           - Extracts trade_returns via `adapter.extract_trade_returns(result)`.
           - Calls `validation_svc.enrich_metrics(result["metrics"], trade_returns)` to get PerformanceReport with profit_factor.
           - Saves to repo if available.
           - Returns Ok with symbol, performance_report (as dict), equity_curve summary (start, end, length).
         - `run_walk_forward(cmd: RunWalkForwardCommand) -> Result`:
           - Calls `adapter.run_walk_forward(cmd.ohlcv_df, cmd.signals_series, cmd.n_splits, cmd.train_ratio, cmd.initial_capital, cmd.symbol)`.
           - Enriches both IS and OOS metrics with profit_factor (extract trade returns from each split's backtest).
           - Computes overfitting_score from result.
           - Saves to repo if available.
           - Returns Ok with symbol, n_splits, oos_report (PerformanceReport dict), is_report (PerformanceReport dict), overfitting_score.

    3. Create `src/backtest/infrastructure/duckdb_backtest_store.py`:
       - `DuckDBBacktestStore` implementing `IBacktestResultRepository`.
       - Constructor accepts `duckdb.DuckDBPyConnection` via DI.
       - `_ensure_table()`: `CREATE TABLE IF NOT EXISTS backtest_results (id INTEGER PRIMARY KEY, symbol VARCHAR, config JSON, report JSON, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP)`. Use `CREATE SEQUENCE IF NOT EXISTS backtest_id_seq` for auto-increment.
       - `save(symbol, config, report)`: INSERT with next sequence value.
       - `find_latest(symbol)`: SELECT ... WHERE symbol = ? ORDER BY created_at DESC LIMIT 1.
       - `find_all()`: SELECT * ORDER BY created_at DESC.

    4. Update `src/backtest/infrastructure/__init__.py` to export `DuckDBBacktestStore`.
    5. Update `src/backtest/application/__init__.py` with exports.

    6. Extend `tests/unit/test_backtest_validation.py` (append to existing file from Task 1):
       - Test RunBacktestCommand and RunWalkForwardCommand creation.
       - Test BacktestHandler.run_backtest() with CoreBacktestAdapter (using synthetic data): verify Ok result contains profit_factor.
       - Test BacktestHandler.run_walk_forward() with CoreBacktestAdapter (using synthetic data): verify Ok result contains oos_report and is_report both with profit_factor, and overfitting_score.
       - Test DuckDBBacktestStore with :memory: connection: save, find_latest, find_all.
  </action>
  <verify>
    <automated>cd /home/mqz/workspace/trading && python -m pytest tests/unit/test_backtest_validation.py -x -v</automated>
  </verify>
  <done>BacktestHandler orchestrates adapter calls and enriches with profit_factor. DuckDB store persists results. Walk-forward returns IS/OOS reports with overfitting score. All tests pass.</done>
</task>

</tasks>

<verification>
cd /home/mqz/workspace/trading && python -m pytest tests/unit/test_backtest_validation.py -v
cd /home/mqz/workspace/trading && python -m pytest tests/ -x --tb=short
</verification>

<success_criteria>
- Walk-forward validation produces IS/OOS metrics across N folds with overfitting score
- Performance report includes all 8 metrics: cagr, sharpe_ratio, max_drawdown, win_rate, total_return, num_trades, avg_return_per_trade, profit_factor
- Profit factor correctly computed as gross_profit / abs(gross_loss) without modifying core/backtest/metrics.py
- Backtest results persisted to DuckDB for historical comparison
- Complete DDD bounded context: domain (VOs, services, events, repos), application (commands, handlers), infrastructure (adapter, store)
- All new tests pass; all existing tests remain green
</success_criteria>

<output>
After completion, create `.planning/phases/03-decision-engine/03-03-SUMMARY.md`
</output>
