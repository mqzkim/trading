---
phase: 20-ci-test-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/__init__.py
  - src/regime/__init__.py
  - src/scoring/__init__.py
  - src/signals/__init__.py
  - src/shared/__init__.py
  - src/execution/infrastructure/order_monitor.py
  - src/execution/infrastructure/safe_adapter.py
  - src/execution/infrastructure/trading_stream.py
  - src/regime/application/handlers.py
  - src/signals/application/handlers.py
  - src/pipeline/domain/services.py
  - src/bootstrap.py
  - core/scoring/technical.py
  - src/scoring/domain/services.py
  - src/backtest/domain/services.py
  - src/portfolio/domain/value_objects.py
  - src/regime/domain/entities.py
  - src/regime/infrastructure/sqlite_repo.py
autonomous: true
requirements:
  - CI-01

must_haves:
  truths:
    - "mypy src/ exits with zero errors"
    - "ruff check src/ exits with zero errors"
    - "pytest tests/ passes without regressions from type fixes"
  artifacts:
    - path: "pyproject.toml"
      provides: "mypy config with explicit_package_bases = true"
      contains: "explicit_package_bases = true"
    - path: "src/__init__.py"
      provides: "Package marker for mypy module resolution"
    - path: "src/regime/__init__.py"
      provides: "Package marker for mypy module resolution"
    - path: "src/scoring/__init__.py"
      provides: "Package marker for mypy module resolution"
    - path: "src/signals/__init__.py"
      provides: "Package marker for mypy module resolution"
    - path: "src/shared/__init__.py"
      provides: "Package marker for mypy module resolution"
  key_links:
    - from: "pyproject.toml"
      to: "mypy"
      via: "explicit_package_bases config"
      pattern: "explicit_package_bases = true"
    - from: "src/bootstrap.py"
      to: "approval handler"
      via: "RegimeType.value passed as str"
      pattern: "event\\.new_regime\\.value"
---

<objective>
Fix all mypy type errors and ruff lint errors so `mypy src/` and `ruff check src/` exit with zero errors.

Purpose: Clean CI pipeline -- mypy and ruff are the project's static analysis gates. Currently 17 mypy errors and 4 ruff errors block clean CI runs.
Output: Zero-error mypy and ruff runs; all existing tests still pass.
</objective>

<execution_context>
@/home/mqz/.claude/get-shit-done/workflows/execute-plan.md
@/home/mqz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-ci-test-debt-cleanup/20-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix mypy config and missing __init__.py files, resolve all 17 type errors</name>
  <files>
    pyproject.toml,
    src/__init__.py, src/regime/__init__.py, src/scoring/__init__.py, src/signals/__init__.py, src/shared/__init__.py,
    src/execution/infrastructure/order_monitor.py,
    src/execution/infrastructure/safe_adapter.py,
    src/execution/infrastructure/trading_stream.py,
    src/regime/application/handlers.py,
    src/signals/application/handlers.py,
    src/pipeline/domain/services.py,
    src/bootstrap.py,
    core/scoring/technical.py,
    src/scoring/domain/services.py
  </files>
  <action>
    **Step 1 -- mypy config fix (BLOCKER):**
    Add `explicit_package_bases = true` to `[tool.mypy]` in `pyproject.toml`. This resolves the "Duplicate module named application" error that prevents mypy from running at all.

    **Step 2 -- Create missing `__init__.py` files:**
    Create empty `__init__.py` files in: `src/`, `src/regime/`, `src/scoring/`, `src/signals/`, `src/shared/`. These are namespace package markers needed for proper module resolution.

    **Step 3 -- Fix Category B errors (`object` typed params, 9 errors):**
    In these files, change `object` type annotations to `Any` (from `typing import Any`). Using `Any` instead of `Protocol` is the lower-risk quick fix -- it preserves runtime behavior and avoids import dependency issues. The research suggested `Protocol` as ideal but `Any` as faster/lower-risk.
    - `src/execution/infrastructure/order_monitor.py`: Change `client: object` to `client: Any`, `notifier: object | None` to `notifier: Any | None`, `bus: object | None` to `bus: Any | None`
    - `src/execution/infrastructure/safe_adapter.py`: Change `notifier: object | None` to `notifier: Any | None`
    - `src/execution/infrastructure/trading_stream.py`: Change `bus: object` to `bus: Any`
    - `src/regime/application/handlers.py`: Change `data_client: object | None` to `data_client: Any | None`

    **Step 4 -- Fix Category C errors (type narrowing, 3 errors):**
    - `src/signals/application/handlers.py:81`: Where `dict | None` is assigned to `dict` variable, add `or {}` default: `regime_weights = self._regime_weights or {}`
    - `src/pipeline/domain/services.py:403`: Add `if budget_repo is None: raise RuntimeError("budget_repo not configured")` guard before `.get_or_create_today()` call
    - `src/pipeline/domain/services.py:462`: Same pattern -- add None guard before `.save()` call

    **Step 5 -- Fix Category D errors (specific type mismatches, 2 errors):**
    - `src/bootstrap.py:228`: Change `event.new_regime` to `event.new_regime.value` to pass `str` instead of `RegimeType` enum (the method already documents "Accepts regime as string")
    - `core/scoring/technical.py:42`: Change variable type annotation from `int` to `float` (or remove the annotation and let inference work)

    **Step 6 -- Fix Category F errors (type:ignore comment mismatches, 2 errors):**
    - `src/scoring/domain/services.py:85`: Verify the `# type: ignore[union-attr]` comment -- if mypy now reports `attr-defined`, change to `# type: ignore[attr-defined]`
    - `src/regime/application/handlers.py:53`: Same pattern -- update error code in type:ignore comment if needed

    **Step 7 -- Run mypy to verify:**
    Run `mypy src/` and confirm zero errors. If any errors remain, fix them following the same category patterns.

    IMPORTANT: After each file change, do NOT run full test suite yet. Just verify with mypy. Run tests only at the end to confirm no regressions.
  </action>
  <verify>
    <automated>cd /home/mqz/workspace/trading && mypy src/</automated>
  </verify>
  <done>
    `mypy src/` exits with "Success: no issues found" (zero errors). All changes are additive type annotation fixes -- no behavioral changes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix ruff lint errors and run full test suite regression check</name>
  <files>
    src/backtest/domain/services.py,
    src/portfolio/domain/value_objects.py,
    src/regime/domain/entities.py,
    src/regime/infrastructure/sqlite_repo.py
  </files>
  <action>
    **Step 1 -- Fix 4 ruff lint errors:**
    - `src/backtest/domain/services.py:8`: Remove unused `import math` (F401)
    - `src/regime/domain/entities.py:6`: Remove unused `from typing import Optional` (F401)
    - `src/regime/infrastructure/sqlite_repo.py:7`: Remove unused `from datetime import timezone` import (F401)
    - `src/portfolio/domain/value_objects.py:99`: Add `# noqa: E402` to the module-level import that's not at top (this is an intentional re-export, not a code smell)

    Can also run `ruff check src/ --fix` for the 3 auto-fixable F401 errors, then manually handle the E402.

    **Step 2 -- Verify ruff clean:**
    Run `ruff check src/` and confirm zero errors.

    **Step 3 -- Run full test suite regression check:**
    Run `pytest tests/ -q --ignore=tests/unit/test_api_routes.py` to verify none of the type annotation changes from Task 1 broke existing tests. Ignore test_api_routes.py since it is stale and will be fixed in Plan 02.

    If any test failures appear that are caused by the type annotation changes, fix them. Type annotation changes (object -> Any, adding None guards) should NOT change runtime behavior.
  </action>
  <verify>
    <automated>cd /home/mqz/workspace/trading && ruff check src/ && pytest tests/ -q --ignore=tests/unit/test_api_routes.py</automated>
  </verify>
  <done>
    `ruff check src/` exits with zero errors. `pytest tests/ -q --ignore=tests/unit/test_api_routes.py` passes all tests. No regressions from Task 1 type fixes.
  </done>
</task>

</tasks>

<verification>
1. `mypy src/` -- zero errors
2. `ruff check src/` -- zero errors
3. `pytest tests/ -q --ignore=tests/unit/test_api_routes.py` -- all pass (no regressions)
</verification>

<success_criteria>
- mypy exits with "Success: no issues found"
- ruff check exits with zero errors
- All existing tests (excluding stale test_api_routes.py) continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/20-ci-test-debt-cleanup/20-01-SUMMARY.md`
</output>
