---
phase: 25-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dashboard/presentation/routes.py
  - src/dashboard/presentation/charts.py
  - src/dashboard/presentation/templates/
  - src/dashboard/presentation/app.py
  - src/dashboard/application/queries.py
  - src/dashboard/presentation/api_routes.py
  - tests/unit/test_dashboard_web.py
  - tests/unit/test_dashboard_charts.py
  - pyproject.toml
autonomous: true
requirements: [CLNP-01, CLNP-02]

must_haves:
  truths:
    - "No HTMX/Jinja2 template files exist in the codebase"
    - "No Python code imports plotly"
    - "plotly and jinja2 are not listed in pyproject.toml dependencies"
    - "The FastAPI JSON API and SSE endpoints still work correctly"
    - "The DDD layer violation (application importing presentation) is fixed"
  artifacts:
    - path: "src/dashboard/presentation/app.py"
      provides: "FastAPI app factory mounting only api_router"
      contains: "api_router"
    - path: "src/dashboard/application/queries.py"
      provides: "RiskQueryHandler without Plotly chart generation"
    - path: "src/dashboard/presentation/api_routes.py"
      provides: "JSON API routes without dead .pop() calls"
    - path: "pyproject.toml"
      provides: "Dependencies without plotly or jinja2"
  key_links:
    - from: "src/dashboard/presentation/app.py"
      to: "src/dashboard/presentation/api_routes.py"
      via: "app.include_router(api_router)"
      pattern: "include_router\\(api_router\\)"
    - from: "src/dashboard/presentation/api_routes.py"
      to: "src/dashboard/application/queries.py"
      via: "QueryHandler imports"
      pattern: "from src\\.dashboard\\.application\\.queries import"
---

<objective>
Remove all legacy HTMX/Jinja2 dashboard code and Plotly dependency, leaving only the React dashboard's FastAPI JSON API backend.

Purpose: The React dashboard (Phases 21-24) fully replaces the HTMX/Jinja2 dashboard. The legacy code is dead weight and contains a DDD layer violation (application/ importing from presentation/).
Output: Clean codebase with 18 files deleted, 4 files modified, 2 dependencies removed.
</objective>

<execution_context>
@/home/mqz/.claude/get-shit-done/workflows/execute-plan.md
@/home/mqz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-cleanup/25-RESEARCH.md
@.planning/phases/25-cleanup/25-VALIDATION.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From src/dashboard/presentation/app.py (lines 14-15, 83-84):
```python
from src.dashboard.presentation.api_routes import api_router
from src.dashboard.presentation.routes import router  # DELETE this
# ...
app.include_router(router)      # DELETE this line (line 83)
app.include_router(api_router)  # KEEP this line (line 84)
```

From src/dashboard/application/queries.py (line 9):
```python
from src.dashboard.presentation.charts import build_drawdown_gauge, build_sector_donut  # DELETE
```

From src/dashboard/application/queries.py RiskQueryHandler.handle() (lines 493-507):
```python
# DELETE lines 493-497:
gauge_json = build_drawdown_gauge(drawdown_pct)
donut_json = build_sector_donut(
    sector_weights if sector_weights else {"No positions": 100}
)
# DELETE from return dict (lines 506-507):
"gauge_json": gauge_json,
"donut_json": donut_json,
```

From src/dashboard/application/queries.py docstring (line 446):
```python
# UPDATE docstring to remove gauge_json, donut_json references
```

From src/dashboard/presentation/api_routes.py (lines 63-64, 84-86):
```python
# DELETE line 64:
data.pop("equity_curve_chart_json", None)
# DELETE lines 85-86:
data.pop("gauge_json", None)
data.pop("donut_json", None)
# UPDATE comments on lines 63, 84 (remove Plotly/HTMX references)
```

From pyproject.toml (lines 27-28):
```toml
"plotly>=6.0",   # DELETE
"jinja2>=3.1",   # DELETE
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete legacy files and update source code</name>
  <files>
    src/dashboard/presentation/templates/ (DELETE entire directory)
    src/dashboard/presentation/routes.py (DELETE)
    src/dashboard/presentation/charts.py (DELETE)
    tests/unit/test_dashboard_web.py (DELETE)
    tests/unit/test_dashboard_charts.py (DELETE)
    src/dashboard/presentation/app.py (MODIFY)
    src/dashboard/application/queries.py (MODIFY)
    src/dashboard/presentation/api_routes.py (MODIFY)
  </files>
  <action>
    Follow this exact dependency order for safe removal:

    1. DELETE the entire templates directory:
       `rm -rf src/dashboard/presentation/templates/`
       (13 HTML files: base.html, overview.html, signals.html, risk.html, pipeline.html, and 8 partials)

    2. DELETE routes.py:
       `rm src/dashboard/presentation/routes.py`
       (342 lines - all HTMX/Jinja2 routes + SSE HTML partial renderer)

    3. DELETE charts.py:
       `rm src/dashboard/presentation/charts.py`
       (139 lines - Plotly chart builders: build_equity_curve, build_drawdown_gauge, build_sector_donut)

    4. DELETE test files for removed code:
       `rm tests/unit/test_dashboard_web.py`  (30 tests for HTMX routes)
       `rm tests/unit/test_dashboard_charts.py`  (5 tests for Plotly charts)

    5. MODIFY app.py -- remove 2 lines:
       - Remove line 15: `from src.dashboard.presentation.routes import router`
       - Remove line 83: `app.include_router(router)`
       - Update module docstring: remove "dashboard routes" reference, say "JSON API routes"
       - KEEP: `from src.dashboard.presentation.api_routes import api_router` (line 14)
       - KEEP: `app.include_router(api_router)` (line 84)

    6. MODIFY queries.py -- remove Plotly dependency (also fixes DDD violation):
       - Remove line 9: `from src.dashboard.presentation.charts import build_drawdown_gauge, build_sector_donut`
       - Remove lines 493-497 in RiskQueryHandler.handle(): the `gauge_json = build_drawdown_gauge(...)` and `donut_json = build_sector_donut(...)` calls
       - Remove lines 506-507 from the return dict: `"gauge_json": gauge_json,` and `"donut_json": donut_json,`
       - Update the handle() docstring (line 446) to remove gauge_json and donut_json from the keys list

    7. MODIFY api_routes.py -- remove dead .pop() calls and stale references:
       - Remove line 63 comment and line 64: `data.pop("equity_curve_chart_json", None)`
       - Remove line 84 comment and lines 85-86: `data.pop("gauge_json", None)` and `data.pop("donut_json", None)`
       - Update module docstring (lines 1-9): remove references to "HTMX routes", "no HTML partials", "Plotly chart data". It should describe the JSON API as the primary dashboard API, not as an alternative to HTMX.
       - Update api_risk docstring (line 80): remove "(no Plotly chart data)"
  </action>
  <verify>
    <automated>cd /home/mqz/workspace/trading && python3 -c "from src.dashboard.presentation.app import create_dashboard_app; print('app import OK')" && python3 -c "from src.dashboard.application.queries import RiskQueryHandler; print('queries import OK')" && python3 -c "from pathlib import Path; assert not Path('src/dashboard/presentation/templates').exists(); print('templates deleted OK')" && python3 -c "assert not __import__('pathlib').Path('src/dashboard/presentation/routes.py').exists(); print('routes.py deleted OK')" && python3 -c "assert not __import__('pathlib').Path('src/dashboard/presentation/charts.py').exists(); print('charts.py deleted OK')" && ! grep -r "import plotly" src/ && echo "no plotly imports OK"</automated>
  </verify>
  <done>All 18 legacy files deleted (13 templates, routes.py, charts.py, 2 test files). app.py mounts only api_router. queries.py has no presentation layer imports. api_routes.py has no dead .pop() calls. No Python code in src/ imports plotly.</done>
</task>

<task type="auto">
  <name>Task 2: Remove dependencies and run full verification</name>
  <files>
    pyproject.toml (MODIFY)
  </files>
  <action>
    1. MODIFY pyproject.toml:
       - Remove line 27: `"plotly>=6.0",`
       - Remove line 28: `"jinja2>=3.1",`
       - NOTE: jinja2 will remain installed as a transitive dependency of edgartools. This is expected.

    2. Reinstall the project to reflect dependency changes:
       `pip install -e .`

    3. Run the full verification suite in order:
       a. `pytest tests/unit/test_dashboard_json_api.py tests/unit/test_dashboard_sse.py -x` (remaining dashboard tests pass)
       b. `pytest tests/ -x` (full test suite passes -- expect ~35 fewer tests than before)
       c. `mypy src/` (type check passes)
       d. `ruff check src/` (lint passes)

    4. Verify final state with smoke checks:
       - `python3 -c "t=open('pyproject.toml').read(); assert 'plotly' not in t; assert 'jinja2' not in t; print('deps removed OK')"`
       - `! grep -r "import plotly" src/` (returns nothing)
       - `! grep -r "from src.dashboard.presentation.charts" src/` (returns nothing)
       - `! grep -r "from src.dashboard.presentation.routes" src/` (returns nothing)
  </action>
  <verify>
    <automated>cd /home/mqz/workspace/trading && pytest tests/ -x && mypy src/ && ruff check src/</automated>
  </verify>
  <done>plotly and jinja2 removed from pyproject.toml. Full test suite passes (with expected reduction of ~35 tests from deleted test files). mypy and ruff report zero errors. No stale references to deleted modules anywhere in src/.</done>
</task>

</tasks>

<verification>
Phase-level verification after all tasks complete:

1. CLNP-01 checks:
   - `python3 -c "from pathlib import Path; assert not Path('src/dashboard/presentation/templates').exists()"`
   - `python3 -c "assert not __import__('pathlib').Path('src/dashboard/presentation/routes.py').exists()"`
   - `python3 -c "import inspect; from src.dashboard.presentation.app import create_dashboard_app; src=inspect.getsource(create_dashboard_app); assert 'routes' not in src.replace('api_routes','')"`

2. CLNP-02 checks:
   - `python3 -c "t=open('pyproject.toml').read(); assert 'plotly' not in t"`
   - `! grep -r "import plotly" src/`
   - `python3 -c "assert not __import__('pathlib').Path('src/dashboard/presentation/charts.py').exists()"`

3. Regression checks:
   - `pytest tests/unit/test_dashboard_json_api.py -x` (JSON API tests pass)
   - `pytest tests/unit/test_dashboard_sse.py -x` (SSE tests pass)
   - `pytest tests/ -x` (full suite passes)
   - `mypy src/` (zero errors)
   - `ruff check src/` (zero errors)
</verification>

<success_criteria>
- Zero HTMX template files exist in the codebase
- Zero Python files import plotly
- plotly and jinja2 are not in pyproject.toml dependencies
- The DDD violation in queries.py (application importing presentation) is fixed
- All remaining tests pass (JSON API + SSE + full suite)
- Type checking and linting pass with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/25-cleanup/25-01-SUMMARY.md`
</output>
