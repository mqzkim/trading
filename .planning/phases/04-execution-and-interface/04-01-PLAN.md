---
phase: 04-execution-and-interface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/execution/__init__.py
  - src/execution/domain/__init__.py
  - src/execution/domain/value_objects.py
  - src/execution/domain/events.py
  - src/execution/domain/services.py
  - src/execution/domain/repositories.py
  - src/execution/infrastructure/__init__.py
  - src/execution/infrastructure/alpaca_adapter.py
  - src/execution/infrastructure/sqlite_trade_plan_repo.py
  - src/execution/DOMAIN.md
  - tests/unit/test_trade_plan.py
  - tests/unit/test_alpaca_adapter.py
  - pyproject.toml
autonomous: true
requirements:
  - EXEC-01
  - EXEC-03
  - EXEC-04

must_haves:
  truths:
    - "TradePlan VO holds entry/stop/target/size/reasoning with validation"
    - "BracketSpec VO specifies bracket order parameters for Alpaca"
    - "AlpacaExecutionAdapter submits bracket orders in mock mode without credentials"
    - "AlpacaExecutionAdapter submits bracket orders via alpaca-py when credentials present"
    - "Trade plans persist to SQLite with status tracking"
  artifacts:
    - path: "src/execution/domain/value_objects.py"
      provides: "TradePlan, BracketSpec, OrderResult, TradePlanStatus VOs"
      contains: "class TradePlan"
    - path: "src/execution/domain/events.py"
      provides: "TradePlanCreatedEvent, OrderExecutedEvent, OrderFailedEvent"
      contains: "class TradePlanCreatedEvent"
    - path: "src/execution/domain/services.py"
      provides: "TradePlanService (pure plan generation from scoring+risk data)"
      contains: "class TradePlanService"
    - path: "src/execution/domain/repositories.py"
      provides: "ITradePlanRepository ABC"
      contains: "class ITradePlanRepository"
    - path: "src/execution/infrastructure/alpaca_adapter.py"
      provides: "AlpacaExecutionAdapter with mock fallback and bracket orders"
      contains: "class AlpacaExecutionAdapter"
    - path: "src/execution/infrastructure/sqlite_trade_plan_repo.py"
      provides: "SQLite-backed trade plan persistence"
      contains: "class SqliteTradePlanRepository"
    - path: "tests/unit/test_trade_plan.py"
      provides: "Unit tests for TradePlan VO, BracketSpec, TradePlanService"
    - path: "tests/unit/test_alpaca_adapter.py"
      provides: "Unit tests for AlpacaExecutionAdapter mock mode + bracket orders"
  key_links:
    - from: "src/execution/domain/value_objects.py"
      to: "src/shared/domain"
      via: "inherits ValueObject base class"
      pattern: "from src.shared.domain import ValueObject"
    - from: "src/execution/domain/services.py"
      to: "personal/execution/planner.py"
      via: "delegates to plan_entry() without math rewriting"
      pattern: "from personal.execution.planner import plan_entry"
    - from: "src/execution/infrastructure/alpaca_adapter.py"
      to: "src/execution/domain/value_objects.py"
      via: "accepts BracketSpec, returns OrderResult"
      pattern: "BracketSpec.*OrderResult"
---

<objective>
Create the execution bounded context with DDD-compliant domain layer and infrastructure.

Purpose: Establish the trade plan and order execution foundation that the CLI approval workflow (Plan 03) will build on. This is the domain model + Alpaca SDK integration -- the business logic core of Phase 4.

Output: `src/execution/` bounded context with domain VOs (TradePlan, BracketSpec, OrderResult), domain events, TradePlanService delegating to existing `personal/execution/planner.py`, AlpacaExecutionAdapter wrapping `alpaca-py` with mock fallback, SQLite trade plan persistence.
</objective>

<execution_context>
@/home/mqz/.claude/get-shit-done/workflows/execute-plan.md
@/home/mqz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-execution-and-interface/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/shared/domain/__init__.py:
```python
from .base_entity import Entity
from .base_value_object import ValueObject
from .domain_event import DomainEvent
from .result import Ok, Err, Result
```

From src/portfolio/domain/value_objects.py (patterns to follow):
```python
@dataclass(frozen=True)
class ATRStop(ValueObject):
    entry_price: float
    atr: float
    multiplier: float = 2.5
    @property
    def stop_price(self) -> float: ...
    def _validate(self) -> None: ...

@dataclass(frozen=True)
class TakeProfitLevels(ValueObject):
    entry_price: float
    intrinsic_value: float
    @property
    def levels(self) -> list[dict]: ...
```

From personal/execution/planner.py:
```python
def plan_entry(symbol, entry_price, atr, capital, peak_value, current_value,
               sector_exposure=0.0, cooldown_days=0, order_type="LIMIT",
               atr_multiplier=3.0) -> dict:
    # Returns: {"symbol", "status", "order_type", "entry_price", "limit_price",
    #           "shares", "stop_price", "stop_distance", "position_value",
    #           "position_pct", "risk_pct", "atr_multiplier", "drawdown_level"}
```

From personal/execution/paper_trading.py (legacy -- being replaced by alpaca-py):
```python
class PaperTradingClient:
    def submit_order(self, symbol, qty, side, order_type="market", limit_price=None) -> Order: ...
    def get_positions(self) -> list[Position]: ...
    def get_portfolio_value(self) -> float: ...
    def get_account(self) -> dict: ...
```

From src/portfolio/domain/events.py:
```python
@dataclass(frozen=True)
class PositionOpenedEvent(DomainEvent): ...
@dataclass(frozen=True)
class PositionClosedEvent(DomainEvent): ...
@dataclass(frozen=True)
class DrawdownAlertEvent(DomainEvent): ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Execution domain layer (VOs, events, services, repository interface)</name>
  <files>
    src/execution/__init__.py,
    src/execution/domain/__init__.py,
    src/execution/domain/value_objects.py,
    src/execution/domain/events.py,
    src/execution/domain/services.py,
    src/execution/domain/repositories.py,
    src/execution/DOMAIN.md,
    tests/unit/test_trade_plan.py
  </files>
  <behavior>
    - TradePlan VO: frozen dataclass with symbol, direction("BUY"|"SELL"), entry_price(>0), stop_loss_price(<entry for BUY), take_profit_price(>entry for BUY), quantity(>0), position_value(>0), reasoning_trace(str), composite_score(float), margin_of_safety(float), signal_direction(str). Validation raises ValueError on invalid fields.
    - TradePlanStatus enum: PENDING, APPROVED, REJECTED, MODIFIED, EXECUTED, FAILED
    - BracketSpec VO: frozen dataclass with symbol, quantity(>0), entry_price(>0), stop_loss_price, take_profit_price. Validation ensures stop < entry < take_profit for BUY.
    - OrderResult VO: frozen dataclass with order_id(str), status(str), symbol, quantity, filled_price(Optional[float]), error_message(Optional[str])
    - TradePlanCreatedEvent, OrderExecutedEvent, OrderFailedEvent domain events
    - TradePlanService.generate_plan() delegates to personal/execution/planner.plan_entry(), converts dict result to TradePlan VO. Returns None if planner returns status=REJECTED.
    - ITradePlanRepository ABC: save(plan, status), find_pending() -> list, find_by_symbol(symbol) -> Optional, update_status(symbol, new_status)
  </behavior>
  <action>
    1. Create `src/execution/` directory with `__init__.py` and `src/execution/domain/` with `__init__.py`.

    2. Create `src/execution/domain/value_objects.py`:
       - `TradePlanStatus` enum (PENDING, APPROVED, REJECTED, MODIFIED, EXECUTED, FAILED)
       - `TradePlan(ValueObject)` frozen dataclass: symbol, direction, entry_price, stop_loss_price, take_profit_price, quantity, position_value, reasoning_trace, composite_score, margin_of_safety, signal_direction. `_validate()` checks entry_price>0, quantity>0, stop_loss_price<entry_price for BUY direction, take_profit_price>entry_price for BUY direction.
       - `BracketSpec(ValueObject)` frozen dataclass: symbol, quantity, entry_price, stop_loss_price, take_profit_price. `_validate()` checks positivity and ordering.
       - `OrderResult(ValueObject)` frozen dataclass: order_id, status, symbol, quantity, filled_price(Optional[float]=None), error_message(Optional[str]=None). No special validation needed.
       Follow the exact pattern from ATRStop/TakeProfitLevels -- `@dataclass(frozen=True)` inheriting `ValueObject`.

    3. Create `src/execution/domain/events.py`:
       - `TradePlanCreatedEvent(DomainEvent)`: symbol, direction, entry_price, quantity
       - `OrderExecutedEvent(DomainEvent)`: order_id, symbol, quantity, filled_price
       - `OrderFailedEvent(DomainEvent)`: symbol, error_message
       Follow exact pattern from portfolio/domain/events.py -- `@dataclass(frozen=True)` with default values.

    4. Create `src/execution/domain/services.py`:
       - `TradePlanService` class with `generate_plan()` method.
       - Delegates to `personal.execution.planner.plan_entry()` for the risk-checked sizing.
       - Accepts: symbol, entry_price, atr, capital, peak_value, current_value, intrinsic_value, composite_score, margin_of_safety, signal_direction, reasoning_trace, sector_exposure, atr_multiplier.
       - If planner returns status="REJECTED", return None.
       - If status="APPROVED", construct TradePlan VO from planner result dict + the extra fields (reasoning_trace, composite_score, margin_of_safety, signal_direction). Use TakeProfitLevels VO from portfolio domain to compute take_profit_price (first level price if available, else entry_price * 1.1 as fallback).

    5. Create `src/execution/domain/repositories.py`:
       - `ITradePlanRepository(ABC)`: save(plan: TradePlan, status: TradePlanStatus), find_pending() -> list[dict], find_by_symbol(symbol: str) -> Optional[dict], update_status(symbol: str, new_status: TradePlanStatus)

    6. Create `src/execution/domain/__init__.py` exposing: TradePlan, TradePlanStatus, BracketSpec, OrderResult, TradePlanCreatedEvent, OrderExecutedEvent, OrderFailedEvent, TradePlanService, ITradePlanRepository.

    7. Create `src/execution/DOMAIN.md` per DDD rules.

    8. Write tests in `tests/unit/test_trade_plan.py`:
       - Test TradePlan creation with valid fields
       - Test TradePlan validation rejects entry_price<=0, quantity<=0, stop>=entry for BUY
       - Test BracketSpec creation and validation
       - Test OrderResult creation
       - Test TradePlanService.generate_plan() with mocked planner (approved path)
       - Test TradePlanService.generate_plan() returns None for rejected path
  </action>
  <verify>
    <automated>cd /home/mqz/workspace/trading && python -m pytest tests/unit/test_trade_plan.py -x --tb=short</automated>
  </verify>
  <done>TradePlan/BracketSpec/OrderResult VOs validate correctly. TradePlanService delegates to planner and converts results. All tests pass. EXEC-01 domain layer complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Alpaca adapter + SQLite trade plan repo + alpaca-py dependency</name>
  <files>
    src/execution/infrastructure/__init__.py,
    src/execution/infrastructure/alpaca_adapter.py,
    src/execution/infrastructure/sqlite_trade_plan_repo.py,
    tests/unit/test_alpaca_adapter.py,
    pyproject.toml
  </files>
  <behavior>
    - AlpacaExecutionAdapter in mock mode: submit_bracket_order(BracketSpec) returns OrderResult with MOCK- prefixed order_id and status="filled"
    - AlpacaExecutionAdapter in mock mode: get_positions() returns list of position dicts, get_account() returns account dict
    - AlpacaExecutionAdapter real mode: constructs MarketOrderRequest with OrderClass.BRACKET, TakeProfitRequest, StopLossRequest, TimeInForce.DAY
    - SqliteTradePlanRepository: save persists TradePlan+status to SQLite, find_pending returns all PENDING plans, update_status changes status
    - alpaca-py>=0.43 added to pyproject.toml dependencies
  </behavior>
  <action>
    1. Add `alpaca-py>=0.43` to dependencies in `pyproject.toml` and run `pip install alpaca-py>=0.43`.

    2. Create `src/execution/infrastructure/__init__.py` exposing AlpacaExecutionAdapter, SqliteTradePlanRepository.

    3. Create `src/execution/infrastructure/alpaca_adapter.py`:
       - `AlpacaExecutionAdapter` class.
       - Constructor accepts `api_key: str | None = None, secret_key: str | None = None`. Sets `_use_mock = not (api_key and secret_key)`. If NOT mock, lazily import `from alpaca.trading.client import TradingClient` and create `TradingClient(api_key, secret_key, paper=True)`.
       - CRITICAL: alpaca-py imports ONLY inside methods, never at module level (Pitfall 1 from research).
       - `submit_bracket_order(spec: BracketSpec) -> OrderResult`:
         - Mock path: return `OrderResult(order_id=f"MOCK-{spec.symbol}-{uuid4().hex[:8]}", status="filled", symbol=spec.symbol, quantity=spec.quantity, filled_price=spec.entry_price)`.
         - Real path: import `MarketOrderRequest, TakeProfitRequest, StopLossRequest` from `alpaca.trading.requests`, import `OrderSide, TimeInForce, OrderClass` from `alpaca.trading.enums`. Build `MarketOrderRequest` with `order_class=OrderClass.BRACKET`, `time_in_force=TimeInForce.DAY` (Pitfall 2). Submit and convert response to OrderResult.
       - `get_positions() -> list[dict]`: Mock returns empty list or mock positions. Real calls `self._client.get_all_positions()`.
       - `get_account() -> dict`: Mock returns `{"cash": 100000, "portfolio_value": 100000, "status": "ACTIVE"}`. Real calls `self._client.get_account()`.
       - Wrap real API calls in try/except, log error, fall back to mock on failure.

    4. Create `src/execution/infrastructure/sqlite_trade_plan_repo.py`:
       - `SqliteTradePlanRepository(ITradePlanRepository)`.
       - Constructor takes `db_path: str = "data/portfolio.db"` (same DB as positions -- research recommends this).
       - Creates `trade_plans` table: symbol TEXT PRIMARY KEY, direction TEXT, entry_price REAL, stop_loss_price REAL, take_profit_price REAL, quantity INTEGER, position_value REAL, reasoning_trace TEXT, composite_score REAL, margin_of_safety REAL, signal_direction TEXT, status TEXT, created_at TIMESTAMP, updated_at TIMESTAMP.
       - `save(plan, status)`: INSERT OR REPLACE.
       - `find_pending()`: SELECT WHERE status='PENDING'.
       - `find_by_symbol(symbol)`: SELECT WHERE symbol=?.
       - `update_status(symbol, new_status)`: UPDATE status WHERE symbol=?.
       Follow exact pattern from SqlitePositionRepository.

    5. Update `src/execution/infrastructure/__init__.py`.

    6. Write tests in `tests/unit/test_alpaca_adapter.py`:
       - Test mock mode bracket order returns valid OrderResult
       - Test mock mode get_positions returns list
       - Test mock mode get_account returns dict with expected keys
       - Test BracketSpec -> OrderResult field mapping in mock mode
       - Test SqliteTradePlanRepository save/find_pending/update_status round-trip (using :memory: or tmp path)
  </action>
  <verify>
    <automated>cd /home/mqz/workspace/trading && python -m pytest tests/unit/test_alpaca_adapter.py -x --tb=short</automated>
  </verify>
  <done>AlpacaExecutionAdapter submits bracket orders in mock mode. SqliteTradePlanRepository persists trade plans. alpaca-py is installed. EXEC-03 and EXEC-04 infrastructure complete.</done>
</task>

</tasks>

<verification>
cd /home/mqz/workspace/trading && python -m pytest tests/unit/test_trade_plan.py tests/unit/test_alpaca_adapter.py -x --tb=short
</verification>

<success_criteria>
- src/execution/ bounded context exists with domain/ and infrastructure/ layers
- TradePlan, BracketSpec, OrderResult VOs validate correctly
- TradePlanService delegates to personal/execution/planner.py without rewriting math
- AlpacaExecutionAdapter works in mock mode (no credentials needed)
- SQLite trade plan persistence round-trips correctly
- All new tests pass
- No domain layer imports from infrastructure (DDD compliance)
</success_criteria>

<output>
After completion, create `.planning/phases/04-execution-and-interface/04-01-SUMMARY.md`
</output>
