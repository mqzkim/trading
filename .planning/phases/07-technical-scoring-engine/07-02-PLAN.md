---
phase: 07-technical-scoring-engine
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/scoring/application/handlers.py
  - cli/main.py
  - tests/unit/test_cli_score_technical.py
autonomous: true
requirements: [TECH-04]

must_haves:
  truths:
    - "CLI score command displays 5 individual technical indicator scores with explanations"
    - "CLI score output shows technical composite alongside fundamental and sentiment"
    - "Scoring a ticker with strong fundamentals but bearish technicals produces visibly lower composite"
  artifacts:
    - path: "src/scoring/application/handlers.py"
      provides: "Handler creates TechnicalScore with full sub-score breakdown"
      contains: "TechnicalScoringService"
    - path: "cli/main.py"
      provides: "CLI score command with technical sub-score table"
      contains: "sub_scores"
    - path: "tests/unit/test_cli_score_technical.py"
      provides: "Tests for CLI technical sub-score output"
      min_lines: 30
  key_links:
    - from: "src/scoring/application/handlers.py"
      to: "src/scoring/domain/services.py (TechnicalScoringService)"
      via: "handler calls TechnicalScoringService.compute() instead of flat technical_score float"
      pattern: "TechnicalScoringService"
    - from: "src/scoring/application/handlers.py"
      to: "src/scoring/infrastructure/core_scoring_adapter.py"
      via: "adapter provides raw indicator values to handler"
      pattern: "compute_technical_subscores"
    - from: "cli/main.py"
      to: "src/scoring/application/handlers.py"
      via: "CLI calls handler, reads sub_scores from result dict"
      pattern: "technical_sub_scores|sub_scores"
---

<objective>
Wire TechnicalScoringService into the application handler and CLI score command so users see 5 individual technical indicator scores with plain-text explanations.

Purpose: TECH-04 (sub-score breakdown output) -- connects the domain layer built in Plan 01 to the user-facing CLI.
Output: Updated handler + CLI score display + tests
</objective>

<execution_context>
@/home/mqz/.claude/get-shit-done/workflows/execute-plan.md
@/home/mqz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-technical-scoring-engine/07-RESEARCH.md
@.planning/phases/07-technical-scoring-engine/07-01-SUMMARY.md

@src/scoring/application/handlers.py
@src/scoring/domain/services.py
@src/scoring/domain/value_objects.py
@src/scoring/infrastructure/core_scoring_adapter.py
@cli/main.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire TechnicalScoringService into Handler + CLI Sub-Score Display</name>
  <files>src/scoring/application/handlers.py, cli/main.py, tests/unit/test_cli_score_technical.py</files>
  <behavior>
    - Handler result dict includes "technical_sub_scores" key with list of {name, value, explanation, raw_value} dicts
    - Handler result "technical_score" still present (composite technical value) for backward compat
    - CLI score output includes a "Technical Indicators" sub-table with 5 rows (RSI, MACD, MA, ADX, OBV)
    - Each row shows: indicator name, score (0-100), explanation text
    - CLI still shows the main composite table (Fundamental / Technical / Sentiment / Composite)
  </behavior>
  <action>
1. In `src/scoring/application/handlers.py`:
   - Import `TechnicalScoringService` from domain.
   - In `ScoreSymbolHandler.__init__`, create `self._technical_scoring = TechnicalScoringService()`.
   - Update `_get_technical()`: after getting raw technical data, also call the infrastructure adapter's `compute_technical_subscores(symbol)` to get `TechnicalScore` with sub-scores. If adapter fails, fall back to current behavior (`TechnicalScore(value=X)` without sub-scores).
   - Alternative simpler approach: modify `_get_technical` to return a richer dict that includes per-indicator raw values. Then in `handle()`, use `TechnicalScoringService.compute(...)` directly with the raw values to get a `TechnicalScore` with sub-scores.
   - In the result dict, add:
     - `"technical_sub_scores"`: list of dicts from `technical.sub_scores` -- each `{"name": s.name, "value": s.value, "explanation": s.explanation, "raw_value": s.raw_value}`
   - Keep `"technical_score": technical.value` for backward compat.

2. In `cli/main.py` (score command):
   - After the main composite score table, add a "Technical Indicators" Rich Table.
   - Columns: Indicator | Score | Explanation
   - Iterate over `result.get("technical_sub_scores", [])` and add a row per indicator.
   - If no sub-scores available (backward compat), skip the sub-table.
   - Use Rich styling: green for score >= 60, yellow for 40-60, red for < 40.

3. Write `tests/unit/test_cli_score_technical.py` FIRST (RED), then implement (GREEN):
   - Test handler produces technical_sub_scores in result when TechnicalScoringService is wired.
   - Test handler result still has technical_score float.
   - Test CLI output contains indicator names (RSI, MACD, MA, ADX, OBV) when sub-scores present.
  </action>
  <verify>
    <automated>cd /home/mqz/workspace/trading && python -m pytest tests/unit/test_cli_score_technical.py tests/unit/test_technical_scoring_service.py tests/unit/test_scoring_composite_v2.py -x -v</automated>
  </verify>
  <done>CLI score command shows 5 technical sub-scores with explanations. Handler returns sub-score breakdown. All tests pass including existing scoring tests.</done>
</task>

</tasks>

<verification>
```bash
cd /home/mqz/workspace/trading
python -m pytest tests/unit/test_cli_score_technical.py tests/unit/test_technical_scoring_service.py tests/unit/test_scoring_composite_v2.py -x -v
python -m pytest tests/ -x --timeout=60
ruff check src/scoring/ cli/
mypy src/scoring/application/handlers.py --ignore-missing-imports
```
</verification>

<success_criteria>
- CLI `score AAPL` shows 5 individual technical indicator scores with explanations
- Technical composite score visible alongside fundamental and sentiment
- Handler result dict includes technical_sub_scores list
- Strong-fundamentals + bearish-technicals ticker produces visibly lower composite than aligned signals
- All tests pass, no regressions in existing scoring tests
</success_criteria>

<output>
After completion, create `.planning/phases/07-technical-scoring-engine/07-02-SUMMARY.md`
</output>
