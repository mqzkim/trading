---
phase: 07-technical-scoring-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scoring/domain/value_objects.py
  - src/scoring/domain/services.py
  - src/scoring/infrastructure/core_scoring_adapter.py
  - tests/unit/test_technical_scoring_service.py
  - tests/unit/test_scoring_composite_v2.py
autonomous: true
requirements: [TECH-01, TECH-02, TECH-03]

must_haves:
  truths:
    - "TechnicalScoringService produces 5 individual 0-100 scores from raw indicator values"
    - "Each indicator score includes a plain-text explanation string"
    - "Technical composite score (0-100) is computed from weighted 5 indicators"
    - "STRATEGY_WEIGHTS swing default is 40/40/20 (fundamental/technical/sentiment)"
    - "TechnicalScore(value=65.0) still works without sub-scores (backward compat)"
  artifacts:
    - path: "src/scoring/domain/value_objects.py"
      provides: "TechnicalIndicatorScore VO, extended TechnicalScore VO, updated STRATEGY_WEIGHTS"
      contains: "class TechnicalIndicatorScore"
    - path: "src/scoring/domain/services.py"
      provides: "TechnicalScoringService with compute() method"
      contains: "class TechnicalScoringService"
    - path: "src/scoring/infrastructure/core_scoring_adapter.py"
      provides: "Adapter bridging core/data/indicators.py to domain service"
      contains: "def compute_technical_subscores"
    - path: "tests/unit/test_technical_scoring_service.py"
      provides: "Unit tests for TechnicalScoringService"
      min_lines: 60
  key_links:
    - from: "src/scoring/domain/services.py (TechnicalScoringService)"
      to: "src/scoring/domain/value_objects.py (TechnicalIndicatorScore, TechnicalScore)"
      via: "compute() returns TechnicalScore with sub-scores"
      pattern: "TechnicalScore\\("
    - from: "src/scoring/infrastructure/core_scoring_adapter.py"
      to: "core/data/indicators.py (compute_all)"
      via: "adapter calls compute_all, extracts float values for domain service"
      pattern: "compute_all|indicators"
---

<objective>
Build the TechnicalScoringService domain layer: extend TechnicalScore VO with 5 sub-scores and explanations, create TechnicalScoringService that normalizes raw indicators to 0-100, update STRATEGY_WEIGHTS to 40/40/20, and bridge via infrastructure adapter.

Purpose: TECH-01 (5 indicators in DDD), TECH-02 (composite 0-100), TECH-03 (40/40/20 weights) -- the domain foundation that Plan 02 wires into the handler and CLI.
Output: Domain service + VO extension + adapter + tests
</objective>

<execution_context>
@/home/mqz/.claude/get-shit-done/workflows/execute-plan.md
@/home/mqz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-technical-scoring-engine/07-RESEARCH.md

@src/scoring/domain/value_objects.py
@src/scoring/domain/services.py
@src/scoring/domain/__init__.py
@src/scoring/infrastructure/core_scoring_adapter.py
@core/data/indicators.py
@core/scoring/technical.py
@src/shared/domain/__init__.py

<interfaces>
<!-- Key types the executor needs -->

From src/shared/domain/__init__.py:
```python
class ValueObject:
    def __post_init__(self) -> None:
        self._validate()
    def _validate(self) -> None: ...
```

From src/scoring/domain/value_objects.py:
```python
STRATEGY_WEIGHTS: dict[str, dict[str, float]] = {
    "swing":    {"fundamental": 0.35, "technical": 0.40, "sentiment": 0.25},
    "position": {"fundamental": 0.50, "technical": 0.30, "sentiment": 0.20},
}

@dataclass(frozen=True)
class TechnicalScore(ValueObject):
    value: float  # currently only field
```

From core/data/indicators.py:
```python
def compute_all(df: pd.DataFrame) -> dict:
    # Returns: ma50, ma200, rsi14, atr21, adx14, obv, macd, macd_signal, macd_histogram
```

From core/scoring/technical.py:
```python
def _norm(val: float, lo: float, hi: float) -> float:
    # Clamps val to [lo, hi] then maps to 0-100
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend TechnicalScore VO + Create TechnicalScoringService</name>
  <files>src/scoring/domain/value_objects.py, src/scoring/domain/services.py, src/scoring/domain/__init__.py, tests/unit/test_technical_scoring_service.py</files>
  <behavior>
    - TechnicalIndicatorScore("RSI", 65.0, "RSI at 65: bullish momentum", raw_value=65.0) creates valid VO
    - TechnicalIndicatorScore("RSI", -1.0, ...) raises ValueError (out of 0-100)
    - TechnicalScore(value=65.0) still works (backward compat, all sub-scores None)
    - TechnicalScore with all 5 sub-scores populated returns them via .sub_scores property
    - TechnicalScoringService.compute(rsi=65, macd_histogram=0.5, close=150, ma50=145, ma200=130, adx=25, obv_change_pct=5.0) returns TechnicalScore with value 0-100 and 5 populated sub-scores
    - TechnicalScoringService.compute with all None inputs returns TechnicalScore with value=50 (neutral) and explanations saying "insufficient data"
    - Each sub-score explanation is a non-empty string describing the indicator state
    - RSI 75 -> explanation contains "overbought"; RSI 25 -> "oversold"; RSI 50 -> "neutral"
    - MACD positive histogram -> bullish; negative -> bearish
    - close > ma50 > ma200 -> strong uptrend score; close < ma200 -> bearish
    - ADX > 25 -> strong trend; ADX < 20 -> weak/no trend
    - OBV change > 0 -> positive volume; < 0 -> negative volume
  </behavior>
  <action>
1. In `src/scoring/domain/value_objects.py`:
   - Add `TechnicalIndicatorScore` frozen dataclass VO with fields: name (str), value (float 0-100), explanation (str), raw_value (float|None = None). Validate value in 0-100.
   - Extend `TechnicalScore` with optional fields: rsi_score, macd_score, ma_score, adx_score, obv_score (all `TechnicalIndicatorScore | None = None`), and weights (dict[str,float] | None = None). Add `sub_scores` property returning non-None list.
   - ALL new fields default to None -- `TechnicalScore(value=X)` MUST still work.
   - Update `STRATEGY_WEIGHTS["swing"]` to `{"fundamental": 0.40, "technical": 0.40, "sentiment": 0.20}` per TECH-03 requirement. Keep position weights unchanged.

2. In `src/scoring/domain/services.py`:
   - Add `TECHNICAL_INDICATOR_WEIGHTS = {"rsi": 0.20, "macd": 0.20, "ma": 0.25, "adx": 0.15, "obv": 0.20}`.
   - Add `TechnicalScoringService` class with `compute(rsi, macd_histogram, close, ma50, ma200, adx, obv_change_pct)` method. All params are `float | None`.
   - Each indicator gets a private `_score_X` method returning `TechnicalIndicatorScore`. Use `_norm(val, lo, hi)` style normalization (inline, no pandas). Handle None/NaN -> neutral score 50 with "insufficient data" explanation.
   - RSI scoring: 0-100 direct (inverted -- RSI 30 is bullish opportunity = high score, RSI 70 is overbought = lower score). Use scoring logic from research: oversold (high score), neutral (mid), overbought (low score).
   - MACD: normalize histogram around 0 -- positive = bullish (higher score), negative = bearish (lower score).
   - MA: score based on close vs ma50 vs ma200 relationships. close > ma50 > ma200 = 100 (golden cross territory), close < ma200 < ma50 = 0 (death cross territory).
   - ADX: higher ADX = stronger trend = higher score. Normalize 0-50 range to 0-100.
   - OBV: score obv_change_pct. Positive change = bullish, negative = bearish. Normalize roughly -20% to +20% range.
   - Composite = weighted sum of 5 sub-scores using TECHNICAL_INDICATOR_WEIGHTS.

3. In `src/scoring/domain/__init__.py`: export TechnicalIndicatorScore, TechnicalScoringService.

4. Write tests in `tests/unit/test_technical_scoring_service.py` FIRST (RED phase), then implement (GREEN).
  </action>
  <verify>
    <automated>cd /home/mqz/workspace/trading && python -m pytest tests/unit/test_technical_scoring_service.py -x -v</automated>
  </verify>
  <done>TechnicalScoringService produces 5 sub-scores with explanations. TechnicalScore backward-compatible. STRATEGY_WEIGHTS swing = 40/40/20. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Infrastructure Adapter + Composite Weight Tests</name>
  <files>src/scoring/infrastructure/core_scoring_adapter.py, src/scoring/infrastructure/__init__.py, tests/unit/test_scoring_composite_v2.py</files>
  <action>
1. In `src/scoring/infrastructure/core_scoring_adapter.py`:
   - Add `compute_technical_subscores(symbol: str)` method (or new class `TechnicalIndicatorAdapter`) that:
     a. Calls `core.data.client.fetch_ohlcv(symbol)` to get OHLCV DataFrame
     b. Calls `core.data.indicators.compute_all(df)` to get raw indicators
     c. Extracts last (most recent) float value from each pandas Series using safe extraction (handle NaN)
     d. Calls `TechnicalScoringService().compute(rsi=..., macd_histogram=..., ...)` with extracted floats
     e. Returns the resulting `TechnicalScore` with full sub-score breakdown
   - This adapter is the ONLY place pandas touches the domain. Domain service receives only float|None.

2. Update `src/scoring/infrastructure/__init__.py` to export the new adapter.

3. Update `tests/unit/test_scoring_composite_v2.py`:
   - Add/update test asserting CompositeScore.compute with swing strategy uses 40/40/20 weights (TECH-03).
   - Verify that fundamental=80, technical=30, sentiment=50 with swing produces: 0.40*80 + 0.40*30 + 0.20*50 = 32+12+10 = 54.0 (not the old 35/40/25 weights).
   - Add test that TechnicalScore with sub-scores still produces correct composite via CompositeScoringService.

4. Run full scoring test suite to confirm no regressions.
  </action>
  <verify>
    <automated>cd /home/mqz/workspace/trading && python -m pytest tests/unit/test_technical_scoring_service.py tests/unit/test_scoring_composite_v2.py -x -v</automated>
  </verify>
  <done>Infrastructure adapter bridges core/data/indicators to domain TechnicalScoringService. Composite tests pass with 40/40/20 weights. No regressions in existing tests.</done>
</task>

</tasks>

<verification>
```bash
cd /home/mqz/workspace/trading
python -m pytest tests/unit/test_technical_scoring_service.py tests/unit/test_scoring_composite_v2.py -x -v
python -m pytest tests/ -x --timeout=60
ruff check src/scoring/
mypy src/scoring/domain/value_objects.py src/scoring/domain/services.py --ignore-missing-imports
```
</verification>

<success_criteria>
- TechnicalScoringService.compute() returns TechnicalScore with 5 sub-scores and explanations
- Each sub-score is 0-100 with non-empty explanation string
- None/NaN inputs produce neutral 50 scores with "insufficient data" explanation
- TechnicalScore(value=X) backward compat preserved
- STRATEGY_WEIGHTS["swing"] = 40/40/20
- Infrastructure adapter extracts floats from pandas, passes to domain service
- All unit tests pass, no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/07-technical-scoring-engine/07-01-SUMMARY.md`
</output>
