---
phase: 14-strategy-and-budget-approval
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/approval/__init__.py
  - src/approval/domain/__init__.py
  - src/approval/domain/entities.py
  - src/approval/domain/value_objects.py
  - src/approval/domain/services.py
  - src/approval/domain/repositories.py
  - src/approval/domain/events.py
  - src/approval/infrastructure/__init__.py
  - src/approval/infrastructure/sqlite_approval_repo.py
  - src/approval/DOMAIN.md
  - tests/unit/test_approval_domain.py
  - tests/unit/test_approval_gate.py
autonomous: true
requirements:
  - APPR-01
  - APPR-02

must_haves:
  truths:
    - "StrategyApproval entity validates score_threshold, allowed_regimes, max_per_trade_pct, expires_at, daily_budget_cap"
    - "ApprovalGateService rejects trades below score threshold, outside allowed regimes, exceeding position cap, or exceeding daily budget"
    - "Approval state persists in SQLite and survives process restarts"
    - "DailyBudget tracks spent/remaining per UTC date with automatic reset"
    - "suspended_reasons set tracks multiple suspension sources independently"
  artifacts:
    - path: "src/approval/domain/entities.py"
      provides: "StrategyApproval entity with is_effective, suspend, unsuspend, revoke"
      contains: "class StrategyApproval"
    - path: "src/approval/domain/services.py"
      provides: "ApprovalGateService.check() returns GateResult"
      contains: "class ApprovalGateService"
    - path: "src/approval/infrastructure/sqlite_approval_repo.py"
      provides: "SQLite persistence for approvals, daily budget, review queue"
      contains: "CREATE TABLE IF NOT EXISTS strategy_approvals"
    - path: "tests/unit/test_approval_domain.py"
      provides: "Entity and VO unit tests"
    - path: "tests/unit/test_approval_gate.py"
      provides: "Gate service unit tests"
  key_links:
    - from: "src/approval/domain/services.py"
      to: "src/approval/domain/entities.py"
      via: "ApprovalGateService checks StrategyApproval.is_effective"
      pattern: "approval\\.is_effective"
    - from: "src/approval/infrastructure/sqlite_approval_repo.py"
      to: "src/approval/domain/repositories.py"
      via: "implements IApprovalRepository ABC"
      pattern: "class SqliteApprovalRepository"
---

<objective>
Create the approval bounded context domain layer: StrategyApproval entity, DailyBudget value object, ApprovalGateService, repository interfaces, SQLite persistence, and unit tests.

Purpose: Establish the core domain model for strategy approval and budget tracking that Phase 14 Plan 02 will wire into the pipeline and CLI.
Output: `src/approval/` bounded context with domain + infrastructure layers, passing unit tests.
</objective>

<execution_context>
@/home/mqz/.claude/get-shit-done/workflows/execute-plan.md
@/home/mqz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-strategy-and-budget-approval/14-CONTEXT.md
@.planning/phases/14-strategy-and-budget-approval/14-RESEARCH.md

@src/shared/domain/__init__.py
@src/execution/domain/value_objects.py
@src/execution/infrastructure/sqlite_cooldown_repo.py
@src/pipeline/infrastructure/sqlite_pipeline_repo.py

<interfaces>
<!-- Key types and contracts the executor needs from existing codebase -->

From src/shared/domain/__init__.py:
```python
from .base_entity import Entity
from .base_value_object import ValueObject
from .domain_event import DomainEvent
from .result import Ok, Err, Result
```

CooldownState pattern from src/execution/domain/value_objects.py:
- frozen dataclass with optional id for persistence
- Expiry checked in Python not SQL (timezone safety)

SQLite repo pattern from src/execution/infrastructure/sqlite_cooldown_repo.py:
- CREATE TABLE IF NOT EXISTS
- WAL journal mode
- db_path constructor parameter
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Approval domain layer (entity, VOs, gate service, repo interfaces, events)</name>
  <files>
    src/approval/__init__.py,
    src/approval/domain/__init__.py,
    src/approval/domain/entities.py,
    src/approval/domain/value_objects.py,
    src/approval/domain/services.py,
    src/approval/domain/repositories.py,
    src/approval/domain/events.py,
    src/approval/DOMAIN.md,
    tests/unit/test_approval_domain.py,
    tests/unit/test_approval_gate.py
  </files>
  <behavior>
    - StrategyApproval creation with all fields: score_threshold, allowed_regimes, max_per_trade_pct, expires_at, daily_budget_cap
    - is_expired returns True when datetime.now(UTC) > expires_at
    - is_suspended returns True when suspended_reasons is non-empty
    - is_effective returns True only when is_active AND NOT is_expired AND NOT is_suspended
    - suspend("regime_change") adds to suspended_reasons; unsuspend("regime_change") removes it
    - Multiple suspend reasons: suspend("regime_change") + suspend("drawdown_tier2") both present; unsuspend one leaves the other
    - revoke() sets is_active=False
    - DailyBudgetTracker: remaining = budget_cap - spent; can_spend(amount) checks amount <= remaining
    - GateResult: approved=True/False with reason string
    - ApprovalGateService.check():
      - No approval -> GateResult(False, "No active approval")
      - Approval not effective -> GateResult(False, "Approval not effective")
      - Score below threshold -> GateResult(False, "Score X below threshold Y")
      - Regime not in allowed_regimes -> GateResult(False, reason)
      - Position pct > max_per_trade_pct -> GateResult(False, reason)
      - Position value > daily_remaining -> GateResult(False, reason)
      - All checks pass -> GateResult(True)
    - TradeReviewItem VO: symbol, plan_json, rejection_reason, pipeline_run_id, created_at, reviewed=False, expired=False
  </behavior>
  <action>
    Create `src/approval/` bounded context following existing DDD structure.

    1. `src/approval/__init__.py` and `src/approval/domain/__init__.py`: Export public API.

    2. `entities.py`: `StrategyApproval(Entity[str])` dataclass with:
       - _id, score_threshold, allowed_regimes (list[str]), max_per_trade_pct, expires_at, daily_budget_cap, created_at, is_active, suspended_reasons (set[str])
       - Properties: id, is_expired, is_suspended, is_effective
       - Methods: suspend(reason), unsuspend(reason), revoke()
       - Follow CooldownState pattern from execution context: frozen=False dataclass, expiry in Python not SQL

    3. `value_objects.py`:
       - `ApprovalStatus` enum: ACTIVE, SUSPENDED, EXPIRED, REVOKED
       - `GateResult` frozen dataclass: approved (bool), reason (str)
       - `DailyBudgetTracker` dataclass: budget_cap, date (str YYYY-MM-DD), spent, trade_count. Properties: remaining, can_spend(amount). Method: record_spend(amount)
       - `TradeReviewItem` dataclass: symbol, plan_json (str), rejection_reason, pipeline_run_id (optional), created_at, reviewed (bool), expired (bool)

    4. `services.py`: `ApprovalGateService` with check() method per research code example. Pure domain logic, no external imports.

    5. `repositories.py`: ABCs `IApprovalRepository` (get_active, save, find_by_id) and `IBudgetRepository` (get_or_create_today, save) and `IReviewQueueRepository` (add, list_pending, mark_reviewed, expire_old).

    6. `events.py`: `ApprovalSuspendedEvent(DomainEvent)` with approval_id, reason. `ApprovalCreatedEvent(DomainEvent)` with approval_id.

    7. `DOMAIN.md`: Bounded context documentation per DDD rules.

    8. Tests: Write tests first (TDD), then implement. Tests cover all behaviors listed above.
  </action>
  <verify>
    <automated>cd /home/mqz/workspace/trading && python -m pytest tests/unit/test_approval_domain.py tests/unit/test_approval_gate.py -x -v</automated>
  </verify>
  <done>StrategyApproval entity, DailyBudgetTracker, GateResult, ApprovalGateService, all ABCs, and domain events exist with all unit tests passing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SQLite persistence for approval, budget, and review queue</name>
  <files>
    src/approval/infrastructure/__init__.py,
    src/approval/infrastructure/sqlite_approval_repo.py,
    tests/unit/test_approval_persistence.py
  </files>
  <behavior>
    - SqliteApprovalRepository.save() persists a StrategyApproval to SQLite, get_active() retrieves the currently active one
    - Creating a new active approval deactivates all previous ones (single active approval rule)
    - suspended_reasons stored as JSON array in SQLite, reconstructed as set on load
    - allowed_regimes stored as JSON array
    - SqliteBudgetRepository.get_or_create_today() returns today's DailyBudgetTracker (creates with spent=0 if not exists)
    - SqliteBudgetRepository.save() updates spent and trade_count
    - SqliteReviewQueueRepository.add() inserts a TradeReviewItem, list_pending() returns non-reviewed non-expired items
    - SqliteReviewQueueRepository.mark_reviewed(id, approved) marks item as reviewed
    - SqliteReviewQueueRepository.expire_old(hours=24) marks items older than 24h as expired
  </behavior>
  <action>
    Create SQLite repositories following existing patterns (sqlite_cooldown_repo.py, sqlite_pipeline_repo.py).

    1. `sqlite_approval_repo.py`:
       - `SqliteApprovalRepository(IApprovalRepository)`: CREATE TABLE IF NOT EXISTS for strategy_approvals, daily_budget, trade_review_queue tables (schema from research). WAL journal mode. db_path constructor.
       - `SqliteBudgetRepository(IBudgetRepository)`: Uses same db_path, daily_budget table.
       - `SqliteReviewQueueRepository(IReviewQueueRepository)`: Uses same db_path, trade_review_queue table.
       - JSON serialization for allowed_regimes (json.dumps/loads) and suspended_reasons (json.dumps/loads for set->list->set).
       - `get_active()`: SELECT WHERE is_active=1, reconstruct entity. Return None if no active approval.
       - `save()`: Deactivate previous active approvals before inserting new one (if is_active=True). Use REPLACE INTO for upsert.

    2. `infrastructure/__init__.py`: Export SqliteApprovalRepository, SqliteBudgetRepository, SqliteReviewQueueRepository.

    3. Tests: Create test_approval_persistence.py with in-memory SQLite (:memory:) testing save/load round-trip, JSON serialization, deactivation of old approvals, budget daily reset, review queue CRUD, and expire_old.
  </action>
  <verify>
    <automated>cd /home/mqz/workspace/trading && python -m pytest tests/unit/test_approval_persistence.py -x -v</automated>
  </verify>
  <done>All three SQLite repositories persist and retrieve domain objects correctly, with round-trip JSON serialization for set/list fields, and all persistence tests pass.</done>
</task>

</tasks>

<verification>
All approval domain and persistence tests pass:
```bash
cd /home/mqz/workspace/trading && python -m pytest tests/unit/test_approval_domain.py tests/unit/test_approval_gate.py tests/unit/test_approval_persistence.py -x -v
```

No DDD violations (domain layer has no external imports):
```bash
cd /home/mqz/workspace/trading && grep -r "from src\.\(pipeline\|execution\|regime\|scoring\|signals\)" src/approval/domain/ || echo "No cross-context imports in domain layer"
```
</verification>

<success_criteria>
- StrategyApproval entity with is_effective, suspend/unsuspend, revoke
- DailyBudgetTracker with spent/remaining tracking and can_spend check
- ApprovalGateService.check() validates all 6 conditions (approval exists, effective, score, regime, position pct, budget)
- TradeReviewItem VO for rejected trades
- SQLite persistence with JSON serialization for set/list fields
- Single active approval rule enforced at repo level
- All unit tests pass
- No cross-context imports in domain layer
</success_criteria>

<output>
After completion, create `.planning/phases/14-strategy-and-budget-approval/14-01-SUMMARY.md`
</output>
